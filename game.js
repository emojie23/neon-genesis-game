(()=>{'use strict';
  const canvas=document.querySelector('#game'),ctx=canvas.getContext('2d'),W=1280,H=720,TAU=Math.PI*2,TOTAL_FLOORS=6;
  const ROOM={x:86,y:78,w:1108,h:574,cx:640,cy:365},COL={cyan:'#23f7ff',pink:'#ff2c92',violet:'#9b5cff',yellow:'#ffe34c',red:'#ff365f',ink:'#e9f8ff'},MUSIC_LEVEL=.9;
  const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)],clamp=(v,a,b)=>Math.max(a,Math.min(b,v)),rand=(a,b)=>a+Math.random()*(b-a),choice=a=>a[Math.floor(Math.random()*a.length)],chance=p=>Math.random()<p,dist=(a,b)=>Math.hypot(a.x-b.x,a.y-b.y);
  const ui={start:$('#start-screen'),story:$('#story-screen'),pause:$('#pause-screen'),gameover:$('#gameover-screen'),bossIntro:$('#boss-intro'),introPortrait:$('#intro-portrait'),bossHud:$('#boss-hud'),itemBanner:$('#item-banner'),itemRarity:$('#item-rarity'),itemSynergy:$('#item-synergy'),relicArray:$('#relic-array'),relicSlots:$('#relic-slots'),relicCount:$('#relic-count'),toast:$('#toast'),hearts:$('#hearts'),healthValue:$('#health-value'),healthStatus:$('#health-status'),shieldFill:$('#shield-fill'),shieldValue:$('#shield-value'),coins:$('#coins'),keys:$('#keys'),bombs:$('#bombs'),floor:$('#floor-label'),room:$('#room-label'),map:$('#minimap'),bossName:$('#boss-name'),bossPhase:$('#boss-phase'),bossFill:$('#boss-fill'),activeName:$('#active-name'),activeCharge:$('#active-charge'),music:$('#music-btn')};
  const shell=$('#game-shell'),fullscreenButton=$('#fullscreen-btn'),fullscreenLabel=fullscreenButton.querySelector('span'),rotateFullscreenButton=$('#rotate-fullscreen-btn'),rotateStatus=$('#rotate-status');
  const FORMS={
    vanguard:{name:'VANGUARD',color:COL.cyan,damage:3.15,delay:.38,speed:450,range:650,shots:1,spread:0,pierce:0,ricochets:1,size:7,move:245,desc:'均衡脉冲'},
    wraith:{name:'WRAITH',color:COL.violet,damage:1.8,delay:.17,speed:535,range:575,shots:2,spread:.075,pierce:0,ricochets:2,size:5,move:278,desc:'双重反弹'},
    beast:{name:'BEAST',color:COL.red,damage:3.75,delay:.53,speed:370,range:490,shots:3,spread:.19,pierce:0,ricochets:1,size:9,move:222,desc:'狂暴散射'},
    seraph:{name:'SERAPH',color:COL.yellow,damage:4.55,delay:.64,speed:620,range:800,shots:1,spread:0,pierce:2,ricochets:1,size:8,move:235,desc:'贯穿光矛'}
  };
  const ITEMS=[
    {id:'overclock',glyph:'⌁',name:'超频垂体',desc:'射击间隔 -22%',tag:'FIRE RATE UP',category:'skill',rarity:'rare',minFloor:1},
    {id:'prism',glyph:'△',name:'棱镜分裂器',desc:'+2 发弹体，扩散角增加',tag:'MULTISHOT',category:'weapon',rarity:'rare',minFloor:3},
    {id:'phase',glyph:'∞',name:'相位晶状体',desc:'穿透敌人 +2，但仍会被墙体反射',tag:'PIERCING',category:'weapon',rarity:'advanced',minFloor:2},
    {id:'volatile',glyph:'✹',name:'易爆泪腺',desc:'命中时产生小型爆炸',tag:'BLAST TEARS',category:'weapon',rarity:'advanced',minFloor:2},
    {id:'tesla',glyph:'ϟ',name:'特斯拉突触',desc:'命中后电击附近敌人',tag:'CHAIN LIGHTNING',category:'skill',rarity:'rare',minFloor:3},
    {id:'redchip',glyph:'◆',name:'红色运算核',desc:'基础伤害 +1.8',tag:'DAMAGE UP',category:'skill',rarity:'common',minFloor:1},
    {id:'cyberheart',glyph:'▣',name:'义体心室',desc:'+2 核心电池格并恢复生命',tag:'HP UP',category:'skill',rarity:'common',minFloor:1},
    {id:'ghost',glyph:'◌',name:'幽灵协议',desc:'反弹次数 +1，射程 +25%（上限 2）',tag:'RICOCHET',category:'weapon',rarity:'common',minFloor:1},
    {id:'drone',glyph:'◎',name:'子代无人机',desc:'环绕玩家并自动射击',tag:'FAMILIAR',category:'skill',rarity:'advanced',minFloor:2},
    {id:'echo',glyph:'⇆',name:'回声腔体',desc:'每次射击向后追加一发',tag:'BACKSHOT',category:'weapon',rarity:'advanced',minFloor:2},
    {id:'blood',glyph:'†',name:'血色契约',desc:'最大生命 -2 电池格，伤害 ×1.5',tag:'GLASS CANNON',category:'core',rarity:'rare',minFloor:4},
    {id:'magnet',glyph:'⊕',name:'磁性脐带',desc:'拾取范围大幅提升',tag:'MAGNETISM',category:'skill',rarity:'common',minFloor:1},
    {id:'afterimage',glyph:'⫷',name:'第五残像',desc:'每第 5 次射击追加两枚侧向幽灵弹',tag:'FIFTH ECHO',category:'weapon',rarity:'advanced',minFloor:2,synergy:'与棱镜分裂器组合时，可形成高密度交叉火网。'},
    {id:'saw-memory',glyph:'⟁',name:'锯齿记忆',desc:'弹体发生反弹后，造成伤害 +30%',tag:'RICOCHET FURY',category:'weapon',rarity:'advanced',minFloor:2,synergy:'幽灵协议与双生影会更频繁地触发该增幅。'},
    {id:'oracle-eye',glyph:'◉',name:'盲眼先知',desc:'暴击弹额外穿透 1 个敌人',tag:'CRIT PIERCING',category:'weapon',rarity:'rare',minFloor:3,synergy:'弱点演算和黄昏翼会把单次暴击延伸成贯穿处决。'},
    {id:'votive',glyph:'♢',name:'低压圣龛',desc:'生命低于一半时，伤害 +30%、暴击率 +10%',tag:'LAST VOLTAGE',category:'core',rarity:'rare',minFloor:3,synergy:'血色契约能更早开启低电量增幅，但容错也会更低。'},
    {id:'executioner',glyph:'⌗',name:'执刑标尺',desc:'对精英和 Boss 造成伤害 +25%',tag:'ARCHON BREAKER',category:'core',rarity:'rare',minFloor:3,synergy:'适合高单发形态，也能放大暴击与反弹后的最终伤害。'},
    {id:'blackbox',glyph:'▤',name:'无伤黑匣',desc:'无伤净化战斗房时获得 1 层护盾和 2 DATA',tag:'FLAWLESS CACHE',category:'skill',rarity:'common',minFloor:1,synergy:'防火墙扩容可以储存更多无伤奖励。'},
    {id:'choir',glyph:'♮',name:'机械合唱团',desc:'部署 1 架圣咏无人机；拥有子代无人机时增至 2 架',tag:'FAMILIAR CHOIR',category:'skill',rarity:'advanced',minFloor:2,synergy:'子代无人机将合唱团扩展为双机交错轨道。'},
    {id:'harvest',glyph:'⌁',name:'余烬收割器',desc:'每击杀 8 个敌人，为熵骰恢复 1 格充能',tag:'KILL HARVEST',category:'skill',rarity:'advanced',minFloor:2,synergy:'高射速、多重射击和连锁电击能更快完成收割循环。'}
  ];
  const RELIC_FAMILIES={
    weapon:{label:'ARSENAL',name:'兵装遗物',color:COL.cyan},
    skill:{label:'ORGAN',name:'义体器官',color:COL.violet},
    core:{label:'COVENANT',name:'禁忌契约',color:COL.red},
    ascension:{label:'ASCENSION',name:'升格协议',color:COL.yellow},
    tuning:{label:'TUNING',name:'武器调校',color:COL.cyan}
  };
  const RELIC_RARITIES={
    common:{label:'COMMON',color:'#8eb4c8'},
    advanced:{label:'PROTOTYPE',color:COL.cyan},
    rare:{label:'FORBIDDEN',color:COL.yellow},
    ascension:{label:'ASCENDED',color:COL.pink}
  };
  for(const item of ITEMS){const family=RELIC_FAMILIES[item.category]||RELIC_FAMILIES.skill,rarity=RELIC_RARITIES[item.rarity]||RELIC_RARITIES.common;item.family=item.category;item.familyLabel=family.label;item.familyName=family.name;item.color=item.color||rarity.color;item.rarityLabel=rarity.label;item.synergy=item.synergy||`${family.name} // 可与其他${family.name}共同构成神经链路。`}
  const TUNING=[
    {id:'tune-caliber',glyph:'▲',name:'口径校准',desc:'基础伤害 +0.75',tag:'DAMAGE TUNE',stackable:true,max:6,color:COL.red},
    {id:'tune-trigger',glyph:'»',name:'扳机加速',desc:'射击间隔 -10%',tag:'FIRE RATE TUNE',stackable:true,max:6,color:COL.cyan},
    {id:'tune-velocity',glyph:'➤',name:'弹道增压',desc:'弹速与射程 +12%',tag:'VELOCITY TUNE',stackable:true,max:5,color:COL.yellow},
    {id:'tune-target',glyph:'⌖',name:'弱点演算',desc:'暴击率 +5%',tag:'CRITICAL TUNE',stackable:true,max:5,color:COL.violet},
    {id:'tune-servo',glyph:'↟',name:'神经伺服',desc:'移动速度 +7%',tag:'MOBILITY TUNE',stackable:true,max:5,color:COL.cyan},
    {id:'tune-barrier',glyph:'⬡',name:'防火墙扩容',desc:'护盾上限 +1 并补充 1 层',tag:'DEFENSE TUNE',stackable:true,max:4,color:COL.violet}
  ];
  const ASCENSIONS=[
    {id:'ascend-fury',glyph:'✦',name:'无限火力升格',desc:'基础伤害 +0.32，可重复载入',tag:'ASCENSION DAMAGE',category:'ascension',stackable:true,repeatable:true,max:999,color:COL.red},
    {id:'ascend-cycle',glyph:'⌘',name:'无限循环升格',desc:'射击间隔 -5%，可重复载入',tag:'ASCENSION RATE',category:'ascension',stackable:true,repeatable:true,max:999,color:COL.cyan},
    {id:'ascend-focus',glyph:'◈',name:'无限弱点升格',desc:'暴击率 +2%，可重复载入',tag:'ASCENSION CRIT',category:'ascension',stackable:true,repeatable:true,max:999,color:COL.yellow}
  ];
  for(const item of TUNING)Object.assign(item,{family:'tuning',familyLabel:RELIC_FAMILIES.tuning.label,familyName:RELIC_FAMILIES.tuning.name,rarity:'common',rarityLabel:'CALIBRATION',synergy:item.synergy||'重复装载会稳定强化基础参数，并保留原有遗物协同。'});
  for(const item of ASCENSIONS)Object.assign(item,{family:'ascension',familyLabel:RELIC_FAMILIES.ascension.label,familyName:RELIC_FAMILIES.ascension.name,rarity:'ascension',rarityLabel:RELIC_RARITIES.ascension.label,synergy:item.synergy||'当普通遗物池耗尽后，升格协议仍可持续突破构筑上限。'});
  const FLOOR_POWER=[1,2,4,8,16,32];
  const GROWTH_CHANCES=[[.15,.2,.25],[.3,.4,.5],[.55,.65,.75],[.68,.76,.84],[.76,.84,.92],[.84,.92,.98]];
  const GROWTH_TIERS=[{tuning:.7,skill:.25,rare:.05},{tuning:.7,skill:.25,rare:.05},{tuning:.45,skill:.4,rare:.15},{tuning:.45,skill:.4,rare:.15},{tuning:.25,skill:.45,rare:.3},{tuning:.25,skill:.45,rare:.3}];
  const SUPPLY_TABLES=[{coin:.35,heart:.2,shield:.15,bomb:.15,key:.15},{coin:.3,heart:.18,shield:.15,bomb:.17,key:.2},{coin:.25,heart:.15,shield:.15,bomb:.2,key:.25},{coin:.35,heart:.2,shield:.2,bomb:.25,key:0},{coin:.35,heart:.2,shield:.2,bomb:.25,key:0},{coin:.35,heart:.2,shield:.2,bomb:.25,key:0}];
  const SPECIAL_ROOM_COSTS={vault:1,sanctum:2};
  const BOSS_DATA=[
    {id:'cherub',name:'赛博智天使',subtitle:'BROKEN HALO // THREE ASCENSIONS',color:COL.cyan,hp:150},
    {id:'matriarch',name:'空相主母',subtitle:'PREDICTION ENGINE // THREE SHELLS',color:COL.violet,hp:235},
    {id:'hexa',name:'六相母体',subtitle:'ORIGIN PROCESS // FIRST INCARNATION',color:COL.pink,hp:360},
    {id:'cherub',name:'炽化智天使',subtitle:'OVERDRIVE HALO // ASCENDED REPLAY',color:COL.yellow,hp:480,tier:2},
    {id:'matriarch',name:'猩红主母',subtitle:'PREDICTION ENGINE // RED SHIFT',color:COL.red,hp:650,tier:2},
    {id:'hexa',name:'六相原罪体',subtitle:'ORIGIN PROCESS // FINAL INCARNATION',color:COL.pink,hp:900,tier:2}
  ];
  const COMBAT_CURVE=[
    [
      ['crawler','crawler','drone','shooter'],
      ['crawler','crawler','drone','drone','shooter'],
      ['crawler','drone','drone','shooter','shooter','charger']
    ],
    [
      ['crawler','crawler','drone','shooter','shooter','charger'],
      ['crawler','drone','drone','shooter','shooter','charger','turret'],
      ['crawler','drone','drone','shooter','shooter','charger','charger','turret']
    ],
    [
      ['crawler','drone','drone','shooter','shooter','charger','charger','turret'],
      ['crawler','drone','drone','shooter','shooter','shooter','charger','charger','turret','turret'],
      ['crawler','drone','drone','drone','shooter','shooter','shooter','shooter','charger','charger','turret','turret']
    ],
    [
      ['crawler','drone','drone','drone','shooter','shooter','shooter','charger','charger','turret','turret'],
      ['crawler','drone','drone','drone','shooter','shooter','shooter','shooter','charger','charger','turret','turret'],
      ['crawler','drone','drone','drone','shooter','shooter','shooter','shooter','charger','charger','charger','turret','turret']
    ],
    [
      ['crawler','drone','drone','drone','shooter','shooter','shooter','charger','charger','turret','turret','turret'],
      ['crawler','drone','drone','drone','shooter','shooter','shooter','shooter','charger','charger','turret','turret','turret'],
      ['crawler','drone','drone','drone','shooter','shooter','shooter','shooter','charger','charger','charger','turret','turret','turret']
    ],
    [
      ['crawler','drone','drone','drone','shooter','shooter','shooter','shooter','charger','charger','turret','turret','turret'],
      ['crawler','drone','drone','drone','shooter','shooter','shooter','shooter','charger','charger','charger','turret','turret','turret'],
      ['crawler','drone','drone','drone','shooter','shooter','shooter','shooter','shooter','charger','charger','charger','turret','turret','turret']
    ]
  ];
  const ROOM_BULLET_CAPS=[10,12,14,18,22,26,32,40,48,52,58,64,66,70,76,78,82,88];
  const BOSS_CURVE={
    cherub:[
      {pressure:1,bulletCap:14,windup:1.45,fan:3,spread:.22,speed:165,cooldown:1.25},
      {pressure:2,bulletCap:22,windup:1,radial:8,speed:160,cooldown:1.35,dashWarning:.72,dashReset:2.5},
      {pressure:3,bulletCap:30,windup:.85,radial:10,radialSpeed:185,fan:3,spread:.18,fanSpeed:220,cooldown:.95,summonCooldown:4.6}
    ],
    matriarch:[
      {pressure:4,bulletCap:20,windup:1.3,hazardRadius:70,hazardLife:.88,damage:1,cooldown:.9,teleportCooldown:2.5},
      {pressure:5,bulletCap:30,windup:.95,radial:8,speed:145,cooldown:1.25},
      {pressure:6,bulletCap:38,windup:.8,fan:7,spread:.13,speed:255,hazardRadius:92,hazardLife:.78,damage:2,cooldown:.82,dashWarning:.52,dashReset:1.8}
    ],
    hexa:[
      {pressure:7,bulletCap:28,windup:1.15,radial:10,speed:180,hazardRadius:66,hazardLife:.72,damage:1,cooldown:1},
      {pressure:8,bulletCap:42,windup:.85,radial:16,speed:200,cooldown:1.1,summonCooldown:3},
      {pressure:9,bulletCap:56,windup:.72,radial:20,radialSpeed:245,fan:7,spread:.12,fanSpeed:295,hazardRadius:108,hazardLife:.7,damage:3,cooldown:.72,dashWarning:.46,dashReset:1.6}
    ]
  };
  const STORY=[
    {chapter:'MEMORY 01',code:'ARCHIVE // 00',scene:'prologue',transition:'boot',eyebrow:'THE MEMORY CHAIN',title:'第零号冷柜',text:'你在城市最深处醒来。名字已被删除，只剩四段等待重新接入的战斗人格。'},
    {chapter:'MEMORY 02',code:'HOVER / TAP // SELECT',scene:'economy',transition:'scan',eyebrow:'SAVE NOW // ASCEND LATER',title:'深层经济法则',text:'DATA 用于购买遗物；前三层清剿区会产出神经密钥。遗物库与黑市不再收取钥匙，把它们留给第四层之后的量子武库与升格实验室。',direction:'¢',form:'RESOURCE // DATA · KEY · CHARGE',weapon:'三种生存资源',desc:'黄色 DATA 用于购买；◇ 为深层门禁储备；● 爆破与寻秘。',color:COL.yellow},
    {chapter:'MEMORY 03',code:'WEAPON // V-01',scene:'vanguard',transition:'slide',eyebrow:'WESTERN DEFENSE RESTORED',title:'第一段：守线者',text:'安保无人机封锁西向通路。你从残存的肌肉记忆中，取回最稳定的一次扣动。',direction:'←',form:'FORM // VANGUARD',weapon:'脉冲圣痕',desc:'稳定单发；伤害、射程与节奏均衡。',color:COL.cyan},
    {chapter:'MEMORY 04',code:'WEAPON // W-02',scene:'wraith',transition:'split',eyebrow:'EASTERN MIRROR BREACHED',title:'第二段：双生影',text:'镜像病毒从东侧渗入。你必须同时存在于两条时间线，才能击中它不存在的本体。',direction:'→',form:'FORM // WRAITH',weapon:'双相针列',desc:'高速双发；以低单发伤害换取密集压制。',color:COL.violet},
    {chapter:'MEMORY 05',code:'WEAPON // B-03',scene:'beast',transition:'impact',eyebrow:'LOWER INSTINCT UNCHAINED',title:'第三段：赤兽',text:'下层肉机开始吞噬信号。疼痛不再是警告，而是被你重新编译成扩散火力。',direction:'↓',form:'FORM // BEAST',weapon:'裂颚散射器',desc:'三向重弹；贴近目标时爆发最高。',color:COL.red},
    {chapter:'MEMORY 06',code:'WEAPON // S-04',scene:'seraph',transition:'iris',eyebrow:'UPLINK TO THE FALSE HEAVEN',title:'第四段：黄昏翼',text:'母体在高处伪造天堂。你只剩一束能够贯穿成列装甲、并从墙面折返的光。',direction:'↑',form:'FORM // SERAPH',weapon:'日蚀光矛',desc:'低射速贯穿敌人；射程与单发威力最高，墙面反弹 1 次。',color:COL.yellow},
    {chapter:'MEMORY 07',code:'HOVER / TAP // EIGHT ROOMS',scene:'rooms',transition:'map',eyebrow:'BUILD FIRST // SPEND LATER',title:'八类房间协议',text:'清剿区可能生成二至三选一发育终端；遗物库与黑市免费进入。深层青色和金色门才会消耗前三层储备的神经密钥。',direction:'✕',form:'ROOM // PURGE CHAMBER',weapon:'清剿区',desc:'清房固定掉落补给，并按层数概率生成发育终端。连续两房落空后，下一房保底。',color:COL.red},
    {chapter:'MEMORY 08',code:'THREAT // ARCHON',scene:'boss',transition:'warning',eyebrow:'THREE FORMS // ONE EXECUTION',title:'执政官警报',text:'每层终点都由三形态 Boss 看守。生命降至 66% 与 33% 时会变形并清除旧弹幕；直线与圆形预警代表即将到来的致命区域。',direction:'!',form:'BOSS // THREE INCARNATIONS',weapon:'三相执刑者',desc:'先读预警再输出；Boss 倒下时，全部敌弹与危险区会立即熄灭。',color:COL.pink},
    {chapter:'MEMORY 09',code:'SYNC // 100%',scene:'prologue',transition:'collapse',eyebrow:'FOUR SELVES // SIX SECTORS',title:'四相重合',text:'从脆弱的初始协议出发，在六层记忆链中积累调校、武器模组与稀有协同，最终把原罪体的火力变成你的构筑燃料。'}
  ];
  const RESOURCE_GUIDE={
    data:{glyph:'¢',form:'RESOURCE // DATA CURRENCY',name:'DATA 数据币',desc:'敌人和房间奖励会掉落。靠近自动拾取，用于黑市购买遗物。',color:COL.yellow},
    key:{glyph:'◇',form:'RESOURCE // NEURAL KEY',name:'神经密钥',desc:'只在前三层清房补给与早期幽灵缓存中产出，上限 3 枚。遗物库和黑市免费；密钥留给第四层后的量子武库与升格实验室。',color:COL.cyan},
    charge:{glyph:'●',form:'RESOURCE // BREACH CHARGE',name:'爆破电荷',desc:'按 E 部署。可摧毁可破坏墙体，并炸开通往幽灵缓存的隐藏墙。',color:COL.pink}
  };
  const ROOM_GUIDE={
    start:{glyph:'⌂',code:'ROOM 01 // SAFE NODE',form:'ROOM // SAFE NODE',name:'安全节点',desc:'每层的落点与整备区。这里没有敌人，先读取相邻门色，再规划下潜路线。',risk:'风险：无',reward:'重点：规划路线',color:COL.cyan},
    combat:{glyph:'✕',code:'ROOM 02 // PURGE',form:'ROOM // PURGE CHAMBER',name:'清剿区',desc:'进入即封门；清房固定掉落补给，并按逐层提高的概率生成发育终端。终端只可选择一项强化。',risk:'风险：封门战斗',reward:'奖励：补给 + 概率成长',color:COL.red},
    treasure:{glyph:'◆',code:'ROOM 03 // RELIC',form:'ROOM // RELIC VAULT',name:'遗物库',desc:'黄色信号门，免费进入。房内稳定提供两件永久遗物，只能选择其中一件。',risk:'门禁：免费',reward:'奖励：遗物二选一',color:COL.yellow},
    shop:{glyph:'¢',code:'ROOM 04 // MARKET',form:'ROOM // BLACK MARKET',name:'黑市',desc:'紫色信号门，免费进入。使用 DATA 购买三件固定库存，离开再返回不会刷新货架。',risk:'门禁：免费',reward:'消费：DATA',color:COL.violet},
    secret:{glyph:'◌',code:'ROOM 05 // GHOST',form:'ROOM // GHOST CACHE',name:'幽灵缓存',desc:'默认不会出现在地图上。寻找异常虚线墙，使用爆破电荷打开隐藏入口。',risk:'入口：虚线墙',reward:'工具：爆破电荷',color:COL.ink},
    boss:{glyph:'!',code:'ROOM 06 // ARCHON',form:'ROOM // ARCHON GATE',name:'执政官门',desc:'粉色终点门。执政官拥有三种形态；胜利后从三件 Boss 遗物中选择一件，并开启下层入口。',risk:'风险：最高',reward:'奖励：Boss 三选一',color:COL.pink},
    vault:{glyph:'◇',code:'ROOM 07 // QUANTUM',form:'ROOM // QUANTUM VAULT',name:'量子武库',desc:'第四层后出现的青色特殊门，首次进入消耗 1 枚神经密钥，提供三项高价值模组选择。',risk:'门禁：密钥 ×1',reward:'奖励：高阶三选一',color:COL.cyan},
    sanctum:{glyph:'✦',code:'ROOM 08 // ASCENSION',form:'ROOM // ASCENSION LAB',name:'升格实验室',desc:'最终层金色特殊门，首次进入消耗 2 枚神经密钥，提供三项稀有协同核心。',risk:'门禁：密钥 ×2',reward:'奖励：稀有三选一',color:COL.yellow}
  };
  const PLANS=[
    [{x:0,y:0,type:'start'},{x:1,y:0,type:'combat'},{x:1,y:-1,type:'treasure'},{x:2,y:0,type:'combat'},{x:2,y:1,type:'shop'},{x:2,y:-1,type:'secret'},{x:3,y:0,type:'combat'},{x:4,y:0,type:'boss'}],
    [{x:0,y:0,type:'start'},{x:0,y:-1,type:'combat'},{x:-1,y:-1,type:'treasure'},{x:1,y:-1,type:'combat'},{x:1,y:0,type:'shop'},{x:2,y:-1,type:'combat'},{x:2,y:0,type:'secret'},{x:3,y:-1,type:'boss'}],
    [{x:0,y:0,type:'start'},{x:1,y:0,type:'combat'},{x:1,y:1,type:'treasure'},{x:2,y:0,type:'combat'},{x:2,y:-1,type:'shop'},{x:3,y:0,type:'combat'},{x:3,y:1,type:'secret'},{x:4,y:0,type:'boss'}],
    [{x:0,y:0,type:'start'},{x:1,y:0,type:'combat'},{x:1,y:-1,type:'treasure'},{x:2,y:0,type:'combat'},{x:2,y:1,type:'shop'},{x:2,y:-1,type:'vault'},{x:3,y:0,type:'combat'},{x:3,y:1,type:'secret'},{x:4,y:0,type:'boss'}],
    [{x:0,y:0,type:'start'},{x:0,y:-1,type:'combat'},{x:-1,y:-1,type:'treasure'},{x:1,y:-1,type:'combat'},{x:1,y:0,type:'shop'},{x:1,y:-2,type:'vault'},{x:2,y:-1,type:'combat'},{x:2,y:0,type:'secret'},{x:3,y:-1,type:'boss'}],
    [{x:0,y:0,type:'start'},{x:1,y:0,type:'combat'},{x:1,y:1,type:'treasure'},{x:1,y:-1,type:'vault'},{x:2,y:0,type:'combat'},{x:2,y:-1,type:'shop'},{x:2,y:1,type:'sanctum'},{x:3,y:0,type:'combat'},{x:3,y:1,type:'secret'},{x:4,y:0,type:'boss'}]
  ];
  const ROOM_NAMES={start:'SAFE NODE',combat:'PURGE CHAMBER',treasure:'RELIC VAULT',shop:'BLACK MARKET',secret:'GHOST CACHE',boss:'ARCHON GATE',vault:'QUANTUM VAULT',sanctum:'ASCENSION LAB'};
  const state={mode:'menu',floor:1,rooms:[],room:null,enemies:[],tears:[],enemyBullets:[],pickups:[],particles:[],hazards:[],bombEntities:[],pedestals:[],portal:null,obstacles:[],kills:0,critHits:0,roomsCleared:0,growthMisses:0,floorKeysSpawned:0,time:0,shake:0,flash:0,transition:0,itemTimer:0,bossIntroTimer:0,bossIntroToken:0,victory:false};
  const player={x:ROOM.cx,y:ROOM.cy,prevX:ROOM.cx,prevY:ROOM.cy,r:18,hp:6,maxHp:6,shield:0,maxShield:4,coins:5,keys:0,bombs:2,form:'vanguard',items:[],upgrades:{},fireCd:0,invuln:0,formCd:0,activeCharge:3,activeMax:3,pickupRange:80,aimX:1,aimY:0,faceX:1,recoil:0,critOverride:null,damageTaken:0,shotSequence:0,killHarvest:0};
  const input={keys:new Set(),moveX:0,moveY:0,shootX:0,shootY:0,pointerAim:false,mouseInside:false,mouseX:ROOM.cx,mouseY:ROOM.cy};
  let uid=1,last=performance.now(),audio=null,musicGain=null,musicLimiter=null,musicAnalyser=null,sfxGain=null,noiseBuffer=null,musicTimer=null,audioHeartbeatTimer=null,audioResumePromise=null,audioStalled=false,musicStep=0,musicTicks=0,musicNotes=0,musicEnabled=true,storyIndex=0,toastTimer=null,lastRicochetSound=-1;
  let shotBuffers=null,shotSfxVoices=0,shotSfxPlayed=0,audioResumeRequests=0,lastShotSoundTime=-1,forcedRewardTier=null,forcedSupplyKind=null;
  const uiCache={};
  function makeGlowSprite(color){const sprite=document.createElement('canvas');sprite.width=sprite.height=64;const c=sprite.getContext('2d'),hex=color.slice(1),r=parseInt(hex.slice(0,2),16),g=parseInt(hex.slice(2,4),16),b=parseInt(hex.slice(4,6),16),gradient=c.createRadialGradient(32,32,1,32,32,31);gradient.addColorStop(0,`rgba(${r},${g},${b},.72)`);gradient.addColorStop(.24,`rgba(${r},${g},${b},.34)`);gradient.addColorStop(1,`rgba(${r},${g},${b},0)`);c.fillStyle=gradient;c.fillRect(0,0,64,64);return sprite}
  const tearGlowSprites={vanguard:makeGlowSprite(COL.cyan),wraith:makeGlowSprite(COL.violet),beast:makeGlowSprite(COL.red),seraph:makeGlowSprite(COL.yellow),drone:makeGlowSprite(COL.cyan)};
  const projectileGlowSprites={[COL.cyan]:tearGlowSprites.vanguard,[COL.violet]:tearGlowSprites.wraith,[COL.red]:tearGlowSprites.beast,[COL.yellow]:tearGlowSprites.seraph,[COL.pink]:makeGlowSprite(COL.pink)};
  const roomLayer=document.createElement('canvas'),roomLayerCtx=roomLayer.getContext('2d'),vignetteLayer=document.createElement('canvas'),vignetteCtx=vignetteLayer.getContext('2d');roomLayer.width=vignetteLayer.width=W;roomLayer.height=vignetteLayer.height=H;let roomLayerKey='';
  {const gradient=vignetteCtx.createRadialGradient(W/2,H/2,260,W/2,H/2,700);gradient.addColorStop(0,'transparent');gradient.addColorStop(1,'rgba(0,0,0,.62)');vignetteCtx.fillStyle=gradient;vignetteCtx.fillRect(0,0,W,H)}
  function loadAtlas(src){const image=new Image();image.decoding='async';image.src=src;return image}
  const SPRITE_ATLASES={
    player:loadAtlas('assets/sprites/protagonist-atlas-v2.png'),
    boss:loadAtlas('assets/sprites/boss-atlas-v2.png'),
    enemy:loadAtlas('assets/sprites/enemy-atlas-v2.png')
  };
  const BOSS_ATLASES={
    cherub:{src:'assets/sprites/boss-cherub-atlas-v3.png',image:null,columns:6,rows:3,frames:18},
    matriarch:{src:'assets/sprites/boss-matriarch-atlas-v3.png',image:null,columns:6,rows:3,frames:18},
    hexa:{src:'assets/sprites/boss-hexa-atlas-v3.png',image:null,columns:5,rows:3,frames:15}
  };
  const FORM_SPRITE_ROWS={vanguard:0,wraith:1,beast:2,seraph:3},BOSS_SPRITE_ROWS={cherub:0,matriarch:1,hexa:2},ENEMY_SPRITE_ROWS={crawler:0,shooter:1,charger:2,turret:3,drone:4};
  function atlasReady(image){return !!(image?.complete&&image.naturalWidth&&image.naturalHeight)}
  function cacheAtlasFrames(image,columns,rows,width,height){if(!atlasReady(image)||image.frames)return;const sw=image.naturalWidth/columns,sh=image.naturalHeight/rows;image.frames=[];for(let row=0;row<rows;row++)for(let column=0;column<columns;column++){const frame=document.createElement('canvas'),fc=frame.getContext('2d');frame.width=width;frame.height=height;fc.imageSmoothingEnabled=true;fc.imageSmoothingQuality='high';fc.drawImage(image,column*sw,row*sh,sw,sh,0,0,width,height);image.frames.push(frame)}}
  const ATLAS_CACHE_SPECS=[[SPRITE_ATLASES.player,4,4,160,160],[SPRITE_ATLASES.boss,4,3,300,300],[SPRITE_ATLASES.enemy,4,5,180,116]];
  for(const spec of ATLAS_CACHE_SPECS){const [image,...args]=spec;image.addEventListener('load',()=>cacheAtlasFrames(image,...args),{once:true});if(atlasReady(image))cacheAtlasFrames(image,...args)}
  function ensureBossAtlas(id){const entry=BOSS_ATLASES[id];if(!entry)return null;if(!entry.image){entry.image=loadAtlas(entry.src);const prepare=()=>cacheAtlasFrames(entry.image,entry.columns,entry.rows,260,260);entry.image.addEventListener('load',prepare,{once:true});if(atlasReady(entry.image))prepare()}return entry}
  function drawAtlasCell(image,row,column,columns,rows,width,height){if(!atlasReady(image))return false;const cached=image.frames?.[row*columns+column];if(cached){ctx.drawImage(cached,-width/2,-height/2,width,height);return true}const sw=image.naturalWidth/columns,sh=image.naturalHeight/rows;ctx.drawImage(image,column*sw,row*sh,sw,sh,-width/2,-height/2,width,height);return true}

  function has(id){return player.items.includes(id)}
  function upgradeLevel(id){return player.upgrades[id]||0}
  function canTakeItem(item){return !!item&&(item.stackable?upgradeLevel(item.id)<item.max:!has(item.id))}
  function sampleUnique(pool,count){const copy=[...pool],result=[];while(copy.length&&result.length<count)result.push(copy.splice(Math.floor(Math.random()*copy.length),1)[0]);return result}
  function availableTunings(exclude=[]){return TUNING.filter(item=>upgradeLevel(item.id)<item.max&&!exclude.includes(item.id))}
  function availableAscensions(exclude=[]){return ASCENSIONS.filter(item=>upgradeLevel(item.id)<item.max&&!exclude.includes(item.id))}
  function rollTuning(exclude=[]){const pool=availableTunings(exclude);return pool.length?choice(pool):null}
  function relicCandidates(exclude=[],predicate=()=>true){return ITEMS.filter(item=>item.minFloor<=state.floor&&!has(item.id)&&!exclude.includes(item.id)&&predicate(item))}
  function fillChoices(seed,count,exclude=[],rareOnly=false){const result=[...seed],used=[...exclude,...result.map(item=>item.id)];const add=pool=>{for(const item of sampleUnique(pool.filter(candidate=>!used.includes(candidate.id)),count-result.length)){result.push(item);used.push(item.id)}};if(!rareOnly)add(availableTunings(used));if(result.length<count)add(availableAscensions(used));return result.slice(0,count)}
  function rollRelicChoices(count,exclude=[],predicate=()=>true){return fillChoices(sampleUnique(relicCandidates(exclude,predicate),count),count,exclude)}
  function growthTierFor(floor=state.floor,roll=Math.random()){const curve=GROWTH_TIERS[clamp(floor-1,0,GROWTH_TIERS.length-1)];return roll<curve.tuning?'tuning':roll<curve.tuning+curve.skill?'skill':'rare'}
  function growthOptions(tier,count=state.floor>=4?3:2){if(tier==='rare'){const result=sampleUnique(relicCandidates([],item=>item.rarity==='rare'),count);return sampleUnique(fillChoices(result,count,[],true),count)}if(tier==='vault'){const result=sampleUnique(relicCandidates([],item=>item.rarity!=='common'),Math.min(2,count));return sampleUnique(fillChoices(result,count),count)}if(tier==='skill'){const result=sampleUnique(relicCandidates([],item=>item.rarity!=='rare'),Math.min(state.floor>=4?2:1,count));return sampleUnique(fillChoices(result,count),count)}return sampleUnique(fillChoices(sampleUnique(availableTunings(),count),count),count)}
  function resetRun(){
    Object.assign(player,{x:ROOM.cx,y:ROOM.cy,prevX:ROOM.cx,prevY:ROOM.cy,hp:6,maxHp:6,shield:0,maxShield:4,coins:5,keys:0,bombs:2,form:'vanguard',items:[],upgrades:{},fireCd:0,invuln:1,formCd:0,activeCharge:3,activeMax:3,pickupRange:80,aimX:1,aimY:0,faceX:1,recoil:0,critOverride:null,damageTaken:0,shotSequence:0,killHarvest:0});
    Object.assign(state,{mode:'playing',floor:1,enemies:[],tears:[],enemyBullets:[],pickups:[],particles:[],hazards:[],bombEntities:[],pedestals:[],portal:null,kills:0,critHits:0,roomsCleared:0,growthMisses:0,floorKeysSpawned:0,time:0,shake:0,flash:0,transition:0,itemTimer:0,victory:false});forcedRewardTier=null;forcedSupplyKind=null;ui.itemBanner.classList.add('hidden');
    loadFloor(1);hideOverlays();startMusic();updateUI();beep(420,.12,'sine',.03);
  }
  function hideOverlays(){for(const el of [ui.start,ui.story,ui.pause,ui.gameover,ui.bossIntro])el.classList.remove('visible')}
  function beginStory(){state.mode='story';storyIndex=0;ui.start.classList.remove('visible');ui.story.classList.add('visible');startMusic();showStory()}
  function showStory(){
    const slide=STORY[storyIndex],scene=$('#story-scene'),card=$('#weapon-card');scene.className=`story-scene scene-${slide.scene} transition-${slide.transition||'boot'}`;void scene.offsetWidth;scene.classList.add('switching')
    $('#story-chapter').textContent=slide.chapter;$('#story-count').textContent=`${String(storyIndex+1).padStart(2,'0')} / ${String(STORY.length).padStart(2,'0')}`;$('#story-progress-fill').style.width=`${(storyIndex+1)/STORY.length*100}%`;$('#weapon-code').textContent=slide.code;$('#story-eyebrow').textContent=slide.eyebrow;$('#story-title').textContent=slide.title;$('#story-text').textContent=slide.text;
    const schematic=$('#weapon-schematic');schematic.className=`weapon-schematic ${slide.scene}`;card.classList.remove('resource-pop','room-pop');card.classList.toggle('hidden',!slide.weapon);if(slide.weapon){card.style.setProperty('--weapon-color',slide.color);$('#weapon-direction').textContent=slide.direction;$('#weapon-class').textContent=slide.form;$('#weapon-name').textContent=slide.weapon;$('#weapon-desc').textContent=slide.desc}configureResourceGuide(slide.scene==='economy');configureRoomGuide(slide.scene==='rooms');if(slide.scene==='economy')selectResource('data');if(slide.scene==='rooms')selectRoomGuide('combat')
    $('#story-next').innerHTML=storyIndex===STORY.length-1?'开始下潜 <small>ENTER</small>':'继续同步 <small>ENTER</small>';requestAnimationFrame(()=>{const target=slide.scene==='rooms'?$('#room-guide [data-room-guide="combat"]'):$('#story-title');target?.focus({preventScroll:true})});beep(250+storyIndex*62,.09,'square',.018)
  }
  function configureResourceGuide(active){const schematic=$('#weapon-schematic'),parts=[...schematic.children].slice(0,3),ids=['data','key','charge'];parts.forEach((part,i)=>{part.classList.remove('active');if(active){part.dataset.resource=ids[i];part.setAttribute('role','button');part.setAttribute('tabindex','0');part.setAttribute('aria-label',RESOURCE_GUIDE[ids[i]].name)}else{delete part.dataset.resource;part.removeAttribute('role');part.removeAttribute('tabindex');part.removeAttribute('aria-label')}});schematic.classList.remove('interacting');delete schematic.dataset.resource;$('#weapon-card').classList.remove('resource-pop')}
  function selectResource(id,sound=false){const data=RESOURCE_GUIDE[id],schematic=$('#weapon-schematic'),card=$('#weapon-card');if(!data||!schematic.classList.contains('economy'))return;const changed=schematic.dataset.resource!==id;schematic.classList.add('interacting');schematic.dataset.resource=id;for(const part of schematic.children)part.classList.toggle('active',part.dataset.resource===id);card.style.setProperty('--weapon-color',data.color);$('#weapon-direction').textContent=data.glyph;$('#weapon-class').textContent=data.form;$('#weapon-name').textContent=data.name;$('#weapon-desc').textContent=data.desc;card.classList.remove('resource-pop');void card.offsetWidth;card.classList.add('resource-pop');if(sound&&changed)beep(id==='data'?720:id==='key'?520:260,.045,'square',.018)}
  function configureRoomGuide(active){const guide=$('#room-guide'),facts=$('#guide-facts'),buttons=$$('[data-room-guide]');guide.classList.toggle('active',active);guide.classList.remove('interacting');guide.setAttribute('aria-hidden',String(!active));delete guide.dataset.room;for(const button of buttons){button.classList.remove('active');button.tabIndex=active?0:-1;button.setAttribute('aria-selected','false')}facts.classList.add('hidden');$('#weapon-card').classList.remove('room-pop')}
  function selectRoomGuide(id,sound=false){const data=ROOM_GUIDE[id],guide=$('#room-guide'),card=$('#weapon-card');if(!data||!guide.classList.contains('active'))return;const changed=guide.dataset.room!==id;guide.dataset.room=id;guide.classList.add('interacting');for(const button of $$('[data-room-guide]')){const selected=button.dataset.roomGuide===id;button.classList.toggle('active',selected);button.tabIndex=selected?0:-1;button.setAttribute('aria-selected',String(selected))}card.style.setProperty('--weapon-color',data.color);$('#weapon-code').textContent=data.code;$('#weapon-direction').textContent=data.glyph;$('#weapon-class').textContent=data.form;$('#weapon-name').textContent=data.name;$('#weapon-desc').textContent=data.desc;$('#guide-risk').textContent=data.risk;$('#guide-reward').textContent=data.reward;$('#guide-facts').classList.remove('hidden');card.classList.remove('room-pop');void card.offsetWidth;card.classList.add('room-pop');if(sound&&changed)beep({start:620,combat:180,treasure:760,shop:440,secret:330,boss:110,vault:560,sanctum:820}[id],.055,'square',.018)}
  function advanceStory(){if(state.mode!=='story')return;if(storyIndex<STORY.length-1){storyIndex++;showStory()}else resetRun()}
  function loadFloor(number){
    cancelBossIntro();state.mode='playing';state.room=null;state.floor=number;state.floorKeysSpawned=0;state.shake=0;state.flash=0;ui.bossHud.classList.add('hidden');state.rooms=PLANS[number-1].map((r,i)=>({...r,key:`${r.x},${r.y}`,index:i,visited:false,cleared:['start','treasure','shop','secret','vault','sanctum'].includes(r.type),revealed:r.type!=='secret',itemTaken:false,rewardOptions:null,rewardTaken:false,rewardTier:null,shopStock:null,secretStock:null,supplyStock:null,damageStart:null,portalReady:false,bossStarted:false,bossActive:false}));
    enterRoom(state.rooms.find(r=>r.type==='start'),null,true);showToast(`SECTOR ${roman(number)} // 战力升格 ×${FLOOR_POWER[number-1]}`);
  }
  function roomAt(x,y){return state.rooms.find(r=>r.x===x&&r.y===y)}
  function enterRoom(room,fromDir=null,instant=false,force=false){
    if(!room)return false;
    const previousRoom=state.room;if(previousRoom?.type==='boss'&&!roomExitUnlocked(previousRoom)){if(!force){showToast(previousRoom===room?'执政官协议已在运行':'Boss 战斗中 // 出口已封锁');return false}cancelBossIntro();if(state.mode==='bossintro')state.mode='playing'}else if(previousRoom?.type==='boss'&&previousRoom!==room)cancelBossIntro();
    const keyCost=!room.visited?(SPECIAL_ROOM_COSTS[room.type]||0):0;if(keyCost){if(player.keys<keyCost){showToast(`需要神经密钥 ×${keyCost}`);return false}player.keys-=keyCost;beep(220,.08,'square',.03)}
    state.room=room;room.visited=true;room.revealed=true;if(room.type==='combat'&&!room.cleared&&!Number.isFinite(room.damageStart))room.damageStart=player.damageTaken;state.transition=.38;state.enemies=[];state.tears=[];state.enemyBullets=[];state.hazards=[];state.bombEntities=[];state.pickups=[];state.pedestals=[];state.portal=null;state.obstacles=[];
    const previousX=player.x,previousY=player.y;if(fromDir==='left'){player.x=ROOM.x+58;player.y=clamp(previousY,ROOM.cy-44,ROOM.cy+44)}else if(fromDir==='right'){player.x=ROOM.x+ROOM.w-58;player.y=clamp(previousY,ROOM.cy-44,ROOM.cy+44)}else if(fromDir==='up'){player.x=clamp(previousX,ROOM.cx-44,ROOM.cx+44);player.y=ROOM.y+58}else if(fromDir==='down'){player.x=clamp(previousX,ROOM.cx-44,ROOM.cx+44);player.y=ROOM.y+ROOM.h-58}else{player.x=ROOM.cx;player.y=ROOM.cy}player.prevX=player.x;player.prevY=player.y;
    player.invuln=.65;buildObstacles(room);
    if(room.type==='combat'&&!room.cleared)spawnCombat(room);else if(room.type==='combat'){if(room.supplyStock&&!room.supplyStock.taken)spawnCombatSupply(room);if(room.rewardOptions&&!room.rewardTaken)showRoomChoice(room,room.rewardOptions,'发育终端')}
    if(room.type==='treasure'&&!room.rewardTaken)spawnTreasure(room);
    if(room.type==='shop'&&!room.itemTaken)spawnShop(room);
    if(room.type==='secret'&&!room.itemTaken)spawnSecret(room);
    if((room.type==='vault'||room.type==='sanctum')&&!room.rewardTaken)spawnSpecial(room);
    if(room.type==='boss'){if(!room.cleared)startBoss(room);else{room.portalReady=true;room.bossActive=false;state.portal={x:ROOM.cx+360,y:ROOM.cy,r:36,ready:true};if(room.rewardOptions&&!room.rewardTaken)showRoomChoice(room,room.rewardOptions,'执政官遗物')}}updateUI();
    if(!instant)beep(155,.07,'triangle',.025);return true;
  }
  function buildObstacles(room){
    if(room.type==='start'||room.type==='boss'||room.type==='treasure'||room.type==='vault'||room.type==='sanctum')return;
    const groups=[];if(room.type==='shop'){groups.push([300,245,5,44,0,26],[804,485,5,44,0,26],[330,445,3,0,-44,26],[950,275,3,0,44,26])}else if(room.type==='secret'){groups.push([300,250,5,42,18,28],[812,390,5,42,18,28],[475,485,4,44,0,27])}else{const layouts=[[[350,250,5,44,0,28],[754,480,5,44,0,28],[350,450,1,0,0,36],[930,280,1,0,0,36]],[[420,225,5,0,44,28],[860,319,5,0,44,28],[574,215,4,44,0,27]],[[330,300,5,44,0,28],[774,430,5,44,0,28],[640,235,1,0,0,36],[640,495,1,0,0,36]],[[370,220,4,0,44,28],[910,368,4,0,44,28],[500,495,5,44,0,27],[596,225,3,44,0,27]],[[305,250,4,42,18,28],[848,390,4,42,18,28],[500,480,3,44,0,28],[692,245,3,44,0,28]]];groups.push(...layouts[(room.index+state.floor)%layouts.length])}
    const breakable=room.type!=='shop';state.obstacles=groups.flatMap((spec,g)=>wallRun(`${room.key}-${g}`,...spec,breakable));
  }
  function wallRun(group,x,y,count,dx,dy,r,breakable){const wall= count>1,angle=wall?Math.atan2(dy,dx):Math.PI/4,step=wall?Math.hypot(dx,dy):r*1.2;return Array.from({length:count},(_,i)=>({x:x+dx*i,y:y+dy*i,r,wallGroup:wall?group:null,wallIndex:i,wallAngle:angle,wallStep:step,breakable,hp:2}))}
  function hitsObstacle(x,y,r=0){return state.obstacles.some(o=>Math.hypot(x-o.x,y-o.y)<r+o.r)}
  function neighbors(room=state.room){const dirs=[['left',-1,0],['right',1,0],['up',0,-1],['down',0,1]];return dirs.map(([dir,dx,dy])=>({dir,room:roomAt(room.x+dx,room.y+dy)})).filter(n=>n.room)}
  function opposite(dir){return{left:'right',right:'left',up:'down',down:'up'}[dir]}
  function spawnCombat(room){
    const combatRooms=state.rooms.filter(r=>r.type==='combat'),rank=Math.max(0,combatRooms.findIndex(r=>r.key===room.key)),floorCurve=COMBAT_CURVE[state.floor-1]||COMBAT_CURVE.at(-1),roster=floorCurve[Math.min(rank,floorCurve.length-1)],threat=(state.floor-1)*3+rank;
    for(const type of roster)spawnEnemy(type,safeSpawn(),false,threat)
  }
  function safeSpawn(){let p={x:ROOM.cx,y:ROOM.cy};for(let attempt=0;attempt<80;attempt++){const candidate={x:rand(ROOM.x+100,ROOM.x+ROOM.w-100),y:rand(ROOM.y+95,ROOM.y+ROOM.h-95)};p=candidate;if(Math.hypot(p.x-player.x,p.y-player.y)>=220&&!hitsObstacle(p.x,p.y,34))return p}return p}
  function spawnEnemy(type,pos,elite=false,threat=null){
    const defs={crawler:{hp:10,r:18,speed:82,color:COL.pink},shooter:{hp:12,r:20,speed:54,color:COL.violet},charger:{hp:19,r:24,speed:68,color:COL.red},turret:{hp:16,r:23,speed:0,color:COL.yellow},drone:{hp:9,r:15,speed:110,color:COL.cyan}},d=defs[type];
    const pressure=Number.isFinite(threat)?threat:(state.floor-1)*3+2,roomStep=pressure%3,scale=1+(state.floor-1)*.22+roomStep*.06+(elite ? .4 : 0),speedScale=1+(state.floor-1)*.04+roomStep*.015,enemy={uid:uid++,type,x:pos.x,y:pos.y,r:d.r*(elite?1.2:1),hp:d.hp*scale,maxHp:d.hp*scale,speed:d.speed*speedScale,color:d.color,elite,threat:pressure,fire:1.55+rand(0,.55)-Math.min(.28,pressure*.035),move:rand(1,2),angle:rand(0,TAU),warning:0,dash:0,attackPulse:0,dead:false};state.enemies.push(enemy);return enemy;
  }
  function enemyShotProfile(type,threat=0,elite=false){if(type==='shooter')return{count:elite?3:threat>=6?2:1,speed:160+threat*10,cooldown:elite?clamp(1.05-threat*.03,.78,1.05):clamp(1.75-threat*.075,1.15,1.75)};return{count:elite?10:threat>=7?8:6,speed:140+threat*9,cooldown:elite?clamp(1.12-threat*.025,.85,1.12):clamp(1.85-threat*.065,1.3,1.85)}}
  function makePedestal(x,y,item,cost=0,meta={}){return{x,y,item,cost,taken:false,phase:rand(0,TAU),requiresExit:Math.hypot(player.x-x,player.y-y)<player.r+30,...meta}}
  function rollItem(){return rollRelicChoices(1)[0]||rollTuning()}
  function choicePositions(count){if(count===2)return[ROOM.cx-145,ROOM.cx+145];if(count===3)return[ROOM.cx-225,ROOM.cx,ROOM.cx+225];return Array.from({length:count},(_,i)=>ROOM.cx+(i-(count-1)/2)*150)}
  function showRoomChoice(room,options,label){if(room.rewardTaken)return;room.rewardOptions=options;const positions=choicePositions(options.length);state.pedestals=options.map((item,i)=>makePedestal(positions[i],ROOM.cy+24,item,0,{group:room.key,roomChoice:true,choiceIndex:i}));showToast(`${label} // 只能选择一项`)}
  function spawnTreasure(room){if(!room.rewardOptions)room.rewardOptions=rollRelicChoices(2);showRoomChoice(room,room.rewardOptions,'遗物库二选一')}
  function spawnSpecial(room){if(!room.rewardOptions)room.rewardOptions=room.type==='sanctum'?growthOptions('rare',3):growthOptions('vault',3);showRoomChoice(room,room.rewardOptions,room.type==='sanctum'?'升格核心':'量子武库')}
  function spawnShop(room){const slots=[ROOM.cx-220,ROOM.cx,ROOM.cx+220];if(!room.shopStock){const items=rollRelicChoices(3);room.shopStock=items.map((item,i)=>({item,cost:7+state.floor*2+i*2,taken:false,slot:i}))}state.pedestals=room.shopStock.filter(stock=>!stock.taken).map(stock=>makePedestal(slots[stock.slot],ROOM.cy,stock.item,stock.cost,{stock}));room.itemTaken=room.shopStock.every(stock=>stock.taken)}
  function spawnSecret(room){if(!room.secretStock){room.secretStock=[{kind:'coin',value:8,x:ROOM.cx-55,y:ROOM.cy},{kind:'shield',value:1,x:ROOM.cx+55,y:ROOM.cy},...(state.floor<=3?[{kind:'key',value:1,x:ROOM.cx,y:ROOM.cy-58}]:[{kind:'bomb',value:1,x:ROOM.cx,y:ROOM.cy-58},{kind:'heart',value:1,x:ROOM.cx,y:ROOM.cy+58}])].map(stock=>({...stock,taken:false}))}for(const stock of room.secretStock)if(!stock.taken)state.pickups.push(makePickup(stock.kind,stock.x,stock.y,stock.value,{stock,stockRoom:room}));room.itemTaken=room.secretStock.every(stock=>stock.taken)}
  function makePickup(kind,x,y,value=1,meta={}){return{kind,x,y,r:10,value,vx:rand(-15,15),vy:rand(-15,15),life:99,spawnFx:1.05,phase:rand(0,TAU),magnetized:false,magnetTime:0,...meta}}

  function cancelBossIntro(){
    clearTimeout(state.bossIntroTimer);state.bossIntroTimer=0;state.bossIntroToken++;ui.bossIntro.classList.remove('visible');
  }
  function renderBossIntroPortrait(data){
    const entry=ensureBossAtlas(data.id),canvas=ui.introPortrait;if(!entry||!canvas)return;const portrait=canvas.getContext('2d'),draw=()=>{portrait.clearRect(0,0,canvas.width,canvas.height);if(!atlasReady(entry.image))return;const sw=entry.image.naturalWidth/entry.columns,sh=entry.image.naturalHeight/entry.rows,scale=Math.min((canvas.width-28)/sw,(canvas.height-18)/sh),dw=sw*scale,dh=sh*scale;portrait.save();portrait.imageSmoothingEnabled=true;portrait.imageSmoothingQuality='high';portrait.shadowBlur=28;portrait.shadowColor=data.color;portrait.drawImage(entry.image,0,0,sw,sh,(canvas.width-dw)/2,(canvas.height-dh)/2,dw,dh);portrait.restore()};draw();if(!atlasReady(entry.image))entry.image.addEventListener('load',draw,{once:true});ui.bossIntro.style.setProperty('--boss-color',data.color);ui.bossIntro.dataset.boss=data.id;canvas.dataset.boss=data.id;canvas.classList.remove('portrait-enter');void canvas.offsetWidth;canvas.classList.add('portrait-enter')
  }
  function startBoss(room){
    const data=BOSS_DATA[state.floor-1];cancelBossIntro();room.portalReady=false;room.bossStarted=true;room.bossActive=true;const token=state.bossIntroToken;state.mode='bossintro';renderBossIntroPortrait(data);ui.bossIntro.classList.add('visible');$('#intro-name').textContent=data.name;$('#intro-subtitle').textContent=data.subtitle;state.bossIntroTimer=setTimeout(()=>{if(token!==state.bossIntroToken)return;state.bossIntroTimer=0;if(state.room!==room||room.cleared||room.portalReady){ui.bossIntro.classList.remove('visible');if(state.mode==='bossintro')state.mode='playing';return}ui.bossIntro.classList.remove('visible');state.mode='playing';spawnBoss(data,room)},1450);
  }
  function spawnBoss(data,room=state.room){
    if(!room||state.room!==room||room.type!=='boss'||room.cleared||room.portalReady)return false;room.bossStarted=true;room.bossActive=true;state.enemies=[{uid:uid++,type:'boss',boss:true,bossId:data.id,bossTier:data.tier||1,name:data.name,roomKey:room.key,x:ROOM.cx,y:ROOM.y+150,r:54,hp:data.hp,maxHp:data.hp,color:data.color,phase:0,fire:BOSS_CURVE[data.id][0].windup||1.2,move:1.4,angle:0,bladeAngle:0,warning:0,dash:0,attackPulse:0,transformPulse:.72,invuln:.8,dead:false}];ui.bossHud.classList.remove('hidden');updateUI();beep(68,.4,'sawtooth',.07);return true;
  }
  function ensureBossEncounter(){
    const room=state.room;if(room?.type!=='boss')return;if(room.cleared){room.portalReady=true;room.bossActive=false;state.enemies=[];state.enemyBullets=[];state.hazards=[];if(!state.portal)state.portal={x:ROOM.cx+360,y:ROOM.cy,r:36,ready:true};return}if(room.portalReady){room.portalReady=false;state.portal=null}if(state.mode!=='playing')return;const boss=state.enemies.find(e=>e.boss&&!e.dead&&e.roomKey===room.key);if(boss){room.bossStarted=true;room.bossActive=true;return}state.enemyBullets=[];state.hazards=[];if(spawnBoss(BOSS_DATA[state.floor-1],room))showToast('执政官信号重连 // 战斗恢复');
  }
  function bossPhaseFor(b){const r=b.hp/b.maxHp;return r>.66?0:r>.33?1:2}
  function updateBoss(b,dt){
    if(b.invuln>0)b.invuln-=dt;b.attackPulse=Math.max(0,(b.attackPulse||0)-dt);b.transformPulse=Math.max(0,(b.transformPulse||0)-dt);const next=bossPhaseFor(b);if(next!==b.phase){b.phase=next;b.invuln=.9;b.fire=BOSS_CURVE[b.bossId][next].windup;b.move=Math.max(b.move,.8);b.attackPulse=.68;b.transformPulse=1.05;state.enemyBullets=[];state.hazards=[];state.shake=14;burst(b.x,b.y,b.color,55,320);showToast(`${b.name} // FORM 0${next+1}`);beep(95-next*15,.35,'sawtooth',.07)}
    b.angle+=dt*(.5+b.phase*.35);b.bladeAngle=(b.bladeAngle||0)+dt*(1.9+b.phase*1.45+(b.attackPulse>0?4.2:0));b.fire-=dt;b.move-=dt;
    if(b.warning>0){b.warning-=dt;if(b.warning<=0){const speed=470+b.phase*80;b.dash=0.36;b.vx=Math.cos(b.dashAngle)*speed;b.vy=Math.sin(b.dashAngle)*speed}return}
    if(b.dash>0){b.dash-=dt;b.x+=b.vx*dt;b.y+=b.vy*dt;b.vx*=.985;b.vy*=.985;clampEntity(b);return}
    const dx=player.x-b.x,dy=player.y-b.y,d=Math.max(1,Math.hypot(dx,dy)),tune=BOSS_CURVE[b.bossId][b.phase];
    if(b.bossId==='cherub'){
      if(b.phase===0){b.x+=Math.cos(b.angle)*55*dt;b.y+=Math.sin(b.angle*1.4)*40*dt;if(b.fire<=0){fanBullets(b,Math.atan2(dy,dx),tune.fan,tune.spread,tune.speed);b.fire=tune.cooldown}}
      else if(b.phase===1){b.x+=dx/d*45*dt;b.y+=dy/d*45*dt;if(b.fire<=0){radialBullets(b,tune.radial,tune.speed,b.angle);b.fire=tune.cooldown}if(b.move<=0)beginDash(b,player,tune.dashWarning,tune.dashReset)}
      else{b.x+=Math.cos(b.angle*1.7)*80*dt;if(b.fire<=0){radialBullets(b,tune.radial,tune.radialSpeed,b.angle);fanBullets(b,Math.atan2(dy,dx),tune.fan,tune.spread,tune.fanSpeed);b.fire=tune.cooldown}if(b.move<=0){if(state.enemies.filter(e=>!e.boss&&!e.dead).length<2)spawnEnemy('drone',safeSpawn(),false,2);b.move=tune.summonCooldown}}
    }else if(b.bossId==='matriarch'){
      if(b.phase===0){b.x+=Math.cos(b.angle)*80*dt;if(b.fire<=0){b.attackPulse=.42;targetHazard(player.x+rand(-45,45),player.y+rand(-45,45),tune.hazardRadius,tune.hazardLife,tune.damage);b.fire=tune.cooldown}if(b.move<=0){teleportBoss(b);b.move=tune.teleportCooldown}}
      else if(b.phase===1){if(b.fire<=0){laserCross(b,b.angle);radialBullets(b,tune.radial,tune.speed,b.angle);b.fire=tune.cooldown}b.y+=Math.sin(b.angle*1.8)*70*dt}
      else{b.x+=dx/d*62*dt;b.y+=dy/d*62*dt;if(b.fire<=0){fanBullets(b,Math.atan2(dy,dx),tune.fan,tune.spread,tune.speed);targetHazard(player.x,player.y,tune.hazardRadius,tune.hazardLife,tune.damage);b.fire=tune.cooldown}if(b.move<=0)beginDash(b,player,tune.dashWarning,tune.dashReset)}
    }else{
      if(b.phase===0){b.x=ROOM.cx+Math.cos(b.angle)*210;b.y=ROOM.cy+Math.sin(b.angle)*120;if(b.fire<=0){radialBullets(b,tune.radial,tune.speed,-b.angle);targetHazard(player.x,player.y,tune.hazardRadius,tune.hazardLife,tune.damage);b.fire=tune.cooldown}}
      else if(b.phase===1){b.x+=dx/d*34*dt;b.y+=dy/d*34*dt;if(b.fire<=0){laserCross(b,b.angle);radialBullets(b,tune.radial,tune.speed,b.angle);b.fire=tune.cooldown}if(b.move<=0){if(state.enemies.filter(e=>!e.boss&&!e.dead).length<3)spawnEnemy(choice(['charger','drone','shooter']),safeSpawn(),true,8);b.move=tune.summonCooldown}}
      else{b.x+=Math.cos(b.angle*2.2)*105*dt;b.y+=Math.sin(b.angle*1.5)*85*dt;if(b.fire<=0){radialBullets(b,tune.radial,tune.radialSpeed,b.angle);fanBullets(b,Math.atan2(dy,dx),tune.fan,tune.spread,tune.fanSpeed);targetHazard(player.x,player.y,tune.hazardRadius,tune.hazardLife,tune.damage);b.fire=tune.cooldown}if(b.move<=0)beginDash(b,player,tune.dashWarning,tune.dashReset)}
    }clampEntity(b);
  }
  function beginDash(b,target,warning,reset){b.warning=warning;b.attackPulse=Math.max(b.attackPulse||0,warning);b.dashAngle=Math.atan2(target.y-b.y,target.x-b.x);b.move=reset;state.hazards.push({kind:'line',x:b.x,y:b.y,angle:b.dashAngle,length:720,width:b.r*1.25,life:warning,max:warning,color:b.color,safe:true})}
  function teleportBoss(b){b.transformPulse=.62;burst(b.x,b.y,b.color,20,160);b.x=rand(ROOM.x+150,ROOM.x+ROOM.w-150);b.y=rand(ROOM.y+130,ROOM.y+ROOM.h-130);burst(b.x,b.y,b.color,20,160)}
  function radialBullets(e,count,speed,offset=0){if(state.enemyBullets.length+count>enemyBulletCap())return false;for(let i=0;i<count;i++)enemyBullet(e,offset+i/count*TAU,speed);return true}
  function fanBullets(e,angle,count,spread,speed){if(state.enemyBullets.length+count>enemyBulletCap())return false;for(let i=0;i<count;i++)enemyBullet(e,angle+(i-(count-1)/2)*spread,speed);return true}
  function enemyBulletCap(){const boss=state.enemies.find(e=>e.boss&&!e.dead);if(boss)return BOSS_CURVE[boss.bossId][boss.phase].bulletCap+(boss.bossTier>1?12:0);const threat=Math.max(0,...state.enemies.filter(e=>!e.dead).map(e=>e.threat||0));return ROOM_BULLET_CAPS[Math.min(threat,ROOM_BULLET_CAPS.length-1)]}
  function enemyBullet(e,a,speed){if(state.enemyBullets.length>=enemyBulletCap())return false;e.attackPulse=Math.max(e.attackPulse||0,e.boss ? .24 : .18);const velocity=speed*(e.boss&&e.bossTier>1?1.12:1);state.enemyBullets.push({x:e.x,y:e.y,vx:Math.cos(a)*velocity,vy:Math.sin(a)*velocity,r:e.boss?7:6,life:6,color:e.color,damage:1,source:e.boss?'boss':'enemy',spin:rand(0,TAU),phase:e.phase||0});return true}
  function laserCross(b,angle){b.attackPulse=Math.max(b.attackPulse||0,.55);for(let i=0;i<3;i++)state.hazards.push({kind:'laser',x:b.x,y:b.y,angle:angle+i*Math.PI/3,length:1300,width:24,life:.78,max:.78,trigger:.12,color:b.color,damage:1})}
  function targetHazard(x,y,r,life,damage){state.hazards.push({kind:'circle',x,y,r,life,max:life,damage,color:COL.pink})}

  function update(dt){
    state.time+=dt;if(state.mode!=='playing'){updateParticles(dt);return}ensureBossEncounter();state.transition=Math.max(0,state.transition-dt);state.flash=Math.max(0,state.flash-dt);state.shake=Math.max(0,state.shake-dt*32);player.invuln=Math.max(0,player.invuln-dt);player.formCd=Math.max(0,player.formCd-dt);player.recoil=Math.max(0,(player.recoil||0)-dt*42);player.fireCd-=dt;
    player.prevX=player.x;player.prevY=player.y;updatePlayer(dt);updateTears(dt);updateEnemies(dt);updateEnemyBullets(dt);updateHazards(dt);updateBombs(dt);updatePickups(dt);updatePedestals();updateParticles(dt);updatePortal();checkRoomClear(dt);updateUI();
  }
  function updatePlayer(dt){
    let mx=(input.keys.has('KeyD')?1:0)-(input.keys.has('KeyA')?1:0)+input.moveX,my=(input.keys.has('KeyS')?1:0)-(input.keys.has('KeyW')?1:0)+input.moveY,mag=Math.hypot(mx,my);if(mag>1){mx/=mag;my/=mag}
    const speed=FORMS[player.form].move*(1+upgradeLevel('tune-servo')*.07);player.x+=mx*speed*dt;player.y+=my*speed*dt;resolveWalls(player);resolveObstacles(player);
    if(input.mouseInside){const fx=input.mouseX-player.x,fy=input.mouseY-player.y,fm=Math.hypot(fx,fy);if(fm>4){player.aimX=fx/fm;player.aimY=fy/fm;if(Math.abs(player.aimX)>.12)player.faceX=player.aimX<0?-1:1}}
    let sx=input.shootX,sy=input.shootY;if(input.pointerAim){sx=input.mouseX-player.x;sy=input.mouseY-player.y}else if(Math.hypot(sx,sy)<.2&&!input.keys.has('ShiftLeft')&&!input.keys.has('ShiftRight')){sx=(input.keys.has('ArrowRight')?1:0)-(input.keys.has('ArrowLeft')?1:0);sy=(input.keys.has('ArrowDown')?1:0)-(input.keys.has('ArrowUp')?1:0)}
    const sm=Math.hypot(sx,sy);if(sm>.2){sx/=sm;sy/=sm;player.aimX=sx;player.aimY=sy;if(Math.abs(sx)>.12)player.faceX=sx<0?-1:1;if(player.fireCd<=0)firePlayer(sx,sy)}
    updateDrone(dt);
  }
  function getStats(){const f=FORMS[player.form],power=FLOOR_POWER[state.floor-1]||1,lowVoltage=has('votive')&&player.hp/player.maxHp<=.5,mult=(has('blood')?1.5:1)*(lowVoltage?1.3:1),velocity=1+upgradeLevel('tune-velocity')*.12,formCrit=player.form==='wraith'?.07:player.form==='beast'?.04:0,critChance=.08+formCrit+(has('redchip')?.05:0)+(lowVoltage?.1:0)+upgradeLevel('tune-target')*.05+upgradeLevel('ascend-focus')*.02;return{damage:(f.damage+upgradeLevel('tune-caliber')*.75+upgradeLevel('ascend-fury')*.32+(has('redchip')?1.8:0))*mult*power,delay:Math.max(.065,f.delay*Math.pow(.9,upgradeLevel('tune-trigger'))*Math.pow(.95,upgradeLevel('ascend-cycle'))*(has('overclock')?.72:1)),speed:f.speed*velocity,range:f.range*velocity*(has('ghost')?1.25:1),shots:Math.min(7,f.shots+(has('prism')?2:0)),spread:f.spread+(has('prism')?.1:0),pierce:Math.min(5,f.pierce+(has('phase')?2:0)),ricochets:Math.min(2,f.ricochets+(has('ghost')?1:0)),size:f.size,critChance:player.critOverride??Math.min(.7,critChance),critMult:2,power}}
  function firePlayer(x,y){
    const s=getStats(),base=Math.atan2(y,x),count=s.shots;player.fireCd=s.delay;player.shotSequence=(player.shotSequence||0)+1;for(let i=0;i<count;i++){const a=base+(i-(count-1)/2)*s.spread;spawnTear(player.x+Math.cos(a)*20,player.y+Math.sin(a)*20,a,s)}if(has('echo'))spawnTear(player.x,player.y,base+Math.PI,{...s,damage:s.damage*.65,shots:1});if(has('afterimage')&&player.shotSequence%5===0){for(const side of [-1,1])spawnTear(player.x,player.y,base+side*Math.PI/2,{...s,damage:s.damage*.62,size:Math.max(4,s.size*.78),pierce:Math.max(1,s.pierce),ricochets:2,critChance:s.critChance*.6});burst(player.x,player.y,COL.violet,12,150);beep(890,.055,'triangle',.018)}weaponFlash(base);playShotSound(player.form)
  }
  function spawnTear(x,y,a,s,owner='player'){const form=owner==='drone'?'drone':player.form,color=form==='drone'?COL.cyan:FORMS[form].color,ricochets=s.ricochets??Math.min(2,1+(has('ghost')?1:0)),critical=chance(s.critChance??(owner==='drone'?.05:.08));state.tears.push({x,y,px:x-Math.cos(a)*8,py:y-Math.sin(a)*8,vx:Math.cos(a)*s.speed,vy:Math.sin(a)*s.speed,r:s.size,damage:s.damage,life:s.range/s.speed,pierce:s.pierce+(critical&&has('oracle-eye')?1:0),color,form,age:0,seed:rand(0,TAU),ricochetsLeft:ricochets,ricochetCount:0,volatile:has('volatile'),tesla:has('tesla'),crit:critical,critMult:s.critMult||2,owner,hit:new Set()})}
  function fxPressure(){return state.tears.length+state.enemyBullets.length*1.2+state.particles.length*.12}
  function pushWeaponFx(fx){let count=0;for(const p of state.particles)if(p.kind==='muzzle'||p.kind==='impact'||p.kind==='ricochet')count++;const cap=fxPressure()>90?32:56;if(count<cap)state.particles.push(fx)}
  function weaponFlash(angle){const form=player.form,color=FORMS[form].color,x=player.x+Math.cos(angle)*25,y=player.y+Math.sin(angle)*25,life=form==='seraph'?.2:form==='beast'?.16:.13;pushWeaponFx({kind:'muzzle',x,y,angle,color,form,life,max:life});const counts={vanguard:4,wraith:3,beast:8,seraph:6};burst(x,y,color,counts[form],form==='beast'?190:125);player.recoil=Math.max(player.recoil,{vanguard:2,wraith:1,beast:6,seraph:4}[form]);if(form==='beast')state.shake=Math.max(state.shake,2.8);else if(form==='seraph')state.shake=Math.max(state.shake,1.6)}
  function weaponImpact(x,y,tear,critical){if(!tear)return;const form=tear.form||'vanguard',life=form==='seraph'?.2:form==='beast'?.18:.14;pushWeaponFx({kind:'impact',x,y,color:tear.color,form,crit:critical,angle:Math.atan2(tear.vy,tear.vx),life,max:life})}
  function ricochetFx(t,nx,ny,blocked=false){const life=.13;pushWeaponFx({kind:'ricochet',x:t.x,y:t.y,nx,ny,color:blocked?COL.red:t.color,blocked,life,max:life});burst(t.x,t.y,blocked?COL.red:t.color,blocked?4:6,blocked?75:115);if(!blocked&&state.time-lastRicochetSound>.045){lastRicochetSound=state.time;beep(620+t.ricochetCount*90,.035,'triangle',.012)}}
  function openTearDoor(side,x,y){const adjacent=neighbors().find(n=>n.dir===side);if(!roomExitUnlocked()||!adjacent||adjacent.room.type==='secret'&&!adjacent.room.revealed)return false;return side==='left'||side==='right'?Math.abs(y-ROOM.cy)<58:Math.abs(x-ROOM.cx)<58}
  function firstTearCollision(t,dx,dy,ignoreSurface=null){
    const EPS=1e-7;let best=null;const consider=hit=>{if(hit.time<-EPS||hit.time>1+EPS)return;hit.time=clamp(hit.time,0,1);if(!best||hit.time<best.time-1e-5){best=hit;return}if(Math.abs(hit.time-best.time)<1e-5&&hit.boundary&&best.boundary&&!hit.exit&&!best.exit){const nx=best.nx+hit.nx,ny=best.ny+hit.ny,l=Math.hypot(nx,ny)||1;best={time:best.time,nx:nx/l,ny:ny/l,surface:`${best.surface}+${hit.surface}`,boundary:true,exit:false}}};
    const minX=ROOM.x+t.r,maxX=ROOM.x+ROOM.w-t.r,minY=ROOM.y+t.r,maxY=ROOM.y+ROOM.h-t.r;
    if(dx<0&&t.x+dx<=minX){const u=(minX-t.x)/dx,y=t.y+dy*u;consider({time:u,nx:1,ny:0,surface:'room-left',boundary:true,exit:openTearDoor('left',minX,y)})}
    if(dx>0&&t.x+dx>=maxX){const u=(maxX-t.x)/dx,y=t.y+dy*u;consider({time:u,nx:-1,ny:0,surface:'room-right',boundary:true,exit:openTearDoor('right',maxX,y)})}
    if(dy<0&&t.y+dy<=minY){const u=(minY-t.y)/dy,x=t.x+dx*u;consider({time:u,nx:0,ny:1,surface:'room-up',boundary:true,exit:openTearDoor('up',x,minY)})}
    if(dy>0&&t.y+dy>=maxY){const u=(maxY-t.y)/dy,x=t.x+dx*u;consider({time:u,nx:0,ny:-1,surface:'room-down',boundary:true,exit:openTearDoor('down',x,maxY)})}
    const a=dx*dx+dy*dy;if(a<EPS)return best;const moveLength=Math.sqrt(a);
    for(const o of state.obstacles){const surface=o.wallGroup?`wall:${o.wallGroup}`:o,R=o.r+t.r,fx=t.x-o.x,fy=t.y-o.y,c=fx*fx+fy*fy-R*R;let u,rx,ry;if(c<=0){const d=Math.hypot(fx,fy)||1;rx=fx/d;ry=fy/d;if(dx*rx+dy*ry>=0)continue;u=0}else{const b=2*(fx*dx+fy*dy),disc=b*b-4*a*c;if(disc<0)continue;u=(-b-Math.sqrt(disc))/(2*a);if(u<-EPS||u>1+EPS)continue;const hx=t.x+dx*u,hy=t.y+dy*u,d=Math.hypot(hx-o.x,hy-o.y)||1;rx=(hx-o.x)/d;ry=(hy-o.y)/d}if(ignoreSurface===surface&&u<.03)continue;let nx=rx,ny=ry;if(o.wallGroup){const wx=-Math.sin(o.wallAngle),wy=Math.cos(o.wallAngle);if(Math.abs(dx*wx+dy*wy)>moveLength*.08){nx=wx;ny=wy}}if(dx*nx+dy*ny>0){nx=-nx;ny=-ny}if(dx*nx+dy*ny>=-EPS)continue;consider({time:u,nx,ny,surface,boundary:false,exit:false})}
    return best
  }
  function moveTear(t,dt){let remaining=dt,ignoreSurface=null;for(let pass=0;pass<4&&remaining>1e-6;pass++){const dx=t.vx*remaining,dy=t.vy*remaining,hit=firstTearCollision(t,dx,dy,ignoreSurface);if(!hit){t.x+=dx;t.y+=dy;return false}t.x+=dx*hit.time;t.y+=dy*hit.time;if(hit.exit)return true;if(t.ricochetsLeft<=0){ricochetFx(t,hit.nx,hit.ny,true);return true}const dot=t.vx*hit.nx+t.vy*hit.ny;if(dot>=-1e-6){t.x+=hit.nx*.25;t.y+=hit.ny*.25;ignoreSurface=hit.surface;remaining*=1-hit.time;continue}t.vx-=2*dot*hit.nx;t.vy-=2*dot*hit.ny;t.ricochetsLeft--;t.ricochetCount++;t.x+=hit.nx*.35;t.y+=hit.ny*.35;ricochetFx(t,hit.nx,hit.ny);ignoreSurface=hit.surface;remaining*=1-hit.time}return remaining>1e-6}
  function updateTears(dt){
    for(let i=state.tears.length-1;i>=0;i--){const t=state.tears[i];t.px=t.x;t.py=t.y;t.age+=dt;t.life-=dt;let remove=t.life<=0;if(!remove)remove=moveTear(t,dt);if(remove){state.tears.splice(i,1);continue}
      for(const e of [...state.enemies])if(!e.dead&&!t.hit.has(e.uid)&&Math.hypot(t.x-e.x,t.y-e.y)<t.r+e.r){t.hit.add(e.uid);damageEnemy(e,t.damage,t);if(t.tesla)chainLightning(e,t.damage*.55);if(t.volatile)explode(t.x,t.y,48,t.damage*.45,false);t.pierce--;if(t.pierce<0){remove=true;break}}
      if(remove)state.tears.splice(i,1)
    }
  }
  function chainLightning(from,damage){const target=state.enemies.filter(e=>!e.dead&&e!==from).sort((a,b)=>dist(a,from)-dist(b,from))[0];if(target&&dist(target,from)<180){damageEnemy(target,damage,null);state.particles.push({kind:'beam',x:from.x,y:from.y,x2:target.x,y2:target.y,color:COL.cyan,life:.13,max:.13})}}
  function familiarCount(){return has('choir')?(has('drone')?2:1):has('drone')?1:0}
  function updateDrone(dt){const count=familiarCount();if(!count)return;player.droneAngle=(player.droneAngle||0)+dt*2.1;player.droneFire=(player.droneFire||0)-dt;if(player.droneFire<=0){const target=nearestEnemy({x:player.x,y:player.y});if(target){for(let i=0;i<count;i++){const orbit=player.droneAngle+i/count*TAU,x=player.x+Math.cos(orbit)*46,y=player.y+Math.sin(orbit)*46,a=Math.atan2(target.y-y,target.x-x);spawnTear(x,y,a,{speed:480,range:560,size:5,damage:2.2,pierce:0,ricochets:1,critChance:.06},'drone')}player.droneFire=count>1?.62:.7}}}
  function nearestEnemy(from){return state.enemies.filter(e=>!e.dead).sort((a,b)=>dist(a,from)-dist(b,from))[0]}

  function updateEnemies(dt){
    for(const e of [...state.enemies]){if(e.dead)continue;if(e.boss){updateBoss(e,dt);continue}e.fire-=dt;e.move-=dt;e.warning=Math.max(0,e.warning-dt);e.attackPulse=Math.max(0,(e.attackPulse||0)-dt);const dx=player.x-e.x,dy=player.y-e.y,d=Math.max(1,Math.hypot(dx,dy)),nx=dx/d,ny=dy/d;
      if(e.type==='crawler'){e.x+=nx*e.speed*dt;e.y+=ny*e.speed*dt}
      else if(e.type==='drone'){e.angle+=dt*2;e.x+=(nx*.65+Math.cos(e.angle)*.5)*e.speed*dt;e.y+=(ny*.65+Math.sin(e.angle)*.5)*e.speed*dt}
      else if(e.type==='shooter'){const move=d>290?1:d<190?-1:0,shot=enemyShotProfile('shooter',e.threat,e.elite);e.x+=nx*e.speed*move*dt;e.y+=ny*e.speed*move*dt;if(e.fire<=0){fanBullets(e,Math.atan2(dy,dx),shot.count,.16,shot.speed);e.fire=shot.cooldown}}
      else if(e.type==='turret'){const shot=enemyShotProfile('turret',e.threat,e.elite);if(e.fire<=0){radialBullets(e,shot.count,shot.speed,e.angle);e.angle+=.35;e.fire=shot.cooldown}}
      else if(e.type==='charger'){if(e.dash>0){e.dash-=dt;e.x+=e.vx*dt;e.y+=e.vy*dt}else if(e.warning>0){if(e.warning-dt<=0){e.dash=.42;e.vx=nx*390;e.vy=ny*390}}else if(e.move<=0){e.warning=.55;e.move=2.6;state.hazards.push({kind:'line',x:e.x,y:e.y,angle:Math.atan2(dy,dx),length:450,width:e.r,life:.55,max:.55,color:e.color,safe:true})}else{e.x+=nx*e.speed*.55*dt;e.y+=ny*e.speed*.55*dt}}
      clampEntity(e);resolveObstacles(e);if(Math.hypot(e.x-player.x,e.y-player.y)<e.r+player.r)hurtPlayer(1)
    }
  }
  function updateEnemyBullets(dt){for(let i=state.enemyBullets.length-1;i>=0;i--){const b=state.enemyBullets[i];b.x+=b.vx*dt;b.y+=b.vy*dt;b.life-=dt;const blocked=hitsObstacle(b.x,b.y,b.r);if(b.life<=0||b.x<ROOM.x||b.x>ROOM.x+ROOM.w||b.y<ROOM.y||b.y>ROOM.y+ROOM.h||blocked){if(blocked)burst(b.x,b.y,b.color,3,55);state.enemyBullets.splice(i,1);continue}if(Math.hypot(b.x-player.x,b.y-player.y)<b.r+player.r){hurtPlayer(b.damage);state.enemyBullets.splice(i,1)}}}
  function updateHazards(dt){for(let i=state.hazards.length-1;i>=0;i--){const h=state.hazards[i];h.life-=dt;if(h.kind==='laser'&&h.life<=h.trigger&&!h.fired){h.fired=true;const dx=player.x-h.x,dy=player.y-h.y,along=dx*Math.cos(h.angle)+dy*Math.sin(h.angle),perp=Math.abs(-dx*Math.sin(h.angle)+dy*Math.cos(h.angle));if(Math.abs(along)<h.length/2&&perp<h.width+player.r)hurtPlayer(h.damage);state.flash=.12;beep(110,.18,'sawtooth',.04)}if(h.life<=0){if(h.kind==='circle'){if(Math.hypot(player.x-h.x,player.y-h.y)<h.r+player.r)hurtPlayer(h.damage);explode(h.x,h.y,h.r,0,false)}state.hazards.splice(i,1)}}}
  function damageEnemy(e,damage,tear){if(e.invuln>0)return;const critical=!!tear?.crit,ricochetBoost=has('saw-memory')&&(tear?.ricochetCount||0)>0?1.3:1,archonBoost=has('executioner')&&(e.boss||e.elite)?1.25:1,dealt=damage*(critical?(tear.critMult||2):1)*ricochetBoost*archonBoost;e.hp-=dealt;if(critical)state.critHits++;floatText(e.x,e.y-22,Math.max(1,Math.round(dealt)),critical?COL.yellow:ricochetBoost>1?COL.violet:tear&&tear.damage>6?'#fff0a8':'#f4fdff',critical);weaponImpact(e.x,e.y,tear,critical);burst(e.x,e.y,critical?COL.yellow:ricochetBoost>1?COL.violet:e.color,critical?10:ricochetBoost>1?7:3,critical?150:ricochetBoost>1?120:75);if(critical){state.shake=Math.max(state.shake,4);if(chance(.45))beep(860,.045,'square',.022)}if(e.hp<=0)killEnemy(e)}
  function killEnemy(e){if(e.dead)return;e.dead=true;const i=state.enemies.indexOf(e);if(i>=0)state.enemies.splice(i,1);state.kills++;if(has('harvest')&&!e.boss){player.killHarvest=(player.killHarvest||0)+1;if(player.killHarvest>=8){player.killHarvest=0;player.activeCharge=Math.min(player.activeMax,player.activeCharge+1);showToast('余烬收割完成 // 熵骰充能 +1');beep(480,.16,'triangle',.035)}}burst(e.x,e.y,e.color,e.boss?60:e.elite?22:12,e.boss?360:170);if(e.boss){state.enemyBullets.length=0;state.hazards.length=0;for(const summon of state.enemies)summon.dead=true;state.enemies.length=0;state.room.cleared=true;state.room.portalReady=true;state.room.bossActive=false;if(!state.room.rewardOptions)state.room.rewardOptions=rollRelicChoices(3);ui.bossHud.classList.add('hidden');showRoomChoice(state.room,state.room.rewardOptions,'执政官遗物三选一');state.portal={x:ROOM.cx+360,y:ROOM.cy,r:36,ready:true};player.activeCharge=Math.min(player.activeMax,player.activeCharge+1);beep(55,.75,'sawtooth',.08);state.shake=18}else if(chance(.28))state.pickups.push(makePickup(choice(['coin','coin','heart','shield']),e.x,e.y))}
  function hurtPlayer(damage){if(player.invuln>0||state.mode!=='playing')return;player.invuln=1;player.damageTaken+=damage;if(player.shield>0)player.shield=Math.max(0,player.shield-damage);else player.hp-=damage;state.shake=Math.min(4,Math.max(state.shake,3.2));state.flash=.12;burst(player.x,player.y,COL.pink,22,240);beep(62,.18,'sawtooth',.07);haptic([45,25,60]);if(player.hp<=0)endGame(false)}

  function plantBomb(){if(state.mode!=='playing'||player.bombs<=0)return;player.bombs--;state.bombEntities.push({x:player.x,y:player.y,r:13,life:1.05,max:1.05});beep(170,.05,'square',.025)}
  function updateBombs(dt){for(let i=state.bombEntities.length-1;i>=0;i--){const b=state.bombEntities[i];b.life-=dt;if(b.life<=0){explode(b.x,b.y,125,35,true);state.bombEntities.splice(i,1)}}}
  function explode(x,y,r,damage,world){
    state.shake=Math.max(state.shake,12);burst(x,y,COL.yellow,36,300);for(const e of [...state.enemies])if(Math.hypot(x-e.x,y-e.y)<r+e.r)damageEnemy(e,damage,null);if(world){for(let i=state.obstacles.length-1;i>=0;i--){const o=state.obstacles[i];if(o.breakable&&Math.hypot(x-o.x,y-o.y)<r+o.r){o.hp-=2;if(o.hp<=0)state.obstacles.splice(i,1)}}for(const n of neighbors()){if(n.room.type==='secret'&&!n.room.revealed){const p=doorPosition(n.dir);if(Math.hypot(x-p.x,y-p.y)<175){n.room.revealed=true;showToast('发现隐藏的幽灵缓存');beep(720,.18,'sine',.045)}}}}beep(80,.22,'sawtooth',.06)
  }
  function useActive(){if(state.mode!=='playing'||player.activeCharge<player.activeMax)return showToast('主动协议尚未充能');const available=state.pedestals.filter(p=>!p.taken);if(!available.length)return showToast('此房间没有可重写目标');const used=[],rewrites=[];for(const p of available){const next=rollRelicChoices(1,used)[0];if(!next){showToast('所有协议已达上限');return}used.push(next.id);rewrites.push([p,next])}player.activeCharge=0;for(const [p,next] of rewrites){p.item=next;if(p.stock)p.stock.item=next;if(Number.isInteger(p.choiceIndex))state.room.rewardOptions[p.choiceIndex]=next}burst(ROOM.cx,ROOM.cy,COL.violet,30,230);showToast('熵骰重写了当前选择');beep(380,.25,'square',.045)}
  function pointSegmentDistance(px,py,x1,y1,x2,y2){const vx=x2-x1,vy=y2-y1,len=vx*vx+vy*vy;if(len<.001)return Math.hypot(px-x1,py-y1);const t=clamp(((px-x1)*vx+(py-y1)*vy)/len,0,1),x=x1+vx*t,y=y1+vy*t;return Math.hypot(px-x,py-y)}
  function updatePickups(dt){for(let i=state.pickups.length-1;i>=0;i--){const p=state.pickups[i],oldX=p.x,oldY=p.y,dx=player.x-p.x,dy=player.y-p.y,d=Math.max(1,Math.hypot(dx,dy)),range=has('magnet')?230:player.pickupRange,approach=pointSegmentDistance(oldX,oldY,player.prevX,player.prevY,player.x,player.y);p.spawnFx=Math.max(0,p.spawnFx-dt);p.phase+=dt*4;if(d<range||approach<range)p.magnetized=true;if(p.magnetized){p.magnetTime=(p.magnetTime||0)+dt;let pvx=dt>1e-4?(player.x-player.prevX)/dt:0,pvy=dt>1e-4?(player.y-player.prevY)/dt:0;const pv=Math.hypot(pvx,pvy),pvLimit=FORMS[player.form].move*1.25;if(pv>pvLimit){pvx*=pvLimit/pv;pvy*=pvLimit/pv}const pull=clamp(720+d*3+p.magnetTime*120,720,1500),steer=1-Math.exp(-22*dt);p.vx+=(pvx+dx/d*pull-p.vx)*steer;p.vy+=(pvy+dy/d*pull-p.vy)*steer}else{p.vx*=Math.pow(.05,dt);p.vy*=Math.pow(.05,dt)}p.x+=p.vx*dt;p.y+=p.vy*dt;const hitRange=player.r+p.r+8,rx0=player.prevX-oldX,ry0=player.prevY-oldY,rx1=player.x-p.x,ry1=player.y-p.y,relativeSweep=pointSegmentDistance(0,0,rx0,ry0,rx1,ry1),current=Math.hypot(rx1,ry1);if(Math.min(relativeSweep,current)<hitRange){collectPickup(p);state.pickups.splice(i,1)}}}
  function collectPickup(p){if(p.kind==='coin')player.coins+=p.value;else if(p.kind==='key')player.keys=Math.min(3,player.keys+p.value);else if(p.kind==='bomb')player.bombs+=p.value;else if(p.kind==='heart')player.hp=Math.min(player.maxHp,player.hp+2);else if(p.kind==='shield')player.shield=Math.min(player.maxShield,player.shield+1);if(p.stock){p.stock.taken=true;if(p.stockRoom?.secretStock)p.stockRoom.itemTaken=p.stockRoom.secretStock.every(stock=>stock.taken)}beep(p.kind==='coin'?760:520,.04,'sine',.018)}
  function claimPedestal(p){if(!p||p.taken)return false;if(!canTakeItem(p.item)){showToast('该协议已装载或达到上限');p.taken=true;if(p.stock){p.stock.taken=true;state.room.itemTaken=state.room.shopStock.every(stock=>stock.taken)}return false}if(p.cost&&player.coins<p.cost){showToast(`需要 ${p.cost} DATA`);player.x+=player.x<p.x?-3:3;return false}if(!takeItem(p.item))return false;if(p.cost)player.coins-=p.cost;p.taken=true;if(p.stock)p.stock.taken=true;if(p.group){for(const other of state.pedestals)if(other.group===p.group)other.taken=true;state.room.rewardTaken=true;state.room.itemTaken=true}if(state.room.type==='treasure')state.room.itemTaken=true;if(state.room.type==='shop'){state.room.itemTaken=state.room.shopStock.every(stock=>stock.taken)}return true}
  function updatePedestals(){for(const p of state.pedestals){if(p.taken)continue;p.phase+=.025;const touching=pointSegmentDistance(p.x,p.y,player.prevX,player.prevY,player.x,player.y)<player.r+28;if(p.requiresExit){if(!touching)p.requiresExit=false;continue}if(touching)claimPedestal(p)}}
  function takeItem(item){if(!canTakeItem(item))return false;let level=0;if(item.stackable){level=upgradeLevel(item.id)+1;player.upgrades[item.id]=level;if(item.id==='tune-barrier'){player.maxShield+=1;player.shield=Math.min(player.maxShield,player.shield+1)}}else{player.items.push(item.id);if(item.id==='cyberheart'){player.maxHp+=2;player.hp=Math.min(player.maxHp,player.hp+2)}if(item.id==='blood'){player.maxHp=Math.max(2,player.maxHp-2);player.hp=Math.min(player.hp,player.maxHp)}}const color=relicColor(item);$('#item-glyph').textContent=item.glyph;$('#item-name').textContent=item.stackable?`${item.name} // LV.${level}`:item.name;$('#item-desc').textContent=item.desc;ui.itemRarity.textContent=`${item.rarityLabel||'COMMON'} // ${item.familyLabel||'RELIC'}`;ui.itemSynergy.textContent=item.synergy||item.tag||'';ui.itemBanner.style.setProperty('--relic-color',color);ui.itemBanner.classList.remove('hidden');state.itemTimer=2.65;burst(player.x,player.y,color,rareBurstCount(item),220);renderRelicArray();beep(item.rarity==='rare'?760:640,.22,'sine',.045);haptic(item.rarity==='rare'?[35,20,55]:40);return true}
  function rareBurstCount(item){return item.rarity==='rare'||item.rarity==='ascension'?42:28}
  function updatePortal(){if(!state.portal)return;state.portal.phase=(state.portal.phase||0)+.03;if(Math.hypot(player.x-state.portal.x,player.y-state.portal.y)<player.r+state.portal.r){if(state.floor<TOTAL_FLOORS){loadFloor(state.floor+1);showToast('核心击穿 // 继续下潜')}else endGame(true)}}
  function rollSupply(room){const combatRooms=state.rooms.filter(r=>r.type==='combat'),rank=Math.max(0,combatRooms.findIndex(r=>r.key===room.key)),table=SUPPLY_TABLES[state.floor-1];let kind=forcedSupplyKind;forcedSupplyKind=null;if(!kind){let roll=Math.random(),sum=0;for(const [candidate,weight] of Object.entries(table)){sum+=weight;if(roll<sum){kind=candidate;break}}}if(state.floor<=3&&rank===combatRooms.length-1&&state.floorKeysSpawned===0)kind='key';if(state.floor>3&&kind==='key')kind='coin';kind=kind||'coin';if(kind==='key')state.floorKeysSpawned++;return kind}
  function spawnCombatSupply(room){const stock=room.supplyStock;if(stock&&!stock.taken)state.pickups.push(makePickup(stock.kind,ROOM.cx,ROOM.cy-88,stock.value,{stock,stockRoom:room}))}
  function resolveCombatSupply(room){if(!room.supplyStock){const kind=rollSupply(room);room.supplyStock={kind,value:kind==='coin'?3:1,taken:false}}spawnCombatSupply(room);return room.supplyStock.kind}
  function resolveCombatGrowth(room){const combatRooms=state.rooms.filter(r=>r.type==='combat'),rank=Math.max(0,combatRooms.findIndex(r=>r.key===room.key)),growthChance=GROWTH_CHANCES[state.floor-1][rank]??GROWTH_CHANCES[state.floor-1].at(-1),forced=forcedRewardTier;forcedRewardTier=null;const spawn=state.growthMisses>=2||(forced?forced!=='none':chance(growthChance));room.growthChance=growthChance;if(!spawn){state.growthMisses++;room.rewardTier='none';return false}state.growthMisses=0;const tier=forced&&forced!=='none'?forced:growthTierFor();room.rewardTier=tier;room.rewardOptions=growthOptions(tier);showRoomChoice(room,room.rewardOptions,`${tier==='tuning'?'武器调校':tier==='skill'?'技能模组':'稀有协同'}终端`);return true}
  function checkRoomClear(dt){if(state.room.type==='combat'&&!state.room.cleared&&state.enemies.length===0){state.room.cleared=true;state.roomsCleared++;player.activeCharge=Math.min(player.activeMax,player.activeCharge+1);const flawless=has('blackbox')&&player.damageTaken===state.room.damageStart;if(flawless){player.shield=Math.min(player.maxShield,player.shield+1);player.coins+=2}resolveCombatSupply(state.room);const grew=resolveCombatGrowth(state.room);if(flawless)showToast('无伤黑匣解锁 // 防火墙 +1 · DATA +2');else if(!grew)showToast('房间净化 // 补给已投放');beep(560,.18,'sine',.035)}if(state.itemTimer>0){state.itemTimer-=dt;if(state.itemTimer<=0)ui.itemBanner.classList.add('hidden')}}

  function roomExitUnlocked(room=state.room){if(!room)return false;return room.type==='boss'?!!(room.cleared&&room.portalReady):!!room.cleared}
  function tryDoor(dir){if(state.transition>0||!roomExitUnlocked())return false;const n=neighbors().find(n=>n.dir===dir);if(!n||!n.room.revealed)return false;if(!enterRoom(n.room,opposite(dir)))return false;return true}
  function resolveWalls(e){
    const centerGap=58;if(e.x<ROOM.x+e.r){if(Math.abs(e.y-ROOM.cy)<centerGap&&e===player&&tryDoor('left'))return;e.x=ROOM.x+e.r}if(e.x>ROOM.x+ROOM.w-e.r){if(Math.abs(e.y-ROOM.cy)<centerGap&&e===player&&tryDoor('right'))return;e.x=ROOM.x+ROOM.w-e.r}if(e.y<ROOM.y+e.r){if(Math.abs(e.x-ROOM.cx)<centerGap&&e===player&&tryDoor('up'))return;e.y=ROOM.y+e.r}if(e.y>ROOM.y+ROOM.h-e.r){if(Math.abs(e.x-ROOM.cx)<centerGap&&e===player&&tryDoor('down'))return;e.y=ROOM.y+ROOM.h-e.r}
  }
  function clampEntity(e){e.x=clamp(e.x,ROOM.x+e.r,ROOM.x+ROOM.w-e.r);e.y=clamp(e.y,ROOM.y+e.r,ROOM.y+ROOM.h-e.r)}
  function resolveObstacles(e){for(const o of state.obstacles){const dx=e.x-o.x,dy=e.y-o.y,d=Math.max(.1,Math.hypot(dx,dy)),over=e.r+o.r-d;if(over>0){e.x+=dx/d*over;e.y+=dy/d*over}}}
  function doorPosition(dir){return dir==='left'?{x:ROOM.x,y:ROOM.cy}:dir==='right'?{x:ROOM.x+ROOM.w,y:ROOM.cy}:dir==='up'?{x:ROOM.cx,y:ROOM.y}:{x:ROOM.cx,y:ROOM.y+ROOM.h}}
  function switchForm(id){if(!FORMS[id]||player.formCd>0||state.mode!=='playing')return;player.form=id;player.formCd=.22;player.fireCd=Math.max(player.fireCd,.12);burst(player.x,player.y,FORMS[id].color,22,190);$$('#forms button').forEach(b=>b.classList.toggle('active',b.dataset.form===id));showToast(`${FORMS[id].name} // ${FORMS[id].desc}`);beep({vanguard:310,wraith:450,beast:105,seraph:720}[id],.1,'square',.025);haptic(18)}

  function render(){
    if(!state.room){ctx.fillStyle='#03050c';ctx.fillRect(0,0,W,H);return}
    cacheRoomLayer();ctx.fillStyle='#03050c';ctx.fillRect(0,0,W,H);ctx.save();const sx=state.shake?Math.sin(state.time*58)*state.shake*.68:0,sy=state.shake?Math.cos(state.time*47)*state.shake*.56:0;ctx.translate(sx,sy);ctx.drawImage(roomLayer,0,0);drawHazards();drawObstacles();drawDoors();drawPortal();drawPedestals();drawPickups();drawBombs();drawTears();drawEnemies();drawPlayer();drawParticles();ctx.restore();drawPost();
  }
  function cacheRoomLayer(){const key=`${state.floor}:${state.room.key}:${state.room.type}:${state.room.index}`;if(roomLayerKey===key)return;roomLayerKey=key;drawRoom();roomLayerCtx.clearRect(0,0,W,H);roomLayerCtx.drawImage(canvas,0,0)}
  function drawRoom(){
    const themes=[['#07101b','#0c2230',COL.cyan],['#0b0818','#201133',COL.violet],['#120710','#2b0c24',COL.pink],['#100d05','#2a2510',COL.yellow],['#120709','#301018',COL.red],['#09040d','#26091f',COL.pink]],t=themes[(state.floor-1)%themes.length];ctx.fillStyle=t[0];ctx.fillRect(0,0,W,H);const g=ctx.createRadialGradient(ROOM.cx,ROOM.cy,30,ROOM.cx,ROOM.cy,650);g.addColorStop(0,t[1]);g.addColorStop(1,t[0]);ctx.fillStyle=g;ctx.fillRect(ROOM.x,ROOM.y,ROOM.w,ROOM.h);
    drawFloorPlating(t[2]);ctx.strokeStyle='rgba(90,190,220,.075)';ctx.lineWidth=1;for(let x=ROOM.x;x<=ROOM.x+ROOM.w;x+=56){ctx.beginPath();ctx.moveTo(x,ROOM.y);ctx.lineTo(x,ROOM.y+ROOM.h);ctx.stroke()}for(let y=ROOM.y;y<=ROOM.y+ROOM.h;y+=56){ctx.beginPath();ctx.moveTo(ROOM.x,y);ctx.lineTo(ROOM.x+ROOM.w,y);ctx.stroke()}
    drawRoomMotif(t[2]);ctx.strokeStyle=t[2];ctx.shadowBlur=13;ctx.shadowColor=t[2];ctx.lineWidth=5;ctx.strokeRect(ROOM.x,ROOM.y,ROOM.w,ROOM.h);ctx.shadowBlur=0;ctx.fillStyle='rgba(0,0,0,.45)';ctx.fillRect(ROOM.x,ROOM.y,ROOM.w,16);ctx.fillRect(ROOM.x,ROOM.y+ROOM.h-16,ROOM.w,16);ctx.fillRect(ROOM.x,ROOM.y,16,ROOM.h);ctx.fillRect(ROOM.x+ROOM.w-16,ROOM.y,16,ROOM.h);
    ctx.fillStyle='rgba(255,255,255,.035)';for(let i=0;i<18;i++){const x=ROOM.x+35+(i*97+state.room.index*31)%1035,y=ROOM.y+30+(i*71+state.floor*49)%510;ctx.fillRect(x,y,18+(i%3)*11,3)}
  }
  function drawFloorPlating(accent){
    ctx.save();ctx.beginPath();ctx.rect(ROOM.x,ROOM.y,ROOM.w,ROOM.h);ctx.clip();const seed=state.room.index*47+state.floor*83;for(let i=0;i<18;i++){const x=ROOM.x+24+(i*137+seed*3)%1040,y=ROOM.y+22+(i*89+seed)%515,w=74+(i%4)*28,h=34+(i%3)*18;ctx.fillStyle=i%3?'rgba(3,8,17,.18)':'rgba(255,255,255,.018)';ctx.fillRect(x,y,w,h);ctx.strokeStyle='rgba(118,153,174,.055)';ctx.strokeRect(x+.5,y+.5,w-1,h-1);ctx.fillStyle=i%4===0?accent:'rgba(138,174,192,.16)';ctx.globalAlpha=i%4===0?.18:.2;ctx.fillRect(x+8,y+8,3,3);ctx.fillRect(x+w-11,y+h-11,3,3);ctx.globalAlpha=1}
    const stain=ctx.createRadialGradient(ROOM.cx-210+state.floor*54,ROOM.cy+state.room.index*17-60,5,ROOM.cx-210+state.floor*54,ROOM.cy+state.room.index*17-60,190);stain.addColorStop(0,'rgba(100,0,48,.16)');stain.addColorStop(.55,'rgba(38,0,28,.07)');stain.addColorStop(1,'transparent');ctx.fillStyle=stain;ctx.fillRect(ROOM.x,ROOM.y,ROOM.w,ROOM.h);
    ctx.strokeStyle='rgba(30,218,222,.08)';ctx.lineWidth=8;ctx.beginPath();ctx.moveTo(ROOM.x-20,ROOM.y+110+(seed%4)*74);ctx.bezierCurveTo(ROOM.cx-280,ROOM.cy-170,ROOM.cx+240,ROOM.cy+190,ROOM.x+ROOM.w+20,ROOM.y+170+(seed%3)*90);ctx.stroke();ctx.strokeStyle='rgba(255,44,146,.11)';ctx.lineWidth=2;ctx.stroke();ctx.restore()
  }
  function drawRoomMotif(accent){
    const type=state.room.type;ctx.save();ctx.translate(ROOM.cx,ROOM.cy);ctx.lineWidth=2;
    if(type==='start'){ctx.strokeStyle='rgba(35,247,255,.12)';for(let r=70;r<=210;r+=46){ctx.beginPath();ctx.arc(0,0,r,0,TAU);ctx.stroke()}ctx.fillStyle='rgba(35,247,255,.05)';ctx.fillRect(-210,-16,420,32);ctx.fillRect(-16,-210,32,420)}
    else if(type==='combat'){ctx.strokeStyle='rgba(255,44,146,.09)';ctx.setLineDash([18,12]);ctx.beginPath();ctx.arc(0,0,245,0,TAU);ctx.stroke();for(let i=0;i<4;i++){ctx.rotate(Math.PI/2);ctx.strokeRect(205,-28,72,56)}ctx.setLineDash([])}
    else if(type==='treasure'){ctx.strokeStyle='rgba(255,227,76,.18)';for(let i=0;i<3;i++){ctx.rotate(Math.PI/3);ctx.strokeRect(-105-i*22,-105-i*22,210+i*44,210+i*44)}ctx.fillStyle='rgba(255,227,76,.035)';ctx.beginPath();ctx.arc(0,0,155,0,TAU);ctx.fill()}
    else if(type==='shop'){ctx.strokeStyle='rgba(155,92,255,.14)';for(let x=-430;x<=430;x+=172)ctx.strokeRect(x-54,-150,108,300);ctx.fillStyle='rgba(155,92,255,.08)';ctx.font='900 46px Consolas';ctx.textAlign='center';ctx.fillText('¢  BLACK MARKET  ¢',0,-200)}
    else if(type==='secret'){ctx.fillStyle='rgba(35,247,255,.09)';ctx.font='12px Consolas';ctx.textAlign='center';const glyphs=['0','1','?','∆'];for(let y=-220;y<=220;y+=32)for(let x=-480;x<=480;x+=48)if((x+y+state.floor*17)%3)ctx.fillText(glyphs[Math.abs(Math.floor(x/48+y/32+state.floor))%glyphs.length],x,y)}
    else if(type==='boss'){ctx.strokeStyle='rgba(255,44,146,.16)';for(let r=100;r<=290;r+=47){ctx.beginPath();ctx.arc(0,0,r,0,TAU);ctx.stroke()}for(let i=0;i<8;i++){ctx.rotate(Math.PI/4);ctx.fillStyle='rgba(255,44,146,.06)';ctx.fillRect(250,-9,110,18)}}
    else if(type==='vault'){ctx.strokeStyle='rgba(35,247,255,.2)';for(let i=0;i<4;i++){ctx.rotate(Math.PI/4);ctx.strokeRect(-78-i*24,-78-i*24,156+i*48,156+i*48)}ctx.font='900 74px Consolas';ctx.textAlign='center';ctx.fillStyle='rgba(35,247,255,.08)';ctx.fillText('◇',0,25)}
    else if(type==='sanctum'){ctx.strokeStyle='rgba(255,227,76,.22)';for(let r=70;r<=250;r+=45){ctx.beginPath();for(let i=0;i<6;i++){const a=i/6*TAU,x=Math.cos(a)*r,y=Math.sin(a)*r;i?ctx.lineTo(x,y):ctx.moveTo(x,y)}ctx.closePath();ctx.stroke()}ctx.font='900 64px Consolas';ctx.textAlign='center';ctx.fillStyle='rgba(255,227,76,.09)';ctx.fillText('✦',0,22)}
    ctx.restore();
  }
  function drawDoors(){for(const n of neighbors()){const p=doorPosition(n.dir),hidden=n.room.type==='secret'&&!n.room.revealed;if(hidden){ctx.save();ctx.strokeStyle='rgba(255,44,146,.22)';ctx.setLineDash([3,8]);ctx.beginPath();ctx.arc(p.x,p.y,34,0,TAU);ctx.stroke();ctx.restore();continue}const locked=!roomExitUnlocked(),gateCost=!n.room.visited?(SPECIAL_ROOM_COSTS[n.room.type]||0):0;ctx.save();ctx.translate(p.x,p.y);if(n.dir==='left'||n.dir==='right')ctx.rotate(Math.PI/2);ctx.fillStyle='#02050d';ctx.fillRect(-58,-21,116,42);ctx.strokeStyle=locked?COL.pink:n.room.type==='boss'?COL.pink:n.room.type==='treasure'||n.room.type==='sanctum'?COL.yellow:n.room.type==='shop'?COL.violet:COL.cyan;ctx.lineWidth=3;ctx.shadowBlur=12;ctx.shadowColor=ctx.strokeStyle;ctx.strokeRect(-54,-18,108,36);if(locked){ctx.fillStyle=COL.pink;for(let x=-38;x<44;x+=16)ctx.fillRect(x,-15,5,30)}else if(gateCost){ctx.fillStyle=ctx.strokeStyle;ctx.font='900 13px Consolas';ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillText(`◇ ${gateCost}`,0,1)}else{ctx.fillStyle=ctx.strokeStyle;ctx.fillRect(-35,-2,70,4)}ctx.restore()}}
  function drawObstacles(){const grouped=new Map;for(const o of state.obstacles)if(o.wallGroup){if(!grouped.has(o.wallGroup))grouped.set(o.wallGroup,[]);grouped.get(o.wallGroup).push(o)}for(const nodes of grouped.values()){nodes.sort((a,b)=>a.wallIndex-b.wallIndex);const color=nodes[0].breakable?COL.pink:'#486477';for(let i=1;i<nodes.length;i++){const a=nodes[i-1],b=nodes[i];if(b.wallIndex-a.wallIndex!==1)continue;ctx.save();ctx.lineCap='square';ctx.beginPath();ctx.moveTo(a.x,a.y);ctx.lineTo(b.x,b.y);ctx.strokeStyle='#101927';ctx.lineWidth=a.r*1.4;ctx.shadowBlur=18;ctx.shadowColor=color;ctx.stroke();ctx.strokeStyle=color;ctx.lineWidth=3;ctx.stroke();ctx.restore()}}for(const o of state.obstacles){ctx.save();ctx.translate(o.x,o.y);ctx.rotate(o.wallGroup?o.wallAngle:Math.PI/4);ctx.fillStyle='#101927';ctx.strokeStyle=o.breakable?COL.pink:'#486477';ctx.lineWidth=3;ctx.shadowBlur=o.wallGroup?16:10;ctx.shadowColor=ctx.strokeStyle;if(o.wallGroup){const w=o.wallStep+12,h=o.r*1.18;ctx.fillRect(-w/2,-h/2,w,h);ctx.strokeRect(-w/2,-h/2,w,h);ctx.fillStyle=ctx.strokeStyle;ctx.fillRect(-w*.36,-3,w*.72,6);ctx.fillStyle='#dffaff';ctx.fillRect(-w*.42,-h*.28,3,3);ctx.fillRect(w*.42-3,h*.28-3,3,3)}else{ctx.fillRect(-o.r*.72,-o.r*.72,o.r*1.44,o.r*1.44);ctx.strokeRect(-o.r*.72,-o.r*.72,o.r*1.44,o.r*1.44);ctx.fillStyle=ctx.strokeStyle;ctx.fillRect(-4,-o.r*.48,8,o.r*.96)}ctx.restore()}}
  function relicColor(item){return item.color||RELIC_RARITIES[item.rarity]?.color||COL.yellow}
  function drawPolygon(sides,r,rotation=0){ctx.beginPath();for(let i=0;i<sides;i++){const a=rotation+i/sides*TAU,x=Math.cos(a)*r,y=Math.sin(a)*r;i?ctx.lineTo(x,y):ctx.moveTo(x,y)}ctx.closePath()}
  function drawRelicSigil(item,size=28){
    const color=relicColor(item),family=item.family||item.category||'skill';ctx.save();ctx.shadowBlur=16;ctx.shadowColor=color;ctx.strokeStyle=color;ctx.fillStyle='rgba(3,8,17,.94)';ctx.lineWidth=2;
    if(family==='weapon'){ctx.rotate(Math.PI/4);ctx.strokeRect(-size*.55,-size*.55,size*1.1,size*1.1);ctx.rotate(-Math.PI/4);ctx.beginPath();ctx.moveTo(-size*.85,0);ctx.lineTo(-size*.48,0);ctx.moveTo(size*.48,0);ctx.lineTo(size*.85,0);ctx.stroke()}
    else if(family==='core'){drawPolygon(3,size*.72,-Math.PI/2);ctx.fill();ctx.stroke();ctx.beginPath();ctx.arc(0,0,size*.88,0,TAU);ctx.stroke()}
    else if(family==='ascension'){for(let i=0;i<3;i++){ctx.rotate(Math.PI/3);ctx.strokeRect(-size*.56,-size*.56,size*1.12,size*1.12)}}
    else if(family==='tuning'){ctx.beginPath();ctx.arc(0,0,size*.7,0,TAU);ctx.stroke();for(let i=0;i<6;i++){ctx.rotate(TAU/6);ctx.fillRect(size*.62,-2,size*.32,4)}}
    else{ctx.beginPath();ctx.arc(0,0,size*.72,0,TAU);ctx.fill();ctx.stroke();for(let i=0;i<4;i++){ctx.rotate(Math.PI/2);ctx.beginPath();ctx.arc(size*.67,0,size*.22,0,TAU);ctx.stroke()}}
    ctx.rotate(-state.time*.35);ctx.setLineDash([3,5]);ctx.beginPath();ctx.arc(0,0,size,0,TAU);ctx.stroke();ctx.setLineDash([]);ctx.rotate(state.time*.35);ctx.shadowBlur=8;ctx.fillStyle='#f6ffff';ctx.font=`900 ${Math.round(size*.72)}px Consolas`;ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillText(item.glyph||'◈',0,1);ctx.restore()
  }
  function drawRelicFocus(p){
    const item=p.item,color=relicColor(item),family=RELIC_FAMILIES[item.family]||RELIC_FAMILIES.skill,rarity=item.rarityLabel||RELIC_RARITIES[item.rarity]?.label||'COMMON',w=246,h=72,x=clamp(p.x-w/2,ROOM.x+20,ROOM.x+ROOM.w-w-20),y=clamp(p.y-126,ROOM.y+24,ROOM.y+ROOM.h-h-24);ctx.save();ctx.translate(x,y);ctx.fillStyle='rgba(2,6,15,.95)';ctx.strokeStyle=color;ctx.lineWidth=1.5;ctx.shadowBlur=20;ctx.shadowColor=color;ctx.fillRect(0,0,w,h);ctx.strokeRect(0,0,w,h);ctx.shadowBlur=0;ctx.fillStyle=color;ctx.fillRect(0,0,5,h);ctx.font='700 8px Consolas';ctx.fillText(`${rarity} // ${family.label}`,14,15);ctx.fillStyle='#f5ffff';ctx.font='900 13px "Microsoft YaHei",sans-serif';ctx.fillText(item.name,14,34);ctx.fillStyle='#93aabd';ctx.font='9px "Microsoft YaHei",sans-serif';ctx.fillText(item.desc,14,52);ctx.fillStyle=color;ctx.font='700 7px Consolas';ctx.fillText(item.tag||'NEURAL RELIC',14,66);ctx.restore()
  }
  function drawPedestals(){
    let focused=null,focusDistance=Infinity;for(const p of state.pedestals){if(p.taken)continue;const d=Math.hypot(player.x-p.x,player.y-p.y);if(d<focusDistance){focusDistance=d;focused=p}const y=p.y+Math.sin(p.phase)*6,item=p.item,color=p.cost?COL.violet:relicColor(item),rare=item.rarity==='rare'||item.rarity==='ascension';ctx.save();ctx.translate(p.x,y);ctx.globalAlpha=.12;ctx.fillStyle=color;ctx.beginPath();ctx.moveTo(-24,-96);ctx.lineTo(24,-96);ctx.lineTo(38,30);ctx.lineTo(-38,30);ctx.closePath();ctx.fill();ctx.globalAlpha=1;ctx.strokeStyle=color;ctx.shadowBlur=rare?28:18;ctx.shadowColor=color;ctx.lineWidth=2;ctx.beginPath();ctx.ellipse(0,31,42,13,0,0,TAU);ctx.fillStyle='rgba(3,7,16,.94)';ctx.fill();ctx.stroke();ctx.beginPath();ctx.ellipse(0,25,30,8,0,0,TAU);ctx.stroke();ctx.translate(0,-8);drawRelicSigil(item,rare?31:27);ctx.translate(0,8);ctx.shadowBlur=0;ctx.font='700 7px Consolas';ctx.textAlign='center';ctx.fillStyle=color;ctx.fillText(`${item.rarityLabel||'COMMON'} // ${item.familyLabel||'RELIC'}`,0,53);ctx.font='900 9px "Microsoft YaHei",sans-serif';ctx.fillStyle='#dff8ff';ctx.fillText(item.stackable?`${item.name} ${upgradeLevel(item.id)+1}${item.repeatable?'':`/${item.max}`}`:item.name,0,67);if(p.cost){ctx.font='900 12px Consolas';ctx.fillStyle=COL.yellow;ctx.fillText(`¢ ${p.cost}`,0,84)}else if(p.roomChoice){ctx.font='700 7px Consolas';ctx.fillStyle=COL.cyan;ctx.fillText('NEURAL LINK // CHOOSE ONE',0,82)}ctx.restore()}if(focused&&focusDistance<145)drawRelicFocus(focused)
  }
  function drawPickups(){for(const p of state.pickups){ctx.save();ctx.translate(p.x,p.y);const data={coin:['¢',COL.yellow],key:['◇',COL.cyan],bomb:['●',COL.pink],heart:['▣',COL.red],shield:['⬡',COL.violet]}[p.kind],fx=clamp(p.spawnFx/1.05,0,1),q=1-fx,scale=1+fx*.48+Math.sin(p.phase)*.055;ctx.textAlign='center';ctx.textBaseline='middle';ctx.strokeStyle=data[1];ctx.shadowColor=data[1];ctx.shadowBlur=fx>0?30:18;ctx.globalAlpha=.48+fx*.5;ctx.lineWidth=2.5;ctx.beginPath();ctx.arc(0,0,18+Math.sin(p.phase)*2,0,TAU);ctx.stroke();if(fx>0){ctx.globalAlpha=Math.pow(fx,1.35);ctx.lineWidth=3;ctx.beginPath();ctx.arc(0,0,21+q*34,0,TAU);ctx.stroke();ctx.rotate(Math.PI/4+q*.35);ctx.strokeRect(-15-q*18,-15-q*18,30+q*36,30+q*36);ctx.rotate(-Math.PI/4-q*.35)}ctx.globalAlpha=1;ctx.scale(scale,scale);ctx.font='900 25px Consolas';ctx.lineWidth=5;ctx.strokeStyle='rgba(2,4,10,.92)';ctx.strokeText(data[0],0,0);ctx.lineWidth=1.5;ctx.strokeStyle='#fff';ctx.strokeText(data[0],0,0);ctx.fillStyle=data[1];ctx.fillText(data[0],0,0);if(p.value>1){ctx.font='900 11px Consolas';ctx.lineWidth=3;ctx.strokeStyle='#02040a';ctx.strokeText(`×${p.value}`,0,22);ctx.fillStyle='#fff';ctx.fillText(`×${p.value}`,0,22)}ctx.restore()}}
  function drawBombs(){for(const b of state.bombEntities){ctx.save();ctx.translate(b.x,b.y);ctx.fillStyle=b.life<.35&&Math.floor(b.life*20)%2?COL.ink:COL.pink;ctx.shadowBlur=18;ctx.shadowColor=COL.pink;ctx.beginPath();ctx.arc(0,0,b.r,0,TAU);ctx.fill();ctx.strokeStyle=COL.yellow;ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(5,-10);ctx.quadraticCurveTo(16,-25,20,-13);ctx.stroke();ctx.restore()}}
  function drawTears(){
    const pressure=fxPressure(),reduced=pressure>88;for(const t of state.tears){ctx.save();ctx.translate(t.x,t.y);const a=Math.atan2(t.vy,t.vx),travel=Math.max(14,Math.hypot(t.x-t.px,t.y-t.py)*(reduced?1.25:1.75)),r=t.r;ctx.rotate(a);ctx.globalCompositeOperation='source-over';ctx.shadowBlur=0;ctx.globalAlpha=reduced?.48:.72;const glow=tearGlowSprites[t.form]||tearGlowSprites.vanguard;ctx.drawImage(glow,-r*3.8,-r*3.8,r*7.6,r*7.6);ctx.globalAlpha=1;
      if(t.form==='wraith'){ctx.globalAlpha=.42;ctx.strokeStyle=t.color;ctx.lineWidth=Math.max(2,r*.55);for(const y of [-r*.72,r*.72]){ctx.beginPath();ctx.moveTo(-travel*1.55,y);ctx.lineTo(-2,y);ctx.stroke()}ctx.globalAlpha=1;ctx.fillStyle='#f8efff';ctx.beginPath();ctx.moveTo(r*1.9,0);ctx.lineTo(0,r*.62);ctx.lineTo(-r*1.35,0);ctx.lineTo(0,-r*.62);ctx.closePath();ctx.fill();ctx.strokeStyle=t.color;ctx.lineWidth=2;ctx.stroke()}
      else if(t.form==='beast'){ctx.fillStyle=t.color;ctx.beginPath();ctx.moveTo(r*2,0);ctx.lineTo(-r*.4,r);ctx.lineTo(-travel*.55,r*.55);ctx.lineTo(-travel,-r*.1);ctx.lineTo(-travel*.48,-r*.72);ctx.lineTo(-r*.35,-r);ctx.closePath();ctx.fill();ctx.fillStyle='#fff0a8';ctx.beginPath();ctx.ellipse(r*.35,0,r*.9,r*.42,0,0,TAU);ctx.fill()}
      else if(t.form==='seraph'){ctx.globalAlpha=.62;ctx.strokeStyle=t.color;ctx.lineWidth=r*1.7;ctx.beginPath();ctx.moveTo(-travel*2.25,0);ctx.lineTo(r*1.8,0);ctx.stroke();ctx.globalAlpha=1;ctx.strokeStyle='#fffde0';ctx.lineWidth=Math.max(2.5,r*.46);ctx.beginPath();ctx.moveTo(-travel*1.6,0);ctx.lineTo(r*2,0);ctx.stroke();ctx.strokeStyle=t.color;ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(0,-r*1.4);ctx.lineTo(0,r*1.4);ctx.stroke()}
      else if(t.form==='drone'){ctx.globalAlpha=.55;ctx.strokeStyle=COL.cyan;ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(-travel,0);ctx.lineTo(0,0);ctx.stroke();ctx.globalAlpha=1;ctx.fillStyle='#fff';ctx.rotate(Math.PI/4);ctx.fillRect(-r*.65,-r*.65,r*1.3,r*1.3)}
      else{ctx.globalAlpha=.48;ctx.strokeStyle=t.color;ctx.lineWidth=Math.max(2,r*.7);ctx.beginPath();ctx.moveTo(-travel*1.25,0);ctx.lineTo(-1,0);ctx.stroke();ctx.globalAlpha=1;ctx.fillStyle='#f5ffff';ctx.beginPath();ctx.ellipse(0,0,r*1.7,r*.68,0,0,TAU);ctx.fill();ctx.strokeStyle=t.color;ctx.lineWidth=2;ctx.stroke();ctx.beginPath();ctx.moveTo(-r*2,-r*.9);ctx.lineTo(-r*2,r*.9);ctx.stroke()}
      if(t.crit){ctx.globalAlpha=.9;ctx.strokeStyle=COL.yellow;ctx.lineWidth=2;ctx.beginPath();ctx.arc(0,0,r*2.25,0,TAU);ctx.stroke()}ctx.restore()}
    for(const b of state.enemyBullets){ctx.save();ctx.translate(b.x,b.y);const a=Math.atan2(b.vy,b.vx),glow=projectileGlowSprites[b.source==='boss'?COL.red:b.color]||projectileGlowSprites[COL.pink],glowSize=b.r*(b.source==='boss'?7.4:5.8);ctx.globalAlpha=reduced?.38:.58;ctx.drawImage(glow,-glowSize/2,-glowSize/2,glowSize,glowSize);ctx.globalAlpha=1;if(b.source==='boss'){const vr=b.r*1.78;ctx.rotate(state.time*(3.2+b.phase*.45)+b.spin);ctx.shadowBlur=reduced?0:10;ctx.shadowColor=COL.red;ctx.strokeStyle=COL.yellow;ctx.lineWidth=2;if(!reduced)ctx.setLineDash([4,4]);ctx.beginPath();ctx.arc(0,0,vr*1.35,0,TAU);ctx.stroke();ctx.setLineDash([]);ctx.strokeStyle=COL.red;ctx.lineWidth=3;ctx.beginPath();ctx.arc(0,0,vr,0,TAU);ctx.stroke();ctx.rotate(-state.time*5.2);ctx.beginPath();for(let i=0;i<6;i++){const q=i/6*TAU,x=Math.cos(q)*vr*.78,y=Math.sin(q)*vr*.78;i?ctx.lineTo(x,y):ctx.moveTo(x,y)}ctx.closePath();ctx.fillStyle='#12030b';ctx.fill();ctx.strokeStyle=b.color;ctx.stroke();ctx.fillStyle='#fff0a8';ctx.beginPath();ctx.arc(0,0,b.r*.42,0,TAU);ctx.fill()}else{const vr=b.r*1.28;ctx.rotate(a+Math.PI/4);ctx.shadowBlur=0;ctx.fillStyle='#070913';ctx.strokeStyle=b.color;ctx.lineWidth=2.5;ctx.fillRect(-vr*.72,-vr*.72,vr*1.44,vr*1.44);ctx.strokeRect(-vr*.72,-vr*.72,vr*1.44,vr*1.44);ctx.fillStyle='#fff';ctx.beginPath();ctx.arc(0,0,b.r*.28,0,TAU);ctx.fill()}ctx.restore()}ctx.shadowBlur=0
  }
  function drawAvatarShell(form,color,alpha=1){
    const baseAlpha=ctx.globalAlpha;ctx.save();ctx.globalAlpha=baseAlpha*alpha;ctx.strokeStyle=color;ctx.shadowColor=color;ctx.shadowBlur=18;ctx.lineWidth=2.5;
    if(form==='wraith'){ctx.fillStyle='rgba(155,92,255,.18)';for(const y of [-13,13]){ctx.beginPath();ctx.moveTo(-30,y);ctx.lineTo(-9,y*.62);ctx.lineTo(-17,y*.1);ctx.closePath();ctx.fill();ctx.stroke()}}
    if(form==='beast'){ctx.fillStyle='#27060c';for(const y of [-1,1]){ctx.beginPath();ctx.moveTo(-8,y*15);ctx.lineTo(-30,y*26);ctx.lineTo(-21,y*6);ctx.lineTo(5,y*12);ctx.closePath();ctx.fill();ctx.stroke()}for(const x of [-17,-8,1]){ctx.beginPath();ctx.moveTo(x,-17);ctx.lineTo(x-5,-29-Math.abs(x)*.16);ctx.lineTo(x+4,-16);ctx.fill();ctx.stroke()}}
    if(form==='seraph'){ctx.globalAlpha*=.8;for(const y of [-1,1])for(let i=0;i<3;i++){ctx.beginPath();ctx.moveTo(-8,y*(8+i*4));ctx.quadraticCurveTo(-26-i*5,y*(20+i*7),-43-i*4,y*(12+i*4));ctx.stroke()}ctx.beginPath();ctx.ellipse(-5,0,34,28,0,0,TAU);ctx.globalAlpha*=.32;ctx.stroke()}
    if(form==='vanguard'){ctx.fillStyle='rgba(35,247,255,.14)';ctx.beginPath();ctx.moveTo(-14,-20);ctx.lineTo(-31,-13);ctx.lineTo(-31,13);ctx.lineTo(-14,20);ctx.closePath();ctx.fill();ctx.stroke()}
    ctx.globalAlpha=baseAlpha*alpha;ctx.fillStyle='#07111b';ctx.beginPath();ctx.moveTo(-18,-13);ctx.quadraticCurveTo(-2,-24,17,-13);ctx.lineTo(25,0);ctx.lineTo(17,13);ctx.quadraticCurveTo(-2,24,-18,13);ctx.lineTo(-24,0);ctx.closePath();ctx.fill();ctx.stroke();
    ctx.shadowBlur=8;ctx.fillStyle='#bad6df';ctx.beginPath();ctx.moveTo(-10,-12);ctx.quadraticCurveTo(8,-17,18,-8);ctx.lineTo(14,8);ctx.quadraticCurveTo(0,15,-11,9);ctx.closePath();ctx.fill();ctx.strokeStyle='rgba(255,255,255,.45)';ctx.stroke();
    ctx.fillStyle='#02060c';ctx.beginPath();ctx.moveTo(4,-10);ctx.lineTo(18,-7);ctx.lineTo(20,0);ctx.lineTo(17,7);ctx.lineTo(4,10);ctx.closePath();ctx.fill();ctx.strokeStyle=color;ctx.stroke();
    ctx.fillStyle=color;ctx.shadowBlur=12;ctx.beginPath();ctx.ellipse(13,0,5,2.8,0,0,TAU);ctx.fill();ctx.fillRect(22,-3,12,6);ctx.strokeRect(20,-6,16,12);
    ctx.strokeStyle=color;ctx.globalAlpha=baseAlpha*alpha*.65;ctx.beginPath();ctx.moveTo(-15,-8);ctx.lineTo(-6,-3);ctx.lineTo(-15,3);ctx.moveTo(-9,8);ctx.lineTo(0,4);ctx.stroke();
    if(form==='seraph'){ctx.globalAlpha=baseAlpha*alpha;ctx.strokeStyle=COL.yellow;ctx.lineWidth=2;ctx.beginPath();ctx.ellipse(2,0,14,25,0,0,TAU);ctx.stroke();ctx.fillStyle='#fff8c8';ctx.fillRect(31,-2,12,4)}
    if(form==='beast'){ctx.globalAlpha=baseAlpha*alpha;ctx.fillStyle=COL.red;ctx.beginPath();ctx.moveTo(18,-9);ctx.lineTo(35,-14);ctx.lineTo(27,-2);ctx.lineTo(38,0);ctx.lineTo(27,2);ctx.lineTo(35,14);ctx.lineTo(18,9);ctx.closePath();ctx.fill()}
    ctx.restore()
  }
  function drawAtlasBlend(image,row,frame,nextFrame,blend,columns,rows,width,height){if(nextFrame===null||blend<=.001)return drawAtlasCell(image,row,frame,columns,rows,width,height);const alpha=ctx.globalAlpha;ctx.globalAlpha=alpha*(1-blend);drawAtlasCell(image,row,frame,columns,rows,width,height);ctx.globalAlpha=alpha*blend;drawAtlasCell(image,row,nextFrame,columns,rows,width,height);ctx.globalAlpha=alpha;return true}
  function drawPlayerAtlas(form,frame,color,alpha=1,nextFrame=null,blend=0){const image=SPRITE_ATLASES.player;if(!atlasReady(image))return false;ctx.save();ctx.globalAlpha*=alpha;drawAtlasBlend(image,FORM_SPRITE_ROWS[form],frame,nextFrame,blend,4,4,144,144);ctx.restore();return true}
  function drawEnemyAtlas(e){const image=SPRITE_ATLASES.enemy;if(!atlasReady(image))return false;const moving=e.type!=='turret',attacking=e.attackPulse>0||e.warning>0||e.dash>0,cycle=(state.time*6+e.uid*.37)%2,frame=attacking?3:moving?(cycle<1?1:2):0,nextFrame=attacking||!moving?null:cycle<1?2:1,blend=attacking||!moving?0:cycle%1,angle=Math.atan2(player.y-e.y,player.x-e.x),width=e.r*(e.elite?6.7:6.15),height=width/(image.naturalWidth/4/(image.naturalHeight/5));ctx.save();ctx.globalAlpha=e.elite ? .22 : .13;ctx.fillStyle=e.color;ctx.beginPath();ctx.ellipse(e.x,e.y,e.r*1.65,e.r*1.05,0,0,TAU);ctx.fill();ctx.restore();ctx.save();ctx.translate(e.x,e.y);ctx.rotate(angle);drawAtlasBlend(image,ENEMY_SPRITE_ROWS[e.type],frame,nextFrame,blend,4,5,width,height);ctx.restore();if(e.elite){ctx.save();ctx.translate(e.x,e.y);ctx.rotate(-state.time*.9);ctx.setLineDash([5,5]);ctx.strokeStyle=COL.yellow;ctx.lineWidth=2.5;ctx.beginPath();ctx.arc(0,0,e.r+11,0,TAU);ctx.stroke();ctx.restore()}return true}
  function bossAtlasAction(b){
    const entry=BOSS_ATLASES[b.bossId],last=entry?entry.columns-1:0;if(!entry)return{frame:0,nextFrame:null,blend:0,action:'fallback'};
    if(b.transformPulse>0)return{frame:last,nextFrame:null,blend:0,action:'transform'};
    if(b.warning>0)return{frame:Math.min(3,last),nextFrame:null,blend:0,action:'charge'};
    if(b.attackPulse>0)return{frame:Math.min(4,last),nextFrame:null,blend:0,action:'attack'};
    if(b.dash>0)return{frame:Math.min(2,last),nextFrame:null,blend:0,action:'move'};
    const cycle=(state.time*2.5+b.uid*.13)%2;return{frame:cycle<1?0:1,nextFrame:cycle<1?1:0,blend:cycle%1,action:'idle'}
  }
  function drawBossMotionFx(b,front=false){
    const phase=b.phase||0,transform=clamp((b.transformPulse||0)/1.05,0,1);ctx.save();ctx.globalCompositeOperation='lighter';
    if(transform>0){ctx.globalAlpha=.22+transform*.5;ctx.strokeStyle=b.color;ctx.lineWidth=3+transform*5;for(let i=0;i<3;i++){ctx.beginPath();ctx.arc(0,0,b.r*(1.05+i*.38+(1-transform)*1.1),0,TAU);ctx.stroke()}}
    if(b.bossId==='cherub'){
      const spin=b.bladeAngle||0,radius=b.r*(1.2+phase*.13);ctx.lineCap='round';for(let i=0;i<6;i++){const a=spin+i*TAU/6,trail=front?0:.72;ctx.save();ctx.rotate(a);ctx.globalAlpha=front?.58:.12+phase*.045;ctx.strokeStyle=i%2?COL.cyan:COL.yellow;ctx.lineWidth=front?2.2:8+phase*2;ctx.beginPath();ctx.arc(0,0,radius,-trail,0);ctx.stroke();if(front){ctx.translate(radius,0);ctx.rotate(.12);ctx.fillStyle=i%2?'rgba(35,247,255,.75)':'rgba(255,227,76,.75)';ctx.beginPath();ctx.moveTo(-18,-3);ctx.lineTo(17,0);ctx.lineTo(-18,3);ctx.closePath();ctx.fill()}ctx.restore()}
      if(front){ctx.globalAlpha=.32;ctx.strokeStyle=COL.yellow;ctx.lineWidth=2;ctx.beginPath();ctx.arc(0,0,radius*.78,0,TAU);ctx.stroke()}
    }else if(b.bossId==='matriarch'){
      const nodes=3+phase*2,spin=state.time*(.7+phase*.32);for(let i=0;i<nodes;i++){const a=spin+i*TAU/nodes,r=b.r*(1.18+(i%2)*.24);ctx.save();ctx.rotate(a);ctx.translate(r,0);ctx.globalAlpha=front?.68:.15;ctx.strokeStyle=i%2?COL.violet:b.color;ctx.fillStyle='rgba(155,92,255,.25)';ctx.lineWidth=2;ctx.beginPath();ctx.ellipse(0,0,9+phase*2,4+phase,0,0,TAU);ctx.fill();ctx.stroke();ctx.restore()}if(!front){ctx.globalAlpha=.13;ctx.strokeStyle=COL.violet;ctx.lineWidth=5;for(let i=0;i<2+phase;i++){ctx.beginPath();ctx.ellipse(Math.sin(state.time*2+i)*18,0,b.r*(1.1+i*.18),b.r*(.42+i*.08),spin+i,0,TAU);ctx.stroke()}}
    }else{
      const spin=-state.time*(.85+phase*.4);for(let i=0;i<6;i++){const a=spin+i*TAU/6,r=b.r*(1.2+phase*.13);ctx.save();ctx.rotate(a);ctx.globalAlpha=front?.6:.13;ctx.strokeStyle=i%2?(phase===2?COL.red:COL.pink):COL.cyan;ctx.lineWidth=front?2.5:7;ctx.beginPath();ctx.arc(0,0,r,-.48,.18);ctx.stroke();if(front){ctx.translate(r,0);ctx.rotate(Math.PI/4);ctx.strokeRect(-5,-5,10,10)}ctx.restore()}if(front){ctx.globalAlpha=.3;ctx.rotate(spin*.45);ctx.setLineDash([5,6]);ctx.strokeStyle=phase===2?COL.red:COL.yellow;ctx.lineWidth=2;ctx.beginPath();ctx.arc(0,0,b.r*(.9+phase*.12),0,TAU);ctx.stroke()}
    }ctx.restore()
  }
  function drawBossAtlas(b){const entry=ensureBossAtlas(b.bossId),image=entry?.image;if(!entry||!atlasReady(image))return false;const action=bossAtlasAction(b),size=b.r*(b.bossTier>1?4.8:4.45),pulse=1+Math.sin(state.time*3+b.phase)*.025;ctx.save();ctx.translate(b.x,b.y);ctx.scale(pulse,pulse);drawBossMotionFx(b,false);ctx.filter=`drop-shadow(0 0 18px ${b.color})`;drawAtlasBlend(image,b.phase,action.frame,action.nextFrame,action.blend,entry.columns,entry.rows,size,size);ctx.filter='none';drawBossMotionFx(b,true);if(b.phase>0){ctx.rotate(state.time*(b.phase===2?1.2:.7));ctx.setLineDash([4,7]);ctx.strokeStyle=b.bossTier>1?COL.red:COL.pink;ctx.lineWidth=2;ctx.beginPath();ctx.arc(0,0,b.r*(1.62+b.phase*.13),0,TAU);ctx.stroke()}if(b.warning>0){ctx.globalAlpha=.5+.35*Math.sin(state.time*30);ctx.strokeStyle=COL.red;ctx.lineWidth=5;ctx.beginPath();ctx.arc(0,0,b.r*1.82,0,TAU);ctx.stroke()}ctx.restore();return true}
  function drawPlayer(){
    const f=FORMS[player.form],invulnAlpha=player.invuln>0?.62+Math.sin(state.time*TAU*7)*.14:1,moving=Math.hypot(player.x-player.prevX,player.y-player.prevY)>.35,attacking=player.recoil>0,cycle=state.time*8%2,frame=attacking?3:moving?(cycle<1?1:2):0,nextFrame=attacking||!moving?null:cycle<1?2:1,blend=attacking||!moving?0:cycle%1,recoil=player.recoil||0,facing=player.faceX<0?-1:1,glow=tearGlowSprites[player.form]||tearGlowSprites.vanguard;ctx.save();ctx.globalAlpha=.3;ctx.drawImage(glow,player.x-54,player.y-54,108,108);ctx.globalAlpha=.34;ctx.fillStyle='#000';ctx.beginPath();ctx.ellipse(player.x,player.y+16,28,10,0,0,TAU);ctx.fill();ctx.restore();ctx.save();ctx.globalAlpha=invulnAlpha;ctx.translate(player.x-recoil*player.aimX,player.y-recoil*player.aimY);ctx.scale(facing,1);if(atlasReady(SPRITE_ATLASES.player)){if(player.form==='wraith'){ctx.save();ctx.translate(-15,8);drawPlayerAtlas('wraith',frame,f.color,.13,nextFrame,blend);ctx.restore();ctx.save();ctx.translate(-10,-7);drawPlayerAtlas('wraith',frame,f.color,.2,nextFrame,blend);ctx.restore()}drawPlayerAtlas(player.form,frame,f.color,1,nextFrame,blend)}else{if(player.form==='wraith'){ctx.save();ctx.translate(-18,9);drawAvatarShell('wraith',f.color,.16);ctx.restore();ctx.save();ctx.translate(-12,-8);drawAvatarShell('wraith',f.color,.24);ctx.restore()}drawAvatarShell(player.form,f.color,1)}ctx.restore();
    if(player.invuln>0||has('votive')&&player.hp/player.maxHp<=.5){ctx.save();ctx.globalAlpha=.18+Math.sin(state.time*TAU*5)*.07;ctx.strokeStyle=has('votive')?COL.red:COL.pink;ctx.lineWidth=2;ctx.setLineDash([5,6]);ctx.beginPath();ctx.arc(player.x,player.y,30,0,TAU);ctx.stroke();ctx.restore()}
    const drones=familiarCount();for(let i=0;i<drones;i++){const da=(player.droneAngle||0)+i/drones*TAU,x=player.x+Math.cos(da)*46,y=player.y+Math.sin(da)*46;ctx.save();ctx.translate(x,y);ctx.rotate(da+Math.PI/2);ctx.shadowBlur=16;ctx.shadowColor=COL.cyan;ctx.strokeStyle=COL.cyan;ctx.fillStyle='#07111b';ctx.lineWidth=2;drawPolygon(6,10,Math.PI/6);ctx.fill();ctx.stroke();ctx.fillStyle=has('choir')?COL.yellow:COL.cyan;ctx.beginPath();ctx.arc(0,0,3,0,TAU);ctx.fill();ctx.beginPath();ctx.moveTo(-14,0);ctx.lineTo(-7,0);ctx.moveTo(7,0);ctx.lineTo(14,0);ctx.stroke();ctx.restore()}
  }
  function drawEnemies(){for(const e of state.enemies){if(e.boss){drawBoss(e);continue}ctx.save();ctx.globalAlpha=.32;ctx.fillStyle='#000';ctx.beginPath();ctx.ellipse(e.x,e.y+e.r*.72,e.r*1.05,e.r*.42,0,0,TAU);ctx.fill();ctx.restore();if(drawEnemyAtlas(e)){drawEnemyHp(e);continue}ctx.save();ctx.translate(e.x,e.y);ctx.rotate(e.angle||0);ctx.shadowBlur=e.elite?24:12;ctx.shadowColor=e.color;ctx.fillStyle='#080d16';ctx.strokeStyle=e.color;ctx.lineWidth=e.elite?3.5:2;
      if(e.type==='crawler'){for(let i=0;i<6;i++){const a=(i/6-.5)*Math.PI*.9,side=i<3?-1:1,y=(i%3-1)*7;ctx.beginPath();ctx.moveTo(-3,y);ctx.lineTo(-e.r*.7,y+side*3);ctx.lineTo(-e.r*1.25,y+Math.sin(a)*10);ctx.stroke()}ctx.beginPath();ctx.ellipse(0,0,e.r*.9,e.r*.62,0,0,TAU);ctx.fill();ctx.stroke();ctx.fillStyle='#25101f';ctx.beginPath();ctx.ellipse(-e.r*.35,0,e.r*.46,e.r*.54,0,0,TAU);ctx.fill();ctx.stroke();ctx.fillStyle=e.color;for(const y of [-6,6]){ctx.beginPath();ctx.arc(e.r*.35,y,3.2,0,TAU);ctx.fill()}}
      else if(e.type==='shooter'){ctx.beginPath();ctx.arc(0,0,e.r*.92,0,TAU);ctx.fill();ctx.stroke();ctx.globalAlpha=.5;ctx.beginPath();ctx.arc(0,0,e.r*.68,0,TAU);ctx.stroke();ctx.globalAlpha=1;ctx.fillStyle='#101a28';ctx.fillRect(-e.r*.22,-e.r-11,e.r*.44,e.r+14);ctx.strokeRect(-e.r*.22,-e.r-11,e.r*.44,e.r+14);ctx.fillStyle=e.color;ctx.beginPath();ctx.ellipse(4,2,8,5,0,0,TAU);ctx.fill();ctx.fillStyle='#fff';ctx.beginPath();ctx.arc(6,2,2.5,0,TAU);ctx.fill()}
      else if(e.type==='charger'){ctx.fillStyle='#190810';ctx.beginPath();ctx.moveTo(e.r*1.12,0);ctx.lineTo(-e.r*.55,-e.r*.82);ctx.lineTo(-e.r*.82,-e.r*.3);ctx.lineTo(-e.r*.82,e.r*.3);ctx.lineTo(-e.r*.55,e.r*.82);ctx.closePath();ctx.fill();ctx.stroke();for(const y of [-1,1]){ctx.beginPath();ctx.moveTo(-e.r*.35,y*e.r*.62);ctx.lineTo(-e.r*1.16,y*e.r*.9);ctx.lineTo(-e.r*.74,y*e.r*.18);ctx.stroke()}ctx.fillStyle=e.color;ctx.fillRect(e.r*.2,-4,e.r*.72,8)}
      else if(e.type==='turret'){ctx.rotate(Math.PI/4);ctx.fillRect(-e.r*.8,-e.r*.8,e.r*1.6,e.r*1.6);ctx.strokeRect(-e.r*.8,-e.r*.8,e.r*1.6,e.r*1.6);ctx.rotate(-Math.PI/4);for(let i=0;i<4;i++){ctx.rotate(Math.PI/2);ctx.fillStyle='#121b25';ctx.fillRect(e.r*.48,-4,e.r*.86,8);ctx.strokeRect(e.r*.48,-4,e.r*.86,8)}ctx.fillStyle=e.color;drawPolygon(6,e.r*.38,Math.PI/6);ctx.fill()}
      else{for(const y of [-1,1]){ctx.fillStyle='rgba(35,247,255,.12)';ctx.beginPath();ctx.moveTo(-5,y*4);ctx.lineTo(-e.r*1.25,y*e.r*.78);ctx.lineTo(-e.r*.5,y*2);ctx.closePath();ctx.fill();ctx.stroke()}ctx.fillStyle='#07121a';drawPolygon(4,e.r*.82,0);ctx.fill();ctx.stroke();ctx.fillStyle=e.color;ctx.beginPath();ctx.arc(4,0,4,0,TAU);ctx.fill()}
      if(e.elite){ctx.rotate(-state.time*.9);ctx.setLineDash([5,5]);ctx.strokeStyle=COL.yellow;ctx.beginPath();ctx.arc(0,0,e.r+9,0,TAU);ctx.stroke();ctx.setLineDash([])}ctx.restore();drawEnemyHp(e)}}
  function drawEnemyHp(e){if(e.hp>=e.maxHp)return;ctx.fillStyle='#1c1420';ctx.fillRect(e.x-e.r,e.y-e.r-11,e.r*2,3);ctx.fillStyle=e.color;ctx.fillRect(e.x-e.r,e.y-e.r-11,e.r*2*clamp(e.hp/e.maxHp,0,1),3)}
  function drawBoss(b){ctx.save();ctx.globalAlpha=.38;ctx.fillStyle='#000';ctx.beginPath();ctx.ellipse(b.x,b.y+b.r*.72,b.r*1.25,b.r*.45,0,0,TAU);ctx.fill();ctx.restore();if(drawBossAtlas(b))return;ctx.save();ctx.translate(b.x,b.y);ctx.shadowBlur=32;ctx.shadowColor=b.color;ctx.strokeStyle=b.color;ctx.fillStyle='#070812';ctx.lineWidth=3.5;const pulse=1+Math.sin(state.time*3+b.phase)*.035;ctx.scale(pulse,pulse);
    if(b.bossId==='cherub'){ctx.save();ctx.rotate(-b.angle*.35);ctx.setLineDash([15,8]);ctx.lineWidth=5;ctx.strokeStyle=b.bossTier>1?COL.red:COL.yellow;ctx.beginPath();ctx.arc(0,0,b.r*(1.18+b.phase*.12),-.2,Math.PI*1.35);ctx.stroke();ctx.setLineDash([]);ctx.restore();const wings=4+b.phase*2;for(let i=0;i<wings;i++){const a=i/wings*TAU+b.angle*.18;ctx.save();ctx.rotate(a);ctx.fillStyle=i%2?'rgba(35,247,255,.1)':'rgba(255,227,76,.09)';ctx.beginPath();ctx.moveTo(b.r*.35,-8);ctx.quadraticCurveTo(b.r*1.35,-24-b.phase*3,b.r*1.7,0);ctx.quadraticCurveTo(b.r*1.1,9,b.r*.35,8);ctx.closePath();ctx.fill();ctx.stroke();ctx.restore()}ctx.fillStyle='#0b1119';ctx.beginPath();ctx.ellipse(0,0,b.r*.62,b.r*.78,0,0,TAU);ctx.fill();ctx.stroke();for(const y of [-1,1]){ctx.fillStyle=b.color;ctx.beginPath();ctx.ellipse(8,y*17,10,5,0,0,TAU);ctx.fill()}}
    else if(b.bossId==='matriarch'){for(let i=0;i<3+b.phase;i++){const a=i/(3+b.phase)*TAU+b.angle*.18;ctx.save();ctx.rotate(a);ctx.strokeStyle=i%2?COL.violet:b.color;ctx.lineWidth=7;ctx.beginPath();ctx.arc(b.r*.28,0,b.r*(.72+b.phase*.08),-.7,.7);ctx.stroke();ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(b.r*.78,0);ctx.quadraticCurveTo(b.r*1.25,18,b.r*1.55,0);ctx.stroke();ctx.restore()}ctx.fillStyle='#160a1d';ctx.beginPath();ctx.ellipse(0,0,b.r*.72,b.r*.88,0,0,TAU);ctx.fill();ctx.stroke();ctx.fillStyle='#02040a';ctx.beginPath();ctx.ellipse(8,0,b.r*.38,b.r*.5,0,0,TAU);ctx.fill();ctx.strokeStyle=COL.yellow;ctx.stroke();ctx.fillStyle=b.color;ctx.beginPath();ctx.ellipse(14,0,8,20,0,0,TAU);ctx.fill();ctx.fillStyle='#fff';ctx.beginPath();ctx.arc(16,0,4,0,TAU);ctx.fill()}
    else{const lobes=6;for(let i=0;i<lobes;i++){const a=i/lobes*TAU+b.angle*.14;ctx.save();ctx.rotate(a);ctx.fillStyle=i%2?'#140715':'#08121a';ctx.beginPath();ctx.moveTo(b.r*.18,-b.r*.25);ctx.lineTo(b.r*(1.1+b.phase*.12),-b.r*.42);ctx.lineTo(b.r*(1.42+b.phase*.1),0);ctx.lineTo(b.r*(1.1+b.phase*.12),b.r*.42);ctx.lineTo(b.r*.18,b.r*.25);ctx.closePath();ctx.fill();ctx.stroke();ctx.fillStyle=i%2?COL.pink:b.color;ctx.beginPath();ctx.arc(b.r*(.84+b.phase*.08),0,6+b.phase*2,0,TAU);ctx.fill();ctx.restore()}ctx.fillStyle='#080912';drawPolygon(6,b.r*.68,b.angle*.2);ctx.fill();ctx.stroke();ctx.rotate(-b.angle*.7);ctx.strokeStyle=COL.yellow;ctx.lineWidth=3;drawPolygon(6,b.r*.4,Math.PI/6);ctx.stroke();ctx.fillStyle=COL.ink;ctx.font=`900 ${18+b.phase*4}px Consolas`;ctx.textAlign='center';ctx.textBaseline='middle';ctx.fillText(['I','II','III'][b.phase],0,1)}
    if(b.phase>0){ctx.rotate(state.time*(b.phase===2?1.2:.7));ctx.setLineDash([4,7]);ctx.strokeStyle=b.bossTier>1?COL.red:COL.pink;ctx.lineWidth=2;ctx.beginPath();ctx.arc(0,0,b.r*(1.5+b.phase*.12),0,TAU);ctx.stroke();ctx.setLineDash([])}if(b.warning>0){ctx.globalAlpha=.5+.35*Math.sin(state.time*30);ctx.strokeStyle=COL.red;ctx.lineWidth=5;ctx.beginPath();ctx.arc(0,0,b.r*1.72,0,TAU);ctx.stroke()}ctx.restore()}
  function drawHazards(){for(const h of state.hazards){const p=1-h.life/h.max;ctx.save();ctx.strokeStyle=h.color;ctx.fillStyle=h.color;ctx.shadowBlur=12;ctx.shadowColor=h.color;if(h.kind==='circle'){ctx.globalAlpha=.18+p*.35;ctx.lineWidth=3+p*4;ctx.setLineDash([8,6]);ctx.beginPath();ctx.arc(h.x,h.y,h.r,0,TAU);ctx.stroke();ctx.globalAlpha=.04+p*.1;ctx.fill()}else{ctx.translate(h.x,h.y);ctx.rotate(h.angle);ctx.globalAlpha=h.kind==='laser'?(h.fired?.7:.12+p*.35):.22+p*.25;ctx.fillRect(-h.length/2,-h.width/2,h.length,h.width);ctx.globalAlpha=.8;ctx.strokeRect(-h.length/2,-h.width/2,h.length,h.width)}ctx.restore()}}
  function drawPortal(){if(!state.portal)return;const p=state.portal,phase=p.phase||0;ctx.save();ctx.translate(p.x,p.y);ctx.rotate(phase);ctx.strokeStyle=COL.cyan;ctx.shadowBlur=22;ctx.shadowColor=COL.cyan;for(let i=0;i<3;i++){ctx.rotate(TAU/3);ctx.strokeRect(-p.r*.6,-p.r*.6,p.r*1.2,p.r*1.2)}ctx.fillStyle='rgba(35,247,255,.12)';ctx.beginPath();ctx.arc(0,0,p.r*.65,0,TAU);ctx.fill();ctx.restore()}
  function drawMuzzleFx(p,simple=false){const k=clamp(p.life/p.max,0,1),q=1-k;ctx.save();ctx.translate(p.x,p.y);ctx.rotate(p.angle);ctx.globalAlpha=k;ctx.globalCompositeOperation=simple?'source-over':'lighter';ctx.shadowBlur=simple?0:24;ctx.shadowColor=p.color;ctx.strokeStyle=p.color;ctx.fillStyle=p.color;if(p.form==='wraith'){ctx.lineWidth=4;for(const y of [-8,8]){ctx.beginPath();ctx.moveTo(-12-q*10,y);ctx.lineTo(24+q*20,y);ctx.stroke()}ctx.rotate(Math.PI/4);ctx.strokeRect(-10-q*5,-10-q*5,20+q*10,20+q*10)}else if(p.form==='beast'){ctx.fillStyle=COL.red;ctx.beginPath();ctx.moveTo(34+q*30,0);ctx.lineTo(-8,22+q*12);ctx.lineTo(3,0);ctx.lineTo(-8,-22-q*12);ctx.closePath();ctx.fill();ctx.fillStyle=COL.yellow;ctx.beginPath();ctx.moveTo(25+q*20,0);ctx.lineTo(-2,9);ctx.lineTo(-2,-9);ctx.closePath();ctx.fill()}else if(p.form==='seraph'){ctx.lineWidth=4;for(const r of [13+q*18,23+q*24]){ctx.beginPath();ctx.arc(0,0,r,-1.05,1.05);ctx.stroke()}ctx.strokeStyle='#fffbd1';ctx.lineWidth=5;ctx.beginPath();ctx.moveTo(-6,0);ctx.lineTo(46+q*35,0);ctx.stroke()}else{ctx.rotate(Math.PI/4);ctx.lineWidth=3;for(const s of [18+q*14,30+q*18])ctx.strokeRect(-s/2,-s/2,s,s);ctx.rotate(-Math.PI/4);ctx.beginPath();ctx.moveTo(-6,0);ctx.lineTo(35+q*22,0);ctx.stroke()}ctx.restore()}
  function drawImpactFx(p,simple=false){const k=clamp(p.life/p.max,0,1),q=1-k,r=10+q*(p.form==='beast'?34:26);ctx.save();ctx.translate(p.x,p.y);ctx.rotate(p.angle||0);ctx.globalAlpha=k;ctx.globalCompositeOperation=simple?'source-over':'lighter';ctx.shadowBlur=simple?0:p.crit?30:20;ctx.shadowColor=p.color;ctx.strokeStyle=p.crit?COL.yellow:p.color;ctx.fillStyle=p.color;if(p.form==='wraith'){ctx.lineWidth=4;ctx.beginPath();ctx.moveTo(-r,-r);ctx.lineTo(r,r);ctx.moveTo(r,-r);ctx.lineTo(-r,r);ctx.stroke()}else if(p.form==='beast'){ctx.lineWidth=6;ctx.beginPath();ctx.arc(0,0,r,0,TAU);ctx.stroke();ctx.fillStyle=COL.yellow;for(let i=0;i<4;i++){ctx.rotate(Math.PI/2);ctx.beginPath();ctx.moveTo(r*.65,-4);ctx.lineTo(r+14,0);ctx.lineTo(r*.65,4);ctx.fill()}}else if(p.form==='seraph'){ctx.lineWidth=3;ctx.beginPath();ctx.arc(0,0,r,0,TAU);ctx.stroke();ctx.strokeStyle='#fffbd1';ctx.lineWidth=4;ctx.beginPath();ctx.moveTo(-r-10,0);ctx.lineTo(r+10,0);ctx.moveTo(0,-r-10);ctx.lineTo(0,r+10);ctx.stroke()}else{ctx.lineWidth=3;ctx.beginPath();ctx.arc(0,0,r,0,TAU);ctx.stroke();ctx.beginPath();ctx.moveTo(-r-8,0);ctx.lineTo(-r*.35,0);ctx.moveTo(r*.35,0);ctx.lineTo(r+8,0);ctx.moveTo(0,-r-8);ctx.lineTo(0,-r*.35);ctx.moveTo(0,r*.35);ctx.lineTo(0,r+8);ctx.stroke()}ctx.restore()}
  function drawRicochetFx(p,simple=false){const k=clamp(p.life/p.max,0,1),q=1-k,r=8+q*24;ctx.save();ctx.translate(p.x,p.y);ctx.rotate(Math.atan2(p.ny,p.nx));ctx.globalAlpha=k;ctx.globalCompositeOperation=simple?'source-over':'lighter';ctx.shadowBlur=simple?0:22;ctx.shadowColor=p.color;ctx.strokeStyle=p.color;ctx.lineWidth=p.blocked?4:3;ctx.beginPath();ctx.arc(0,0,r,-1.05,1.05);ctx.stroke();ctx.strokeStyle=p.blocked?COL.red:'#fff';ctx.beginPath();ctx.moveTo(-2,-r*.75);ctx.lineTo(p.blocked?-14:-9,0);ctx.lineTo(-2,r*.75);ctx.stroke();if(!p.blocked){ctx.strokeStyle=COL.yellow;ctx.lineWidth=2;ctx.beginPath();ctx.moveTo(2,-r*.45);ctx.lineTo(12+q*8,0);ctx.lineTo(2,r*.45);ctx.stroke()}ctx.restore()}
  function drawParticles(){const simpleFx=fxPressure()>64;for(const p of state.particles){ctx.globalAlpha=clamp(p.life/p.max,0,1);if(p.kind==='text'){const label=p.crit?`✦ ${p.text}`:p.text,y=p.y-(1-p.life/p.max)*(p.crit?42:30);ctx.font=`900 ${p.crit?30:22}px Consolas`;ctx.textAlign='center';ctx.lineJoin='round';ctx.shadowBlur=simpleFx?0:p.crit?28:12;ctx.shadowColor=p.color;ctx.lineWidth=p.crit?8:6;ctx.strokeStyle='rgba(2,4,10,.94)';ctx.strokeText(label,p.x,y);ctx.lineWidth=p.crit?2.5:1.7;ctx.strokeStyle=p.color;ctx.strokeText(label,p.x,y);ctx.fillStyle=p.crit?'#fffbd1':p.color;ctx.fillText(label,p.x,y);ctx.shadowBlur=0}else if(p.kind==='beam'){ctx.strokeStyle=p.color;ctx.lineWidth=3;ctx.beginPath();ctx.moveTo(p.x,p.y);ctx.lineTo(p.x2,p.y2);ctx.stroke()}else if(p.kind==='muzzle')drawMuzzleFx(p,simpleFx);else if(p.kind==='impact')drawImpactFx(p,simpleFx);else if(p.kind==='ricochet')drawRicochetFx(p,simpleFx);else{ctx.fillStyle=p.color;ctx.fillRect(p.x,p.y,p.size,p.size)}}ctx.globalAlpha=1}
  function drawPost(){if(state.flash>0){const a=clamp(state.flash/.12,0,1),hit=ctx.createRadialGradient(W/2,H/2,Math.min(W,H)*.3,W/2,H/2,Math.max(W,H)*.68);hit.addColorStop(0,'rgba(255,44,146,0)');hit.addColorStop(.72,`rgba(255,44,146,${a*.025})`);hit.addColorStop(1,`rgba(255,44,146,${a*.5})`);ctx.fillStyle=hit;ctx.fillRect(0,0,W,H);ctx.strokeStyle=`rgba(255,96,164,${a*.8})`;ctx.lineWidth=8;ctx.strokeRect(4,4,W-8,H-8)}ctx.drawImage(vignetteLayer,0,0);if(state.transition>0){const p=1-state.transition/.38;ctx.fillStyle=`rgba(1,3,10,${clamp((1-p)*.72,0,.72)})`;ctx.fillRect(0,0,W,H);ctx.fillStyle=`rgba(35,247,255,${clamp((1-p)*.45,0,.45)})`;ctx.fillRect(W*p-14,0,28,H)}ctx.fillStyle='rgba(35,247,255,.45)';ctx.font='8px Consolas';ctx.fillText(`FRAME ${Math.floor(state.time*60).toString().padStart(6,'0')} // BUILD 1.0`,18,704)}
  function burst(x,y,color,count=10,speed=150){const pressure=fxPressure(),limit=pressure>100?260:360;if(pressure>110)count=Math.ceil(count*.45);else if(pressure>75)count=Math.ceil(count*.7);count=Math.min(count,Math.max(0,limit-state.particles.length));for(let i=0;i<count;i++){const a=rand(0,TAU),s=rand(speed*.25,speed);state.particles.push({x,y,vx:Math.cos(a)*s,vy:Math.sin(a)*s,size:rand(1,4),color,life:rand(.18,.55),max:.55})}}
  function floatText(x,y,text,color,crit=false){if(state.particles.length>590){let index=state.particles.findIndex(p=>p.kind!=='text');if(index<0&&crit)index=state.particles.findIndex(p=>p.kind==='text'&&!p.crit);if(index>=0)state.particles.splice(index,1);else if(!crit)return}const life=crit?1.05:.85;state.particles.push({kind:'text',x,y,text:Math.round(Number(text)),color,crit,life,max:life})}
  function updateParticles(dt){for(let i=state.particles.length-1;i>=0;i--){const p=state.particles[i];p.life-=dt;if(p.vx!==undefined){p.x+=p.vx*dt;p.y+=p.vy*dt;p.vx*=Math.pow(.05,dt);p.vy*=Math.pow(.05,dt)}if(p.life<=0)state.particles.splice(i,1)}}

  function cacheText(key,element,value){value=String(value);if(uiCache[key]===value)return;uiCache[key]=value;element.textContent=value}
  function updateUI(){
    cacheText('floor',ui.floor,`SECTOR ${roman(state.floor)} // PWR ×${FLOOR_POWER[state.floor-1]}`);cacheText('room',ui.room,state.room?ROOM_NAMES[state.room.type]:'');cacheText('coins',ui.coins,String(player.coins).padStart(2,'0'));cacheText('keys',ui.keys,String(player.keys).padStart(2,'0'));cacheText('bombs',ui.bombs,String(player.bombs).padStart(2,'0'));cacheText('shieldValue',ui.shieldValue,player.shield);const shieldWidth=`${player.maxShield?player.shield/player.maxShield*100:0}%`;if(uiCache.shieldWidth!==shieldWidth){uiCache.shieldWidth=shieldWidth;ui.shieldFill.style.width=shieldWidth}
    const maxCells=Math.max(1,Math.round(player.maxHp)),chargedCells=clamp(Math.round(player.hp),0,maxCells),lowPower=chargedCells>0&&chargedCells/maxCells<=.34;if(uiCache.healthMax!==maxCells){uiCache.healthMax=maxCells;uiCache.healthCells=-1;ui.hearts.innerHTML='';const fragment=document.createDocumentFragment();for(let i=0;i<maxCells;i++){const cell=document.createElement('i');cell.className='heart battery-cell';cell.setAttribute('aria-hidden','true');fragment.appendChild(cell)}ui.hearts.appendChild(fragment)}if(uiCache.healthCells!==chargedCells){uiCache.healthCells=chargedCells;for(let i=0;i<maxCells;i++){const cell=ui.hearts.children[i],charged=i<chargedCells;cell.classList.toggle('charged',charged);cell.classList.toggle('empty',!charged)}ui.hearts.classList.toggle('low',lowPower);ui.hearts.classList.toggle('depleted',chargedCells===0);ui.hearts.setAttribute('aria-valuemax',maxCells);ui.hearts.setAttribute('aria-valuenow',chargedCells);ui.hearts.setAttribute('aria-label',`核心电池：${chargedCells} / ${maxCells} 格`);if(ui.healthValue)cacheText('healthValue',ui.healthValue,`${String(chargedCells).padStart(2,'0')} / ${String(maxCells).padStart(2,'0')}`);if(ui.healthStatus)cacheText('healthStatus',ui.healthStatus,chargedCells===0?'CORE OFFLINE':lowPower?'LOW POWER':'CORE CELLS')}cacheText('activeCharge',ui.activeCharge,Array.from({length:player.activeMax},(_,i)=>i<player.activeCharge?'●':'○').join(' '));
    renderMap();renderRelicArray();const boss=state.enemies.find(e=>e.boss);if(boss){cacheText('bossName',ui.bossName,boss.name);cacheText('bossPhase',ui.bossPhase,`FORM 0${boss.phase+1}`);const bossWidth=`${clamp(boss.hp/boss.maxHp,0,1)*100}%`;if(uiCache.bossWidth!==bossWidth){uiCache.bossWidth=bossWidth;ui.bossFill.style.width=bossWidth}}
  }
  function renderRelicArray(){const itemLookup=new Map([...ITEMS,...TUNING,...ASCENSIONS].map(item=>[item.id,item])),entries=[...player.items.map(id=>({item:itemLookup.get(id),level:1})),...Object.entries(player.upgrades).filter(([,level])=>level>0).map(([id,level])=>({item:itemLookup.get(id),level}))].filter(entry=>entry.item),signature=entries.map(({item,level})=>`${item.id}:${level}`).join('|');if(uiCache.relicArray===signature)return;uiCache.relicArray=signature;ui.relicSlots.innerHTML='';ui.relicArray.classList.toggle('empty',!entries.length);cacheText('relicCount',ui.relicCount,String(entries.length).padStart(2,'0'));const fragment=document.createDocumentFragment();for(const {item,level} of entries.slice(-18)){const el=document.createElement('i');el.style.setProperty('--relic-color',relicColor(item));el.dataset.family=item.family||item.category||'skill';el.textContent=item.glyph;el.title=`${item.name}${level>1?` LV.${level}`:''}\n${item.rarityLabel||'COMMON'} // ${item.familyLabel||'RELIC'}\n${item.desc}\n${item.synergy||''}`;if(level>1)el.dataset.level=level;fragment.appendChild(el)}ui.relicSlots.appendChild(fragment)}
  function renderMap(){if(!state.room)return;const visible=state.rooms.filter(r=>r.revealed&&(r.visited||state.rooms.some(v=>v.visited&&Math.abs(v.x-r.x)+Math.abs(v.y-r.y)===1))),signature=`${state.floor}|${state.room.key}|${visible.map(r=>`${r.key}:${r.type}:${r.visited?1:0}`).join('|')}`;if(uiCache.map===signature)return;uiCache.map=signature;ui.map.innerHTML='';const minX=Math.min(...visible.map(r=>r.x)),maxX=Math.max(...visible.map(r=>r.x)),minY=Math.min(...visible.map(r=>r.y)),maxY=Math.max(...visible.map(r=>r.y)),cx=(minX+maxX)/2,cy=(minY+maxY)/2;for(const r of visible){const el=document.createElement('i');el.className=`map-room ${r.visited?'visited':''} ${r===state.room?'current':''} ${r.type}`;el.style.left=`${125+(r.x-cx)*25}px`;el.style.top=`${29+(r.y-cy)*19}px`;el.title=ROOM_NAMES[r.type];ui.map.appendChild(el)}}
  function showToast(text){ui.toast.textContent=text;ui.toast.classList.add('show');clearTimeout(toastTimer);toastTimer=setTimeout(()=>ui.toast.classList.remove('show'),1350)}
  function endGame(win){state.mode='gameover';state.victory=win;stopMusic();ui.gameover.classList.add('visible');$('#result-kicker').textContent=win?'ORIGIN PROCESS TERMINATED':'CONNECTION TERMINATED';$('#result-title').textContent=win?'原罪已净化':'意识离线';$('#result-stats').innerHTML=`SECTOR ${state.floor} // 净化房间 ${state.roomsCleared}<br>击杀 ${state.kills} // 遗物 ${player.items.length}<br>承受伤害 ${player.damageTaken}`;beep(win?620:75,.7,win?'sine':'sawtooth',.08)}
  function pause(){if(state.mode==='playing'){state.mode='paused';ui.pause.classList.add('visible')}else if(state.mode==='paused'){state.mode='playing';ui.pause.classList.remove('visible')}}
  function roman(n){return['I','II','III','IV','V','VI'][n-1]||n}
  function buildShotBuffers(a){
    const build=(form,dur)=>{const rate=a.sampleRate,length=Math.ceil(rate*dur),buffer=a.createBuffer(1,length,rate),data=buffer.getChannelData(0);for(let i=0;i<length;i++){const t=i/rate,u=t/dur,attack=Math.min(1,t*900),fade=Math.pow(Math.max(0,1-u),form==='seraph'?1.15:1.8),phase=(f0,f1)=>TAU*(f0*t+(f1-f0)*t*t/(2*dur));let wave=0,level=.24;if(form==='vanguard'){wave=Math.sin(phase(410,190))*.78+Math.sin(phase(820,380))*.22;level=.31}else if(form==='wraith'){const pulse=n=>n<0?0:Math.exp(-n*48)*(Math.sin(TAU*760*n)+Math.sin(TAU*1140*n)*.34);wave=pulse(t)+pulse(t-.022)*.72;level=.27}else if(form==='beast'){const p=phase(150,62)/TAU,saw=2*(p-Math.floor(p+.5)),noise=Math.random()*2-1;wave=saw*.74+Math.sin(phase(92,48))*.38+noise*.2;level=.34}else{wave=Math.sin(phase(520,1120))*.72+Math.sin(phase(1040,2240))*.26;level=.3}data[i]=clamp(wave*attack*fade*level,-.82,.82)}return buffer};shotBuffers={vanguard:build('vanguard',.082),wraith:build('wraith',.064),beast:build('beast',.12),seraph:build('seraph',.145)}
  }
  function playShotSound(form){try{const a=ensureAudio();if(!a||a.state!=='running'||!sfxGain)return;if(!shotBuffers)buildShotBuffers(a);const now=a.currentTime,minGap={vanguard:.055,wraith:.04,beast:.075,seraph:.085}[form]||.05;if(now-lastShotSoundTime<minGap||shotSfxVoices>=5)return;const source=a.createBufferSource();source.buffer=shotBuffers[form]||shotBuffers.vanguard;source.playbackRate.value=1+rand(-.012,.012);source.connect(sfxGain);shotSfxVoices++;shotSfxPlayed++;lastShotSoundTime=now;source.onended=()=>{shotSfxVoices=Math.max(0,shotSfxVoices-1);source.disconnect()};source.start(now)}catch{}}
  function requestAudioResume(a=audio){if(!a||a.state==='closed')return Promise.resolve(false);if(a.state==='running')return Promise.resolve(true);if(audioResumePromise)return audioResumePromise;audioResumeRequests++;audioResumePromise=a.resume().then(()=>a.state==='running').catch(()=>false).finally(()=>{if(audio===a)audioResumePromise=null;updateMusicButton()});return audioResumePromise}
  function ensureAudio(){
    try{
      if(!audio){
        const AudioEngine=window.AudioContext||window.webkitAudioContext;if(!AudioEngine){audioStalled=true;return null}audio=new AudioEngine();musicGain=audio.createGain();musicLimiter=audio.createDynamicsCompressor();musicAnalyser=audio.createAnalyser();sfxGain=audio.createGain();
        musicGain.gain.value=MUSIC_LEVEL;musicLimiter.threshold.value=-18;musicLimiter.knee.value=16;musicLimiter.ratio.value=5;musicLimiter.attack.value=.003;musicLimiter.release.value=.18;musicAnalyser.fftSize=256;sfxGain.gain.value=.9;
        musicGain.connect(musicLimiter).connect(musicAnalyser).connect(audio.destination);sfxGain.connect(audio.destination);audio.onstatechange=updateMusicButton;
        noiseBuffer=audio.createBuffer(1,Math.ceil(audio.sampleRate*.08),audio.sampleRate);const noise=noiseBuffer.getChannelData(0);for(let i=0;i<noise.length;i++)noise[i]=Math.random()*2-1;buildShotBuffers(audio);
      }
      if(audio.state!=='running'&&audio.state!=='closed')requestAudioResume(audio);return audio
    }catch{audioStalled=true;updateMusicButton();return null}
  }
  function tone(freq,dur,type='triangle',vol=.03,delay=0){const a=ensureAudio();if(!a||a.state!=='running'||!musicGain)return;const o=a.createOscillator(),g=a.createGain(),start=a.currentTime+delay;o.type=type;o.frequency.setValueAtTime(freq,start);g.gain.setValueAtTime(.0001,start);g.gain.exponentialRampToValueAtTime(Math.max(.0002,vol),start+.012);g.gain.exponentialRampToValueAtTime(.0001,start+dur);o.connect(g).connect(musicGain);o.start(start);o.stop(start+dur+.02);musicNotes++;o.onended=()=>{o.disconnect();g.disconnect()}}
  function drum(freq=125,dur=.15,vol=.11){const a=ensureAudio();if(!a||a.state!=='running'||!musicGain)return;const o=a.createOscillator(),g=a.createGain(),now=a.currentTime;o.type='sine';o.frequency.setValueAtTime(freq,now);o.frequency.exponentialRampToValueAtTime(48,now+dur);g.gain.setValueAtTime(vol,now);g.gain.exponentialRampToValueAtTime(.0001,now+dur);o.connect(g).connect(musicGain);o.start(now);o.stop(now+dur+.01);musicNotes++;tone(720,.035,'square',vol*.18);o.onended=()=>{o.disconnect();g.disconnect()}}
  function hat(vol=.022){const a=ensureAudio();if(!a||a.state!=='running'||!musicGain||!noiseBuffer)return;const source=a.createBufferSource(),filter=a.createBiquadFilter(),g=a.createGain(),now=a.currentTime;source.buffer=noiseBuffer;filter.type='highpass';filter.frequency.value=1700;g.gain.setValueAtTime(vol,now);g.gain.exponentialRampToValueAtTime(.0001,now+.055);source.connect(filter).connect(g).connect(musicGain);source.start(now);source.stop(now+.06);musicNotes++;source.onended=()=>{source.disconnect();filter.disconnect();g.disconnect()}}
  function musicTick(){if(!musicEnabled||!['story','playing','bossintro'].includes(state.mode))return;musicTicks++;const step=musicStep++%8,roots=[82.41,92.5,98],root=roots[clamp(state.floor-1,0,2)],pattern=[0,7,10,12,10,7,3,7],boss=state.enemies.some(e=>e.boss)||state.mode==='bossintro';tone(root*Math.pow(2,pattern[step]/12)*4,.17,boss?'sawtooth':'square',boss ? .048 : .036);if(step%2===0)tone(root*(step===6?2.25:1.5),.3,'triangle',boss ? .078 : .06);else hat(boss ? .032 : .023);if(step===0||step===4)drum(boss?145:125,boss ? .18 : .15,boss ? .14 : .11);if(step===2||step===6)tone(boss?620:520,.07,'square',boss ? .032 : .024);if(boss&&step%2)tone(root*8*Math.pow(2,pattern[(step+2)%8]/12),.1,'square',.024)}
  function startMusic(){if(!musicEnabled)return;const a=ensureAudio();if(!a){updateMusicButton();return}const arm=()=>{if(!musicEnabled||a.state!=='running'){updateMusicButton();return}audioStalled=false;musicGain.gain.cancelScheduledValues(a.currentTime);musicGain.gain.value=MUSIC_LEVEL;if(!musicTimer){musicStep=0;musicTick();musicTimer=setInterval(musicTick,60000/112/2)}clearTimeout(audioHeartbeatTimer);if(!location.search.includes('test=1')){const heartbeat=a.currentTime;audioHeartbeatTimer=setTimeout(()=>{if(audio===a&&musicEnabled&&a.state==='running'&&a.currentTime-heartbeat<.05){audioStalled=true;if(musicTimer){clearInterval(musicTimer);musicTimer=null}updateMusicButton()}},700)}updateMusicButton()};if(a.state==='running')arm();else requestAudioResume(a).then(ok=>ok?arm():updateMusicButton())}
  function stopMusic(){clearTimeout(audioHeartbeatTimer);if(musicTimer){clearInterval(musicTimer);musicTimer=null}if(audio&&musicGain){musicGain.gain.cancelScheduledValues(audio.currentTime);musicGain.gain.value=.0001}}
  function resetAudioEngine(){clearTimeout(audioHeartbeatTimer);if(musicTimer){clearInterval(musicTimer);musicTimer=null}const previous=audio;try{musicGain?.disconnect();musicLimiter?.disconnect();musicAnalyser?.disconnect();sfxGain?.disconnect()}catch{}audio=null;musicGain=null;musicLimiter=null;musicAnalyser=null;sfxGain=null;noiseBuffer=null;shotBuffers=null;shotSfxVoices=0;lastShotSoundTime=-1;audioResumePromise=null;audioStalled=false;if(previous&&previous.state!=='closed')previous.close().catch(()=>{})}
  function updateMusicButton(){const blocked=musicEnabled&&(audioStalled||audio&&audio.state!=='running');ui.music.textContent=!musicEnabled?'♪ OFF':blocked?'♪ RETRY':'♪ 100%';ui.music.classList.toggle('off',!musicEnabled||blocked);ui.music.setAttribute('aria-pressed',String(musicEnabled&&!blocked));ui.music.setAttribute('aria-label',blocked?'背景音乐未启动，点击重试':musicEnabled?'背景音乐 100%，点击关闭':'背景音乐已关闭，点击开启')}
  function toggleMusic(){if(musicEnabled&&audio&&(audio.state!=='running'||audioStalled)){if(audioStalled)resetAudioEngine();startMusic();showToast('正在重新连接音频输出');return}musicEnabled=!musicEnabled;if(musicEnabled)startMusic();else stopMusic();updateMusicButton()}
  function beep(freq=300,dur=.05,type='sine',vol=.02){try{const a=ensureAudio();if(!a||a.state!=='running'||!sfxGain)return;const o=a.createOscillator(),g=a.createGain();o.type=type;o.frequency.value=freq;g.gain.setValueAtTime(vol,a.currentTime);g.gain.exponentialRampToValueAtTime(.0001,a.currentTime+dur);o.connect(g).connect(sfxGain);o.start();o.stop(a.currentTime+dur);o.onended=()=>{o.disconnect();g.disconnect()}}catch{}}
  function haptic(pattern){try{navigator.vibrate?.(pattern)}catch{}}
  function fullscreenElement(){return document.fullscreenElement||document.webkitFullscreenElement||null}
  function isStandalone(){return !!navigator.standalone||matchMedia('(display-mode: fullscreen)').matches||matchMedia('(display-mode: standalone)').matches}
  function syncViewport(){const viewport=window.visualViewport,height=Math.max(180,Math.round(viewport?.height||innerHeight)),width=Math.max(320,Math.round(viewport?.width||innerWidth));document.documentElement.style.setProperty('--app-height',`${height}px`);document.documentElement.style.setProperty('--app-width',`${width}px`);document.documentElement.dataset.orientation=width>=height?'landscape':'portrait'}
  function syncFullscreenUI(){const native=!!fullscreenElement(),standalone=isStandalone(),active=native||standalone;fullscreenButton.classList.toggle('active',active);fullscreenButton.setAttribute('aria-pressed',String(active));fullscreenButton.setAttribute('aria-label',native?'退出全屏':standalone?'当前为全屏应用':'进入全屏');fullscreenLabel.textContent=native?'退出':standalone?'已全屏':'全屏';rotateFullscreenButton.textContent=active?'已进入全屏，请旋转设备':'全屏并尝试横屏';if(active)rotateStatus.textContent='全屏已连接；若画面仍为竖向，请手动旋转设备。';syncViewport()}
  function fullscreenHint(message){rotateStatus.textContent=message;showToast(message)}
  async function requestLandscape(){try{if(screen.orientation?.lock)await screen.orientation.lock('landscape')}catch{}}
  async function enterFullscreen(){const root=document.documentElement,request=root.requestFullscreen||root.webkitRequestFullscreen;if(!request){fullscreenHint('当前浏览器不支持网页全屏；iPhone 可用“分享 → 添加到主屏幕”。');return false}try{await request.call(root);await requestLandscape();syncFullscreenUI();haptic(18);return true}catch{fullscreenHint('全屏被浏览器拦截，请再次点击或使用浏览器菜单进入全屏。');syncFullscreenUI();return false}}
  async function exitFullscreen(){const exit=document.exitFullscreen||document.webkitExitFullscreen;if(!exit){fullscreenHint('当前全屏模式由浏览器控制，请使用系统返回手势退出。');return false}try{await exit.call(document);syncFullscreenUI();return true}catch{return false}}
  async function toggleFullscreen(){if(fullscreenElement())await exitFullscreen();else if(isStandalone()){await requestLandscape();fullscreenHint('当前为主屏幕全屏模式；请旋转设备，退出可使用系统返回手势。')}else await enterFullscreen()}

  const FORM_KEYS={ArrowUp:'seraph',ArrowRight:'wraith',ArrowDown:'beast',ArrowLeft:'vanguard',Digit1:'vanguard',Digit2:'wraith',Digit3:'beast',Digit4:'seraph',Numpad1:'vanguard',Numpad2:'wraith',Numpad3:'beast',Numpad4:'seraph'};
  addEventListener('keydown',e=>{if(['ArrowUp','ArrowDown','ArrowLeft','ArrowRight','Space'].includes(e.code))e.preventDefault();const interactive=e.target?.closest?.('button,a,input,select,textarea');if(e.code==='Enter'&&interactive)return;if(e.code==='Enter'&&state.mode==='menu'){beginStory();return}if(e.code==='Enter'&&state.mode==='story'){advanceStory();return}if(e.code==='KeyM'&&!e.repeat){toggleMusic();return}if(e.code==='Escape')pause();if((e.shiftKey&&FORM_KEYS[e.code])||((e.code.startsWith('Digit')||e.code.startsWith('Numpad'))&&FORM_KEYS[e.code])){switchForm(FORM_KEYS[e.code]);return}if(e.code==='KeyE'&&!e.repeat)plantBomb();if(e.code==='Space'&&!e.repeat)useActive();input.keys.add(e.code)});
  addEventListener('keyup',e=>input.keys.delete(e.code));addEventListener('blur',()=>input.keys.clear());
  $('#start-btn').onclick=beginStory;$('#story-next').onclick=advanceStory;$('#story-skip').onclick=resetRun;$('#restart-btn').onclick=resetRun;$('#pause-btn').onclick=pause;$('#resume-btn').onclick=pause;ui.music.onclick=toggleMusic;$$('#forms button').forEach(b=>b.onclick=()=>switchForm(b.dataset.form));$$('[data-touch-form]').forEach(b=>b.onclick=()=>switchForm(b.dataset.touchForm));$('#touch-bomb').onclick=plantBomb;$('#touch-active').onclick=useActive;
  fullscreenButton.onclick=toggleFullscreen;rotateFullscreenButton.onclick=toggleFullscreen;addEventListener('keydown',e=>{if(e.code==='KeyF'&&!e.repeat&&!e.target?.closest?.('button,a,input,select,textarea')){e.preventDefault();toggleFullscreen()}});document.addEventListener('fullscreenchange',syncFullscreenUI);document.addEventListener('webkitfullscreenchange',syncFullscreenUI);addEventListener('resize',syncViewport,{passive:true});addEventListener('orientationchange',()=>setTimeout(syncViewport,80),{passive:true});window.visualViewport?.addEventListener('resize',syncViewport,{passive:true});screen.orientation?.addEventListener?.('change',syncViewport);syncViewport();syncFullscreenUI();
  for(const part of [...$('#weapon-schematic').children].slice(0,3)){part.addEventListener('pointerenter',e=>{if(e.pointerType==='mouse'&&part.dataset.resource)selectResource(part.dataset.resource,true)});part.addEventListener('focus',()=>{if(part.dataset.resource)selectResource(part.dataset.resource,true)});part.addEventListener('click',()=>{if(part.dataset.resource)selectResource(part.dataset.resource,true)});part.addEventListener('keydown',e=>{if(part.dataset.resource&&(e.code==='Enter'||e.code==='Space')){e.preventDefault();selectResource(part.dataset.resource,true)}})}
  const roomGuideButtons=$$('[data-room-guide]');for(const [index,button] of roomGuideButtons.entries()){const choose=()=>selectRoomGuide(button.dataset.roomGuide,true);button.addEventListener('pointerenter',e=>{if(e.pointerType==='mouse')choose()});button.addEventListener('focus',choose);button.addEventListener('click',choose);button.addEventListener('keydown',e=>{if(e.code==='Enter'||e.code==='Space'){e.preventDefault();e.stopPropagation();choose();return}const step={ArrowLeft:-1,ArrowRight:1,ArrowUp:-4,ArrowDown:4}[e.code];let next=e.code==='Home'?0:e.code==='End'?roomGuideButtons.length-1:Number.isFinite(step)?(index+step+roomGuideButtons.length)%roomGuideButtons.length:null;if(next===null)return;e.preventDefault();e.stopPropagation();next=(next+roomGuideButtons.length)%roomGuideButtons.length;roomGuideButtons[next].focus()})}
  function updateMousePosition(e){const r=canvas.getBoundingClientRect();input.mouseX=(e.clientX-r.left)/r.width*W;input.mouseY=(e.clientY-r.top)/r.height*H;input.mouseInside=true}
  canvas.addEventListener('pointerdown',e=>{if(e.pointerType==='mouse'){updateMousePosition(e);input.pointerAim=true}});canvas.addEventListener('pointermove',e=>{if(e.pointerType==='mouse')updateMousePosition(e)});canvas.addEventListener('pointerleave',e=>{if(e.pointerType==='mouse'&&!input.pointerAim)input.mouseInside=false});addEventListener('pointerup',e=>{if(e.pointerType==='mouse'){input.pointerAim=false;input.shootX=0;input.shootY=0}});
  document.addEventListener('visibilitychange',()=>{if(!audio)return;if(document.hidden){if(musicTimer){clearInterval(musicTimer);musicTimer=null}audio.suspend().catch(()=>{})}else if(musicEnabled)startMusic()});
  function setupStick(el,kind){let id=null;const knob=el.querySelector('i'),set=e=>{const r=el.getBoundingClientRect(),x=e.clientX-(r.left+r.width/2),y=e.clientY-(r.top+r.height/2),m=Math.hypot(x,y),max=r.width*.34,nx=m?x/Math.max(m,max):0,ny=m?y/Math.max(m,max):0,dx=clamp(x,-max,max),dy=clamp(y,-max,max);knob.style.transform=`translate(${dx}px,${dy}px)`;if(kind==='move'){input.moveX=nx;input.moveY=ny}else{input.shootX=nx;input.shootY=ny}};el.addEventListener('pointerdown',e=>{id=e.pointerId;el.setPointerCapture(id);set(e)});el.addEventListener('pointermove',e=>{if(e.pointerId===id)set(e)});const end=e=>{if(e.pointerId!==id)return;id=null;knob.style.transform='';if(kind==='move'){input.moveX=0;input.moveY=0}else{input.shootX=0;input.shootY=0}};el.addEventListener('pointerup',end);el.addEventListener('pointercancel',end)}
  setupStick($('#move-stick'),'move');setupStick($('#shoot-stick'),'shoot');

  async function debugAudioProbe(){const a=ensureAudio();if(!a||!musicAnalyser)return{state:'unavailable',peak:0,rms:0,offlinePeak:0,offlineRms:0,timeDelta:0,notesDelta:0,timer:false,gain:0};try{if(a.state!=='running')await a.resume()}catch{}startMusic();const began=a.currentTime,notesBefore=musicNotes,data=new Float32Array(musicAnalyser.fftSize);let peak=0,rms=0,offlinePeak=0,offlineRms=0;tone(523.25,.28,'square',.055);for(let sample=0;sample<8;sample++){await new Promise(resolve=>setTimeout(resolve,32));musicAnalyser.getFloatTimeDomainData(data);let sum=0;for(const value of data){const absolute=Math.abs(value);peak=Math.max(peak,absolute);sum+=value*value}rms=Math.max(rms,Math.sqrt(sum/data.length))}try{const OfflineEngine=window.OfflineAudioContext||window.webkitOfflineAudioContext,rate=44100,offline=new OfflineEngine(1,Math.ceil(rate*.34),rate),master=offline.createGain(),limiter=offline.createDynamicsCompressor(),osc=offline.createOscillator(),envelope=offline.createGain();master.gain.value=MUSIC_LEVEL;limiter.threshold.value=-18;limiter.knee.value=16;limiter.ratio.value=5;limiter.attack.value=.003;limiter.release.value=.18;osc.type='square';osc.frequency.value=523.25;envelope.gain.setValueAtTime(.0001,0);envelope.gain.exponentialRampToValueAtTime(.12,.012);envelope.gain.exponentialRampToValueAtTime(.0001,.28);osc.connect(envelope).connect(master).connect(limiter).connect(offline.destination);osc.start(0);osc.stop(.3);const rendered=await offline.startRendering(),samples=rendered.getChannelData(0);let sum=0;for(const value of samples){offlinePeak=Math.max(offlinePeak,Math.abs(value));sum+=value*value}offlineRms=Math.sqrt(sum/samples.length)}catch{}return{state:a.state,peak,rms,offlinePeak,offlineRms,timeDelta:a.currentTime-began,notesDelta:musicNotes-notesBefore,timer:!!musicTimer,gain:musicGain?.gain.value||0}}
  function debugShotProfiles(){const a=ensureAudio();if(a&&!shotBuffers)buildShotBuffers(a);const result={};for(const [form,buffer] of Object.entries(shotBuffers||{})){const data=buffer.getChannelData(0);let peak=0,sum=0,signature=0;for(let i=0;i<data.length;i++){peak=Math.max(peak,Math.abs(data[i]));sum+=data[i]*data[i];if(i%97===0)signature+=data[i]*(i+1)}result[form]={duration:+buffer.duration.toFixed(3),peak:+peak.toFixed(4),rms:+Math.sqrt(sum/data.length).toFixed(4),signature:+signature.toFixed(3)}}return result}
  async function debugSuspendedShotProbe(){const a=ensureAudio();if(!a)return{available:false};await requestAudioResume(a);await a.suspend();const before={played:shotSfxPlayed,voices:shotSfxVoices,resumes:audioResumeRequests,time:a.currentTime};for(let i=0;i<200;i++){playShotSound('wraith');beep(330,.03,'square',.01)}const during={played:shotSfxPlayed,voices:shotSfxVoices,resumes:audioResumeRequests,time:a.currentTime,state:a.state};const resumed=await requestAudioResume(a);return{available:true,createdWhileSuspended:during.played-before.played,voiceGrowth:during.voices-before.voices,resumeRequests:during.resumes-before.resumes,timeMoved:during.time-before.time,resumed,state:a.state}}
  function debugPickupPursuitProbe(){const savedPickups=state.pickups,saved={x:player.x,y:player.y,prevX:player.prevX,prevY:player.prevY,form:player.form,items:[...player.items],coins:player.coins};const run=(dt,offset,magnetic)=>{player.form='wraith';player.items=magnetic?['magnet']:[];player.x=ROOM.cx;player.y=ROOM.cy;player.prevX=player.x;player.prevY=player.y;const p=makePickup('coin',player.x-offset,player.y,1);p.vx=p.vy=0;state.pickups=[p];const limit=Math.ceil(1/dt);let frames=0;for(;frames<limit&&state.pickups.length;frames++){player.prevX=player.x;player.prevY=player.y;player.x+=FORMS.wraith.move*dt;updatePickups(dt)}return state.pickups.length?null:+(frames*dt).toFixed(4)};let result;try{const rates=[1/30,1/60,1/120],normal=rates.map(dt=>run(dt,70,false)),magnetic=rates.map(dt=>run(dt,210,true)),times=[...normal,...magnetic];result={normal,magnetic,allCaught:times.every(Number.isFinite),fast:normal.every(t=>t<=.35)&&magnetic.every(t=>t<=.5),frameStable:Math.max(...times)-Math.min(...times)<.18}}finally{state.pickups=savedPickups;Object.assign(player,saved);player.items=saved.items}return result}
  function debugRicochetProbe(){const saved={room:state.room,rooms:state.rooms,obstacles:state.obstacles,enemies:state.enemies,tears:state.tears,particles:state.particles,lastRicochetSound};let result;try{state.room={x:0,y:0,key:'probe',type:'combat',cleared:false};state.rooms=[state.room];state.enemies=[];state.tears=[];state.particles=[];const node=(y=365)=>({x:640,y,r:28,wallGroup:'probe-wall',wallIndex:0,wallAngle:Math.PI/2,wallStep:44,breakable:false,hp:9}),make=(budget,vx=3300,vy=0,x=560,y=365,extra={})=>({x,y,px:x,py:y,vx,vy,r:6,damage:1,life:5,pierce:0,color:COL.cyan,form:'vanguard',age:0,seed:0,ricochetsLeft:budget,ricochetCount:0,volatile:false,tesla:false,crit:false,critMult:2,owner:'player',hit:new Set(),...extra});state.obstacles=[node()];let t=make(1),before=Math.hypot(t.vx,t.vy),removed=moveTear(t,1/30),after=Math.hypot(t.vx,t.vy);const highSpeed=!removed&&t.vx<0&&t.x<606.5&&t.ricochetCount===1,speedConserved=Math.abs(before-after)<1e-6;t=make(1,3300,600,560,340);removed=moveTear(t,1/30);const angled=!removed&&t.vx<0&&Math.abs(t.vy-600)<1e-6&&t.ricochetCount===1;t=make(1);const singleRemoved=moveTear(t,.25),singleBudget=singleRemoved&&t.ricochetCount===1;t=make(2);const doubleFirstRemoved=moveTear(t,.25),doubleBudget=!doubleFirstRemoved&&t.ricochetCount===2&&t.vx>0,doubleThirdRemoved=moveTear(t,.2)&&t.ricochetCount===2;const wallBlockedAll=[{pierce:4},{spectral:true},{form:'seraph',pierce:2}].every(extra=>{const shot=make(0,3300,0,560,365,extra);return moveTear(shot,1/30)&&shot.x<=606.5});t=make(1,-500,0,606.05,365);removed=moveTear(t,.01);const outwardNoCost=!removed&&t.ricochetCount===0&&t.ricochetsLeft===1&&t.x<606;state.obstacles=[node(348),{...node(382),wallIndex:1}];t=make(2);removed=moveTear(t,1/30);const seamSingle=!removed&&t.ricochetCount===1;result={highSpeed,speedConserved,angled,singleBudget,doubleBudget,doubleThirdRemoved,wallBlockedAll,outwardNoCost,seamSingle}}finally{state.room=saved.room;state.rooms=saved.rooms;state.obstacles=saved.obstacles;state.enemies=saved.enemies;state.tears=saved.tears;state.particles=saved.particles;lastRicochetSound=saved.lastRicochetSound}return result}

  function debugMobileLayout(){const box=element=>{const r=element.getBoundingClientRect(),style=getComputedStyle(element),visible=element.checkVisibility?element.checkVisibility({checkOpacity:true,checkVisibilityCSS:true}):style.display!=='none'&&style.visibility!=='hidden'&&r.width>0&&r.height>0;return{x:+r.x.toFixed(1),y:+r.y.toFixed(1),width:+r.width.toFixed(1),height:+r.height.toFixed(1),right:+r.right.toFixed(1),bottom:+r.bottom.toFixed(1),visible}},viewport=window.visualViewport;return{width:Math.round(viewport?.width||innerWidth),height:Math.round(viewport?.height||innerHeight),orientation:document.documentElement.dataset.orientation,fullscreen:!!fullscreenElement()||isStandalone(),fullscreenSupported:!!(document.documentElement.requestFullscreen||document.documentElement.webkitRequestFullscreen),shell:box(shell),rotate:box($('#rotate-screen')),fullscreenButton:box(fullscreenButton),startButton:box($('#start-btn')),moveStick:box($('#move-stick')),shootStick:box($('#shoot-stick'))}}
  function debugDifficultyCurve(){const rooms=COMBAT_CURVE.flatMap((rosters,floor)=>rosters.map((roster,room)=>{const threat=floor*3+room,roomStep=threat%3;return{floor:floor+1,room:room+1,threat,enemies:roster.length,ranged:roster.filter(type=>type==='shooter'||type==='turret').length,roster:[...roster],bulletCap:ROOM_BULLET_CAPS[threat],hpScale:+(1+floor*.22+roomStep*.06).toFixed(2),speedScale:+(1+floor*.04+roomStep*.015).toFixed(3),firstShotMin:+(1.55-Math.min(.28,threat*.035)).toFixed(3),shooter:enemyShotProfile('shooter',threat,false)}})),bosses=BOSS_DATA.flatMap((data,floor)=>BOSS_CURVE[data.id].map((tune,phase)=>({floor:floor+1,boss:data.id,name:data.name,phase:phase+1,pressure:tune.pressure+(data.tier>1?9:0),bulletCap:tune.bulletCap+(data.tier>1?12:0),windup:tune.windup,volley:(tune.radial||0)+(tune.fan||0),cooldown:tune.cooldown})));return{rooms,bosses}}
  function debugThreatState(){const boss=state.enemies.find(e=>e.boss&&!e.dead),regular=state.enemies.filter(e=>!e.boss&&!e.dead);return{bulletCap:enemyBulletCap(),enemyBullets:state.enemyBullets.length,bossFire:boss?.fire??null,summons:regular.length,roster:regular.map(e=>e.type),threats:[...new Set(regular.map(e=>e.threat))],firstFire:regular.length?Math.min(...regular.map(e=>e.fire)):null}}
  function debugEnterCombat(floor=1,rank=0){loadFloor(clamp(Math.round(floor),1,TOTAL_FLOORS));const rooms=state.rooms.filter(r=>r.type==='combat'),room=rooms[clamp(Math.round(rank),0,rooms.length-1)];enterRoom(room,null,true);return debugThreatState()}
  function debugProgressionModel(){return{totalFloors:TOTAL_FLOORS,powerCurve:[...FLOOR_POWER],startKeys:0,keyCap:3,pityMax:2,gates:{treasure:0,shop:0,vault:1,sanctum:2},floors:PLANS.map((plan,index)=>({floor:index+1,power:FLOOR_POWER[index],roomTypes:plan.map(room=>room.type),growthChances:[...GROWTH_CHANCES[index]],growthTiers:{...GROWTH_TIERS[index]},supply:{...SUPPLY_TABLES[index]},choiceCount:index<3?2:3,keyGuarantee:index<3?'last combat if none spawned':'none'})),items:ITEMS.map(item=>({id:item.id,category:item.category,family:item.family,familyLabel:item.familyLabel,rarity:item.rarity,rarityLabel:item.rarityLabel,minFloor:item.minFloor,synergy:item.synergy})),tunings:TUNING.map(item=>({id:item.id,category:'tuning',max:item.max,tag:item.tag})),ascensions:ASCENSIONS.map(item=>({id:item.id,category:item.category,max:item.max,tag:item.tag}))}}
  function debugProgressionState(){const stats=getStats();return{room:{type:state.room?.type,key:state.room?.key,visited:!!state.room?.visited,cleared:!!state.room?.cleared,rewardTier:state.room?.rewardTier||null,rewardTaken:!!state.room?.rewardTaken,gateCost:SPECIAL_ROOM_COSTS[state.room?.type]||0,supply:state.room?.supplyStock?{...state.room.supplyStock}:null},pickupKinds:state.pickups.map(p=>p.kind),pedestals:state.pedestals.filter(p=>!p.taken).map(p=>({id:p.item.id,category:p.item.category||(p.item.stackable?'tuning':'skill'),stackable:!!p.item.stackable,cost:p.cost,group:p.group||null,requiresExit:!!p.requiresExit})),growthMisses:state.growthMisses,floorKeysSpawned:state.floorKeysSpawned,upgrades:{...player.upgrades},stats:{...stats,dps:+(stats.damage*stats.shots/stats.delay).toFixed(3)},rooms:state.rooms.map(room=>({type:room.type,visited:room.visited,rewardTier:room.rewardTier,rewardTaken:room.rewardTaken,gateCost:SPECIAL_ROOM_COSTS[room.type]||0,supply:room.supplyStock?{kind:room.supplyStock.kind,taken:room.supplyStock.taken}:null}))}}
  function debugTryEnterType(type){const room=state.rooms.find(room=>room.type===type);if(!room)return{entered:false,beforeKeys:player.keys,afterKeys:player.keys,current:state.room?.type||null};room.revealed=true;const beforeKeys=player.keys,previous=state.room?.type||null,entered=enterRoom(room,null,true);return{entered,beforeKeys,afterKeys:player.keys,previous,current:state.room?.type||null,visited:room.visited}}
  function debugForceEnterType(type){const room=state.rooms.find(room=>room.type===type);if(!room)return null;room.revealed=true;player.keys=9;enterRoom(room,null,true,true);return state.room?.type||null}
  function debugBossRoomState(){const room=state.room,bosses=state.enemies.filter(e=>e.boss&&!e.dead);return{floor:state.floor,room:room?.type||null,roomKey:room?.key||null,mode:state.mode,cleared:!!room?.cleared,bossStarted:!!room?.bossStarted,bossActive:!!room?.bossActive,portalReady:!!room?.portalReady,portalPresent:!!state.portal,timerActive:!!state.bossIntroTimer,introVisible:ui.bossIntro.classList.contains('visible'),bossCount:bosses.length,bossId:bosses[0]?.bossId||null,bossPhase:bosses[0]?.phase??null,enemyBullets:state.enemyBullets.length,hazards:state.hazards.length,exits:room?neighbors(room).map(n=>n.dir):[]}}
  function debugBossAnimationState(){const boss=state.enemies.find(e=>e.boss&&!e.dead),id=boss?.bossId||ui.introPortrait?.dataset.boss||null,entry=id?ensureBossAtlas(id):null,action=boss?bossAtlasAction(boss):null;return{id,ready:!!entry&&atlasReady(entry.image),columns:entry?.columns||0,rows:entry?.rows||0,frames:entry?.frames||0,totalFrames:Object.values(BOSS_ATLASES).reduce((sum,item)=>sum+item.frames,0),phase:boss?.phase??0,row:boss?.phase??0,frame:action?.frame??0,action:action?.action||(id?'intro':'none'),introBoss:ui.introPortrait?.dataset.boss||null,introVisible:ui.bossIntro.classList.contains('visible'),bladeCount:id==='cherub'?6:0,bladeAngle:boss?.bladeAngle||0,transformPulse:boss?.transformPulse||0}}
  function debugAttemptDoor(dir){const beforeRoom=state.room?.key||null,entered=tryDoor(dir);return{entered,beforeRoom,afterRoom:state.room?.key||null,state:debugBossRoomState()}}
  function debugRelicDamageProbe(){const saved={items:[...player.items],enemies:state.enemies,particles:state.particles,kills:state.kills,critHits:state.critHits},hit=(items,flags={},ricochetCount=0)=>{player.items=items;const enemy={uid:uid++,type:'crawler',x:ROOM.cx+120,y:ROOM.cy,r:18,hp:1000,maxHp:1000,color:COL.pink,invuln:0,dead:false,...flags};state.enemies=[enemy];state.particles=[];damageEnemy(enemy,10,{damage:10,crit:false,critMult:2,form:'vanguard',color:COL.cyan,vx:1,vy:0,ricochetCount});return +(1000-enemy.hp).toFixed(3)};let result;try{result={base:hit([]),ricochet:hit(['saw-memory'],{},1),boss:hit(['executioner'],{boss:true}),combined:hit(['saw-memory','executioner'],{boss:true},1)}}finally{player.items=saved.items;state.enemies=saved.enemies;state.particles=saved.particles;state.kills=saved.kills;state.critHits=saved.critHits}return result}
  function debugPreviewGrowth(floor,rank,roll,pity=0,tierRoll=.5){floor=clamp(Math.round(floor),1,TOTAL_FLOORS);rank=clamp(Math.round(rank),0,2);const probability=GROWTH_CHANCES[floor-1][rank],spawn=pity>=2||roll<probability;return{floor,rank,probability,spawn,tier:spawn?growthTierFor(floor,tierRoll):'none',choices:spawn?(floor<4?2:3):0}}
  if(location.search.includes('test=1'))window.__GENESIS_DEBUG__={
    start:resetRun,snapshot:()=>({mode:state.mode,floor:state.floor,room:state.room?.type,form:player.form,x:player.x,y:player.y,aimX:player.aimX,aimY:player.aimY,faceX:player.faceX,hp:player.hp,maxHp:player.maxHp,shield:player.shield,maxShield:player.maxShield,invuln:player.invuln,coins:player.coins,keys:player.keys,bombs:player.bombs,activeCharge:player.activeCharge,activeMax:player.activeMax,items:[...player.items],upgrades:{...player.upgrades},stats:getStats(),shotSequence:player.shotSequence,killHarvest:player.killHarvest,familiars:familiarCount(),relicSlots:ui.relicSlots.children.length,enemies:state.enemies.length,tears:state.tears.length,tearForms:[...new Set(state.tears.map(t=>t.form))],tearPierces:state.tears.map(t=>t.pierce),tearRicochets:state.tears.map(t=>({left:t.ricochetsLeft,count:t.ricochetCount})),muzzleFx:state.particles.filter(p=>p.kind==='muzzle').length,impactFx:state.particles.filter(p=>p.kind==='impact').length,ricochetFx:state.particles.filter(p=>p.kind==='ricochet').length,enemyBullets:state.enemyBullets.length,bossBullets:state.enemyBullets.filter(b=>b.source==='boss').length,enemySourceBullets:state.enemyBullets.filter(b=>b.source==='enemy').length,hazards:state.hazards.length,bombEntities:state.bombEntities.length,pedestals:state.pedestals.filter(p=>!p.taken).length,pedestalItems:state.pedestals.filter(p=>!p.taken).map(p=>p.item.id),pedestalPositions:state.pedestals.filter(p=>!p.taken).map(p=>({x:p.x,y:p.y})),pickups:state.pickups.length,pickupKinds:state.pickups.map(p=>p.kind),pickupFx:Math.max(0,...state.pickups.map(p=>p.spawnFx||0)),damageTexts:state.particles.filter(p=>p.kind==='text').map(p=>String(p.text)),obstacles:state.obstacles.length,wallGroups:new Set(state.obstacles.map(o=>o.wallGroup).filter(Boolean)).size,wallAligned:state.obstacles.filter(o=>o.wallGroup).every(o=>Number.isFinite(o.wallAngle)&&Number.isInteger(o.wallIndex)),secretRevealed:state.rooms.some(r=>r.type==='secret'&&r.revealed),roomTypes:state.rooms.map(r=>r.type),growthMisses:state.growthMisses,rewardTier:state.room?.rewardTier||null,critHits:state.critHits,boss:state.enemies.find(e=>e.boss)?.bossId||null,bossPhase:state.enemies.find(e=>e.boss)?.phase??null,roomsCleared:state.roomsCleared,musicEnabled,audioState:audio?.state||'uninitialized',musicTimerActive:!!musicTimer,musicTicks,musicNotes,musicGainValue:musicGain?.gain.value||0,storyIndex,victory:state.victory}),
    probeAudio:debugAudioProbe,probeShotProfiles:debugShotProfiles,probeSuspendedShotAudio:debugSuspendedShotProbe,shotAudioState:()=>({ready:!!shotBuffers,voices:shotSfxVoices,played:shotSfxPlayed,resumePending:!!audioResumePromise,resumeRequests:audioResumeRequests}),spriteState:()=>Object.fromEntries(Object.entries(SPRITE_ATLASES).map(([id,image])=>[id,{ready:atlasReady(image),width:image.naturalWidth,height:image.naturalHeight}])),playerPoseState:()=>({upright:true,rotation:0,anchorX:player.x,anchorY:player.y,facing:player.faceX<0?-1:1,aimX:player.aimX,aimY:player.aimY,recoilX:-(player.recoil||0)*player.aimX,recoilY:-(player.recoil||0)*player.aimY}),bossAnimationState:debugBossAnimationState,mobileLayout:debugMobileLayout,toggleFullscreen,difficultyCurve:debugDifficultyCurve,progressionModel:debugProgressionModel,progressionState:debugProgressionState,previewGrowth:debugPreviewGrowth,relicDamageProbe:debugRelicDamageProbe,tryEnterType:debugTryEnterType,forceEnterType:debugForceEnterType,bossRoomState:debugBossRoomState,attemptDoor:debugAttemptDoor,feedbackState:()=>({shake:state.shake,flash:state.flash,invuln:player.invuln}),threatState:debugThreatState,enterCombat:debugEnterCombat,loadFloor:number=>loadFloor(clamp(Math.round(number),1,TOTAL_FLOORS)),probePickupPursuit:debugPickupPursuitProbe,probePlayerRicochet:debugRicochetProbe,
    spawnPickup:(kind='coin',value=3)=>{state.pickups.push(makePickup(kind,player.x+120,player.y,value));return window.__GENESIS_DEBUG__.snapshot()},
    probeFastPickup:()=>{const savedPickups=state.pickups,saved={x:player.x,y:player.y,prevX:player.prevX,prevY:player.prevY,coins:player.coins};let p=makePickup('coin',ROOM.cx,ROOM.cy,2);p.vx=p.vy=0;state.pickups=[p];player.prevX=ROOM.cx-180;player.prevY=ROOM.cy;player.x=ROOM.cx+180;player.y=ROOM.cy;const before=player.coins;updatePickups(0);const crossed=state.pickups.length===0&&player.coins===before+2;p=makePickup('coin',ROOM.cx,ROOM.cy+60,1);p.vx=p.vy=0;state.pickups=[p];player.coins=before;player.prevX=ROOM.cx-180;player.prevY=ROOM.cy;player.x=ROOM.cx+180;player.y=ROOM.cy;updatePickups(0);const noFalsePositive=state.pickups.length===1&&player.coins===before;state.pickups=savedPickups;Object.assign(player,saved);return crossed&&noFalsePositive},
    clearWeaponFx:()=>{state.tears=[];state.particles=[];return window.__GENESIS_DEBUG__.snapshot()},showcaseRicochet:()=>{const o=state.obstacles.find(o=>o.wallGroup)||state.obstacles[0];if(!o)return window.__GENESIS_DEBUG__.snapshot();state.tears=[];state.particles=[];let nx=-Math.sin(o.wallAngle||Math.PI/4),ny=Math.cos(o.wallAngle||Math.PI/4),gap=o.r+58,x=o.x-nx*gap,y=o.y-ny*gap;if(x<ROOM.x+20||x>ROOM.x+ROOM.w-20||y<ROOM.y+20||y>ROOM.y+ROOM.h-20){nx=-nx;ny=-ny;x=o.x-nx*gap;y=o.y-ny*gap}spawnTear(x,y,Math.atan2(ny,nx),{speed:700,range:1000,size:7,damage:1,pierce:0,ricochets:2,critChance:0});for(let i=0;i<9;i++)updateTears(1/60);return window.__GENESIS_DEBUG__.snapshot()},fire:(x=1,y=0)=>{player.fireCd=0;firePlayer(x,y);return window.__GENESIS_DEBUG__.snapshot()},injectEnemyThreat:()=>{const e=state.enemies.find(x=>!x.boss&&!x.dead);if(e)enemyBullet(e,0,100);return window.__GENESIS_DEBUG__.snapshot()},
    showcaseBossThreat:()=>{const b=state.enemies.find(e=>e.boss);if(b){enemyBullet(b,0,120);const shot=state.enemyBullets[state.enemyBullets.length-1];shot.x=player.x+105;shot.y=player.y-28;shot.vx=0;shot.vy=0;targetHazard(player.x,player.y,70,1,1)}return window.__GENESIS_DEBUG__.snapshot()},
    switchForm:id=>{player.formCd=0;switchForm(id)},enterType:debugForceEnterType,forceReward:tier=>{forcedRewardTier=tier},forceSupply:kind=>{forcedSupplyKind=kind},clearRoom:()=>{for(const e of [...state.enemies])killEnemy(e)},collectAllPickups:()=>{for(const pickup of state.pickups)collectPickup(pickup);state.pickups=[];return window.__GENESIS_DEBUG__.snapshot()},takePedestal:(index=0)=>claimPedestal(state.pedestals.filter(p=>!p.taken)[index]),reroll:()=>{useActive();return window.__GENESIS_DEBUG__.snapshot()},giveItem:id=>{const item=[...ITEMS,...TUNING,...ASCENSIONS].find(i=>i.id===id);if(item)takeItem(item);return window.__GENESIS_DEBUG__.snapshot()},setResources:values=>Object.assign(player,values),setPlayer:(x,y)=>{player.x=x;player.y=y;player.prevX=x;player.prevY=y},setCrit:value=>{player.critOverride=value},setInvuln:value=>{player.invuln=value},hitFirstEnemy:(damage=3.6,crit=false)=>{const e=state.enemies.find(x=>!x.dead);if(e){e.invuln=0;damageEnemy(e,damage,{damage,crit,critMult:2,form:player.form,color:FORMS[player.form].color,vx:1,vy:0})}return window.__GENESIS_DEBUG__.snapshot()},probeBulletWall:()=>{const o=state.obstacles[0];if(!o)return false;state.enemyBullets=[{x:o.x-o.r-12,y:o.y,vx:300,vy:0,r:6,life:2,color:COL.pink,damage:1,source:'enemy',spin:0,phase:0}];for(let i=0;i<12&&state.enemyBullets.length;i++)updateEnemyBullets(1/60);return state.enemyBullets.length===0},injectThreats:()=>{const b=state.enemies.find(e=>e.boss);if(b){enemyBullet(b,0,120);targetHazard(player.x,player.y,70,1,1)}return window.__GENESIS_DEBUG__.snapshot()},spawnBossNow:()=>{cancelBossIntro();state.mode='playing';if(!state.enemies.some(e=>e.boss))spawnBoss(BOSS_DATA[state.floor-1],state.room);return window.__GENESIS_DEBUG__.snapshot()},dropBoss:()=>{state.enemies=state.enemies.filter(e=>!e.boss);return debugBossRoomState()},setBossRoomState:values=>{if(state.room?.type==='boss'){Object.assign(state.room,values);if(values?.cleared)state.enemies=[];if(values&&'portalReady'in values&&!values.portalReady)state.portal=null}return debugBossRoomState()},setBossPhase:phase=>{const b=state.enemies.find(e=>e.boss);if(b){b.hp=b.maxHp*(phase===0?.9:phase===1?.6:.25);b.invuln=0;updateBoss(b,.016)}},killBoss:()=>{const b=state.enemies.find(e=>e.boss);if(b){b.invuln=0;killEnemy(b)}},takePortal:()=>{if(state.portal){player.x=state.portal.x;player.y=state.portal.y;updatePortal()}},nextFloor:()=>{if(state.floor<TOTAL_FLOORS)loadFloor(state.floor+1)},damage:()=>hurtPlayer(1),tick:steps=>{for(let i=0;i<(steps||1);i++)update(1/60);return window.__GENESIS_DEBUG__.snapshot()},renderNow:()=>{player.invuln=0;state.itemTimer=0;ui.itemBanner.classList.add('hidden');render()},simulate:steps=>{player.invuln=999;for(let i=0;i<(steps||600);i++)update(1/60);return window.__GENESIS_DEBUG__.snapshot()},autoplay:(steps=6000)=>{player.invuln=999;for(let i=0;i<steps&&state.mode==='playing'&&state.enemies.length;i++){const target=nearestEnemy(player);if(target){const a=Math.atan2(target.y-player.y,target.x-player.x);player.aimX=Math.cos(a);player.aimY=Math.sin(a);if(player.fireCd<=0)firePlayer(player.aimX,player.aimY);const orbit=i*.018;player.x=ROOM.cx+Math.cos(orbit)*130;player.y=ROOM.cy+Math.sin(orbit)*90}update(1/60)}return window.__GENESIS_DEBUG__.snapshot()}
  };
  if('serviceWorker'in navigator&&location.protocol.startsWith('http'))navigator.serviceWorker.register('service-worker.js').catch(()=>{});
  function frame(now){const dt=Math.min(.033,(now-last)/1000||.016);last=now;update(dt);render();requestAnimationFrame(frame)}updateMusicButton();updateUI();requestAnimationFrame(frame);
})();
